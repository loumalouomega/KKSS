% Shell icon: settings — three slider rails with knobs (app preferences),
% the same three-bar rhythm as cad's hamburger but with adjustment knobs.
\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{arrows.meta,calc,shapes.geometric}
\begin{document}
\begin{tikzpicture}[line width=1.3pt, line cap=round, line join=round, >=Stealth, x=1mm,y=1mm]
  \draw[thick] (-9,6) -- (9,6);
  \filldraw (-3,6) circle (2);
  \draw[thick] (-9,0) -- (9,0);
  \filldraw (4,0) circle (2);
  \draw[thick] (-9,-6) -- (9,-6);
  \filldraw (-1,-6) circle (2);
\end{tikzpicture}
\end{document}
