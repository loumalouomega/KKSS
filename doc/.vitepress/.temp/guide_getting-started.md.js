import { t as _plugin_vue_export_helper_default } from "./plugin-vue_export-helper.BOaGB7Aw.js";
import { t as cad_viewer_default } from "./cad-viewer.CRIx3Wuk.js";
import { t as mesh_viewer_default } from "./mesh-viewer.DgnEvCrG.js";
import { ssrRenderAttr, ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
//#region ../../../../../screenshots/home-screen.png
var home_screen_default = "/KKSS/screenshots/home-screen.png";
//#endregion
//#region ../../../../../screenshots/shell-toolbar.png
var shell_toolbar_default = "/KKSS/screenshots/shell-toolbar.png";
//#endregion
//#region ../../../../../screenshots/chat-kratos-setup.png
var chat_kratos_setup_default = "/KKSS/screenshots/chat-kratos-setup.png";
//#endregion
//#region ../../../../../screenshots/kratos-jobs.png
var kratos_jobs_default = "/KKSS/screenshots/kratos-jobs.png";
//#endregion
//#region ../../../../../screenshots/settings.png
var settings_default = "/KKSS/screenshots/settings.png";
//#endregion
//#region guide/getting-started.md
var __pageData = JSON.parse("{\"title\":\"Getting Started\",\"description\":\"\",\"frontmatter\":{},\"headers\":[],\"relativePath\":\"guide/getting-started.md\",\"filePath\":\"guide/getting-started.md\"}");
var _sfc_main = { name: "guide/getting-started.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
	_push(`<div${ssrRenderAttrs(_attrs)}><h1 id="getting-started" tabindex="-1">Getting Started <a class="header-anchor" href="#getting-started" aria-label="Permalink to “Getting Started”">​</a></h1><p>KKSS (Keep Kratos Simple Stupid) is a desktop application for preparing and inspecting <a href="https://github.com/KratosMultiphysics/Kratos" target="_blank" rel="noreferrer">Kratos Multiphysics</a> simulation models. It bundles two proven viewers — the <a href="https://github.com/loumalouomega/CAD-Preview" target="_blank" rel="noreferrer">CAD-Preview</a> and <a href="https://github.com/loumalouomega/VSCode-MDPA-Preview" target="_blank" rel="noreferrer">VSCode-MDPA-Preview</a> VS Code extensions — into one window with a mode toggle.</p><h2 id="installation" tabindex="-1">Installation <a class="header-anchor" href="#installation" aria-label="Permalink to “Installation”">​</a></h2><p>Grab the installer for your platform and architecture from the <a href="/KKSS/download">download page</a> (built for every release tag by CI):</p><table tabindex="0"><thead><tr><th>Platform</th><th>Architectures</th><th>Artifact</th></tr></thead><tbody><tr><td>Linux</td><td>x86-64, ARM 64</td><td><code>.AppImage</code> (portable), <code>.deb</code> or <code>.rpm</code></td></tr><tr><td>Windows</td><td>x86-64, ARM 64</td><td>NSIS <code>.exe</code> installer</td></tr><tr><td>macOS</td><td>Apple Silicon (ARM 64)</td><td><code>.dmg</code> / <code>.zip</code></td></tr></tbody></table><div class="tip custom-block"><p class="custom-block-title">macOS Gatekeeper</p><p>Release builds are currently unsigned. On macOS, right-click the app and choose <strong>Open</strong> the first time to bypass Gatekeeper. A signed/notarized macOS release will remove that first-launch step when Apple credentials are enabled in release CI.</p></div><h2 id="the-home-screen" tabindex="-1">The home screen <a class="header-anchor" href="#the-home-screen" aria-label="Permalink to “The home screen”">​</a></h2><p>KKSS opens on a main menu with one button per task — <strong>Pre-Processing</strong>, <strong>Post-Processing</strong>, and <strong>Help</strong>. Pick a mode to enter it; the <strong>Home</strong> button in the toolbar (or <code>Ctrl+0</code> / <strong>View ▸ Home</strong>) brings the menu back at any time without losing what&#39;s loaded in either mode.</p><p>Below the buttons, <strong>Recent files</strong> lists what you opened last — CAD models and meshes together, newest first. Clicking one reopens it in the mode it belongs to, so a <code>.stp</code> goes to Pre-Processing and an <code>.mdpa</code> to Post-Processing without you choosing. Files that have moved or been deleted drop off the list by themselves, and <strong>Clear</strong> forgets all of them. The same list is in <strong>File ▸ Open Recent</strong> (ten entries there, five here).</p><p><img${ssrRenderAttr("src", home_screen_default)} alt="The home screen" width="4080" height="2580"></p><h2 id="the-two-modes" tabindex="-1">The two modes <a class="header-anchor" href="#the-two-modes" aria-label="Permalink to “The two modes”">​</a></h2><p>The toolbar at the top of the window holds the mode toggle:</p><p><img${ssrRenderAttr("src", shell_toolbar_default)} alt="The shell toolbar: mode toggle, Open button, and the current file" width="5280" height="222"></p><ul><li><strong>🔷 Pre-Processing</strong> — CAD geometry and model preparation (<a href="/KKSS/guide/cad-mode">details</a>). Opens STEP, IGES, BREP, STL, OBJ, PLY, glTF, and OpenSCAD <code>.csg</code>/<code>.scad</code> (a <code>.scad</code> needs a local <code>openscad</code> binary). <strong>File ▸ New Blank Model…</strong> starts an empty one instead.</li><li><strong>🔶 Post-Processing</strong> — mesh inspection, modification, and result visualization (<a href="/KKSS/guide/mesh-mode">details</a>). Opens MDPA, VTK (legacy + XML), STL, OBJ, PLY, and 39 extended mesh formats via meshio++ (Gmsh, Abaqus, Nastran, UNV, Medit, Netgen, SU2, XDMF, Exodus, CGNS, MED, EnSight Gold, Triangle, …). Result fields render as combinable contour/isosurface/quiver/deformed-shape modes, and an <strong>Advanced</strong> toolbar menu holds the Mesh Size panel (nodal/element size statistics), sphere glyphs for particle meshes, face normals for spotting inverted elements, and boundary-skin export.</li></ul><p>Both mode views stay alive when you switch: the loaded file, the camera, and your undo history are all preserved. The two viewers share one layout — a document chip in the menubar, an icon-headed sidebar with a collapsed <strong>Advanced</strong> group, a floating toolbar and dock, and a status bar — described in <a href="/KKSS/guide/cad-mode#reading-the-window-at-a-glance">Pre-Processing</a> and <a href="/KKSS/guide/mesh-mode#reading-the-window-at-a-glance">Post-Processing</a>. The app&#39;s own toolbar, home screen, chat and jobs panels use the same look, and the <strong>Terminal</strong>, <strong>Chat</strong> and <strong>Jobs</strong> buttons show as pressed while their panel is open.</p><h3 id="the-project-folder" tabindex="-1">The project folder <a class="header-anchor" href="#the-project-folder" aria-label="Permalink to “The project folder”">​</a></h3><p><strong>File ▸ Open Folder…</strong> sets a project folder — the one place KKSS treats as &quot;where you are working&quot;. It is remembered across launches, shown in the toolbar (hover for the full path, click to change) and on the home screen, and it seeds:</p><ul><li>the <strong>terminal</strong>&#39;s working directory,</li><li>the starting folder of <strong>every file dialog</strong> — Open, Save As, exports, the text editor,</li><li>the <strong>AI assistant</strong>&#39;s context, so it knows where to put files it creates,</li><li><strong>project-local problemtypes</strong>: a <code>.kratos/problemtypes/*.js</code> file inside the folder shows up in the Post-Processing Problemtype list alongside the built-ins. This only happens for a folder you chose explicitly — opening a mesh that merely sits next to one never runs it.</li></ul><p>It is a <strong>default, not a restriction</strong>. Nothing is refused for living outside it: you can open, save and export anywhere as always, and the MCP tools are not sandboxed to it. Until you set one, KKSS infers the folder from the document you are working on — exactly what it did before — so everything works with no setup.</p><p><strong>File ▸ Clear Project Root</strong> goes back to that inferred behavior. Note the terminal reads its directory once, when its shell starts, so changing the folder applies to the <em>next</em> shell rather than the one already running.</p><p>A folder kept in sync by a desktop client — <code>~/Google Drive</code>, <code>~/Dropbox</code>, a OneDrive folder — is a perfectly good project folder, and always has been: to KKSS it is an ordinary directory. Every sidecar KKSS writes beside your model now lands through a temp file that is renamed into place, so a sync daemon can never pick up a half-written one or raise a spurious &quot;conflicted copy&quot;. The only visible trace is a <code>.tmp</code> file that exists for a few milliseconds during each save.</p><h3 id="guided-simulation-work" tabindex="-1">Guided simulation work <a class="header-anchor" href="#guided-simulation-work" aria-label="Permalink to “Guided simulation work”">​</a></h3><p>Home&#39;s <strong>Check environment</strong> action probes the configured Python interpreter used for manual runs and the assistant&#39;s uv-managed tool runtime separately. It checks the selected built-in problemtype&#39;s Kratos application imports, available CPU and memory, and whether the current run directory exists and is writable. The report includes each resolved executable and version and does not install packages. A failed manual check disables the mesh Problemtype Run action and guided queue launches while geometry browsing stays available; the action checks again before starting. A thread recommendation appears only when the manual runner exposes verified thread control. MPI has no verified runner contract yet, so the report shows it as unavailable and the UI does not offer rank controls. If the assistant runtime is missing, Home offers its existing retry and uv setup actions; interpreter changes remain in Settings ▸ Kratos.</p><p>With an explicit project folder, <strong>New study</strong> records a geometry reference and its content revision in <code>.kkss/project.json</code>. Project-contained paths follow the folder when it moves; external paths remain explicit references. Home shows geometry, mesh, case, run and result readiness, can open an available geometry, mesh/case setup, and result in their existing viewers, and can duplicate study settings. Geometry stays external by default; <strong>Copy geometry in project</strong> creates a project-local copy, while <strong>Relink geometry</strong> explicitly repairs a missing or changed source. After exporting a mesh, <strong>Attach mesh</strong> records its revision and imports the adjacent case setup when one exists. <strong>Plan run</strong> previews isolated mesh, case-generation and solve tasks under <code>.kkss/runs/&lt;id&gt;/</code>. <strong>Plan sweep</strong> expands one case-setting path and up to 50 values into fresh variant studies and run destinations. Plans persist paused; Resume is bound to the concrete plan revision, and the queue reconciles recorded CAD mesh-export and solver request identities after restart. Home can resume one waiting variant row while holding other rows, or preview a failed row retry with fresh study and run identities. Comparisons label mesh-sensitivity and solver-parameter studies separately using recorded mesh revisions and settings. A terminal run can be imported as an immutable input snapshot; large results remain revision-checked links. <strong>Review run</strong> and <strong>Export review</strong> report generated-input revisions, MDPA counts, the existing mesh-quality diagnostics and versioned structural solve-step outcomes in offline JSON/HTML. <strong>Evaluate quantity</strong> lets you select a result field, location, component, region, time step, reduction and unit; the result record keeps the exact source artifact revision. Units must be declared explicitly. If that result later changes, its saved value is shown as stale and omitted. Variant comparison pairs matching definitions only when their units agree; missing values stay missing. Unsupported-problemtype convergence remains explicitly unavailable. The versioned structural monitor records the solver&#39;s residual norm and convergence criterion parameters for nonlinear residual-criterion runs. Linear solves, other criteria, interrupted runs and truncated monitor files remain explicitly unavailable; process success alone never proves convergence. Run reviews include the upstream preparation report, generated input hashes and runtime identity when available. The assistant has the same environment, study, queue, review, quantity and variant tools under the normal transcript approval policy; preview never launches work.</p><h3 id="working-from-cloud-storage" tabindex="-1">Working from cloud storage <a class="header-anchor" href="#working-from-cloud-storage" aria-label="Permalink to “Working from cloud storage”">​</a></h3><p>If you would rather not run a sync client at all, <strong>Settings ▸ Cloud Accounts</strong> connects KKSS directly to Google Drive, Dropbox or OneDrive, and <strong>File ▸ Open from Cloud…</strong> browses it. You bring your own OAuth client — see <a href="/KKSS/guide/configuration#cloud-accounts">Configuration ▸ Cloud accounts</a> for the one-time setup.</p><p>Opening a cloud file <strong>downloads it into a local staging cache</strong>, along with any of its sidecars that exist beside it, and everything after that behaves exactly like a local document: the same viewers, the same tools, the same assistant. Cloud tabs are marked with a ☁ in the tab strip.</p><p>Saving pushes it back. That happens on <strong>File ▸ Save</strong>, and also a few seconds after any automatic save — the CAD viewer writes your parts, edits, camera and meshing options continuously, and those ride along too. A burst of edits is coalesced into a single upload.</p><p>What to expect in the awkward cases:</p><ul><li><strong>Someone else changed the file while you had it open.</strong> KKSS never overwrites their version and never discards yours. Your copy is uploaded beside theirs as <code>model (conflict 2026-09-06 14-03-11).stp</code> and a warning tells you so. Nothing on your disk is touched.</li><li><strong>You quit with an upload still running.</strong> KKSS holds the quit open for up to ten seconds to finish. If that is not enough — or the machine loses power — the local copy is still there and is flagged; the next launch tells you which files have changes that never reached the provider.</li><li><strong>Recent files.</strong> A cloud document stays in <strong>File ▸ Open Recent</strong> even after the cache trims its local copy; clicking it downloads again. Restoring the previous session on launch does not re-download, because that would mean network access before the window appears — anything trimmed is reported in the usual &quot;files could not be found&quot; notice instead.</li><li><strong>The macro library is not synced.</strong> <code>cad-preview-macros.json</code> belongs to a whole folder while the cache holds one document per directory, so uploading it would let two models from the same remote folder overwrite each other&#39;s macros. It stays local.</li><li><strong>Nested export trees stay local too.</strong> A sibling companion file (XDMF&#39;s <code>.h5</code>) is uploaded; an OpenFOAM case, which writes a whole <code>constant/polyMesh/</code> subdirectory, is not.</li></ul><p>The cache is trimmed on an LRU budget (<strong>Settings ▸ Cloud Accounts ▸ Cache Size Limit…</strong>, 2 GB by default). A document that is open, or that holds an unsent change, is never trimmed.</p><h3 id="picking-up-where-you-left-off" tabindex="-1">Picking up where you left off <a class="header-anchor" href="#picking-up-where-you-left-off" aria-label="Permalink to “Picking up where you left off”">​</a></h3><p>KKSS reopens your last session on launch: every document you had open in each mode, which one was focused, the screen you were on, and whether the terminal or chat panel was showing — and the chat sidebar comes back on the conversation you were last in. Documents that have since been deleted or moved are quietly dropped, with a note saying how many — you never land on a tab pointing at a file that isn&#39;t there.</p><p>Turn it off with <strong>Settings ▸ Restore Last Session</strong> to start clean every time. (Setting <code>KKSS_NO_RESTORE=1</code> in the environment does the same for one launch, which is handy if a particular document is giving the viewer trouble.) Opening a file from the command line always wins: it takes the screen and gets its own tab, leaving the restored documents untouched.</p><p>The toolbar also has an <strong>interface-scale</strong> picker on the right (75 %–150 %) for adjusting how large the whole app appears — useful on high-DPI or low-resolution displays. It scales every part of the window (toolbar, viewers, terminal, chat) together, is remembered across launches, and can also be driven from the keyboard: <code>Ctrl +</code> / <code>Ctrl -</code> step through the sizes and <code>Ctrl+Shift+0</code> resets to 100 % (also under <strong>View ▸ Zoom In / Zoom Out / Reset Zoom</strong>).</p><table tabindex="0"><thead><tr><th>🔷 Pre-Processing</th><th>🔶 Post-Processing</th></tr></thead><tbody><tr><td><img${ssrRenderAttr("src", cad_viewer_default)} alt="CAD mode" width="5280" height="4578"></td><td><img${ssrRenderAttr("src", mesh_viewer_default)} alt="Mesh mode" width="5280" height="4578"></td></tr></tbody></table><h2 id="about-updates" tabindex="-1">About &amp; updates <a class="header-anchor" href="#about-updates" aria-label="Permalink to “About &amp; updates”">​</a></h2><p><strong>Help</strong> on the home screen (or <strong>Help ▸ About KKSS…</strong>) shows the app version and checks GitHub for a newer release. <strong>Include prerelease updates</strong> in Settings controls whether stable-only or prerelease releases are selected. When an update exists, <strong>Update now</strong> downloads and installs it in place on Windows and on the Linux AppImage — restart when prompted. <code>.deb</code>, <code>.rpm</code> and unsigned macOS installs instead get a button to the releases page. No network? The dialog still shows your version and offers a Retry.</p><p>Installing a supported file type (<code>.stp</code>, <code>.mdpa</code>, <code>.vtk</code>, <code>.post.msh</code> and the other formats shown in the Open dialog) opens it in KKSS when the file is double-clicked. The <code>kkss://open?file=…</code> link form accepts the same local files; links to application pages, relative paths and unsupported files are rejected.</p><p>After an update, the next launch automatically pops up a <strong>What&#39;s New</strong> window listing what changed since the version you last ran — no popup on a first install, and it only ever shows entries newer than what you&#39;d already seen. Dismiss it with <strong>Got it</strong> (or Esc); reopen the full history any time from <strong>Help ▸ What&#39;s New…</strong>.</p><h2 id="embedded-terminal" tabindex="-1">Embedded terminal <a class="header-anchor" href="#embedded-terminal" aria-label="Permalink to “Embedded terminal”">​</a></h2><p>The <strong>Terminal</strong> toolbar button (or <code>Ctrl+\` </code> / <strong>View ▸ Toggle Terminal</strong>) opens a shell panel below the viewer — handy for launching Kratos runs (<code>python MainKratos.py</code>) while watching the model. The session starts in the current file&#39;s directory, runs PowerShell on Windows and your <code>\$SHELL</code> on macOS/Linux (changeable under <strong>Settings ▸ Terminal Shell</strong>), keeps running while hidden (hide it with the <strong>Hide</strong> button in the panel&#39;s corner, the toolbar button, or <code>Ctrl+\` </code>), and offers a restart when the shell exits. The panel is shared by both modes; its height is fixed in this version.</p><h2 id="ai-assistant" tabindex="-1">AI assistant <a class="header-anchor" href="#ai-assistant" aria-label="Permalink to “AI assistant”">​</a></h2><p>The <strong>Chat</strong> toolbar button (or <code>Ctrl+Shift+L</code> / <strong>View ▸ Toggle AI Chat</strong>) opens a chat sidebar on the right where an LLM can drive KKSS for you: load and edit CAD models, define sub-model-parts, generate and export meshes, inspect and transform MDPA/VTK files, set up Kratos cases, and run simulations. The assistant works through the same tool servers (MCP) that power the two viewers plus the standalone <a href="https://pypi.org/project/kratos-mcp-server/" target="_blank" rel="noreferrer">kratos-mcp-server</a>; the three dots in the sidebar header show each server&#39;s status (green = ready, red = unavailable — hover for details). The first two ship with KKSS; the Kratos one is fetched with <a href="https://docs.astral.sh/uv/" target="_blank" rel="noreferrer"><code>uvx</code></a> and can install an app-local copy of uv when it is missing.</p><p>The assistant can also pack a run&#39;s per-step results into an XDMF timeline with <code>mesh_pack_series</code>, matching <strong>File ▸ Pack Time Series Into One File…</strong>. Keep the resulting <code>.xdmf</code> and its sibling <code>.h5</code> together when copying or sharing the results.</p><p>Each message uses the tools available when you send it. If a server becomes ready while the assistant is responding, its tools become available on your next message. You can wait for its status dot to turn green before sending a task that needs it.</p><p>If Kratos cannot start, a message below the chat header explains the failure. <strong>Install uv for KKSS</strong> downloads the official Astral installer and installs uv into KKSS’s data directory without administrator privileges or changes to your PATH or shell profiles. It runs only when you click the button. If uv already works, KKSS uses it instead. <strong>Retry</strong> reconnects after a network, package, or startup failure without restarting CAD, mesh, or the app; a first download can take up to five minutes. Normal chat remains available during setup.</p><p><img${ssrRenderAttr("src", chat_kratos_setup_default)} alt="Kratos runtime setup in the chat sidebar" width="540" height="1137"></p><p>This installs the uv launcher, not a bundled Kratos engine or Python runtime. uv keeps its usual cache and Python locations. A compatible Python MCP dependency is selected alongside the pinned Kratos tool server.</p><p>Before first use, pick a provider under <strong>Settings ▸ LLM Assistant</strong>. API-backed providers use a key:</p><ul><li><strong>Anthropic (Claude)</strong> — the default; set <em>Anthropic API Key</em> (and optionally the model, default <code>claude-opus-4-8</code>).</li><li><strong>OpenAI-compatible</strong> — any <code>chat/completions</code> backend: set the <em>Base URL</em> (e.g. <code>https://api.openai.com/v1</code> or <code>http://localhost:11434/v1</code> for Ollama), the model name, and a key if the backend needs one.</li></ul><p>You can also use a subscription account through the official local agent tools:</p><ul><li><strong>ChatGPT subscription (Codex)</strong> — install the Codex CLI, run <code>codex login</code>, then select this provider. KKSS uses Codex App Server with only KKSS&#39;s MCP tools enabled.</li><li><strong>Claude subscription (Claude Code)</strong> — install Claude Code, run <code>claude auth login</code>, then select this provider. KKSS uses the Claude Agent SDK with a private KKSS MCP bridge.</li></ul><p>Use each provider&#39;s <strong>Check installation and sign-in…</strong> action to verify the executable and account. Subscription mode uses the account managed by the official tool, removes inherited API-key and gateway variables, and never falls back to paid API requests. Provider subscription limits and model availability still apply. The <em>Executable path…</em> action is available when automatic detection cannot find the installed tool.</p><p>Subscription runtimes are restricted to KKSS tools; their shell, file, web, plugin, connector, and sub-agent tools are disabled. The same KKSS approval dialog still appears before tools that change files. Conversations retain a provider session when possible; if a provider session has expired or is missing, KKSS starts a new session from the saved transcript without replaying historical tool calls.</p><p>Keys are stored encrypted with your OS keychain when available. Edits made by the assistant land in the same sidecar files the viewers use — reload the file to see them. Send with <code>Enter</code> and stop a running response with the same button.</p><h3 id="approving-what-the-assistant-does" tabindex="-1">Approving what the assistant does <a class="header-anchor" href="#approving-what-the-assistant-does" aria-label="Permalink to “Approving what the assistant does”">​</a></h3><p>The assistant&#39;s tools are not suggestions — they write files, overwrite meshes and start solves. <code>mesh_transform</code>, for instance, overwrites the file it is given when you don&#39;t name an output path. So <strong>a tool that changes anything is shown to you before it runs</strong>: the call appears in the transcript with its full arguments and three buttons.</p><ul><li><strong>Allow</strong> — run it this once.</li><li><strong>Always allow in this chat</strong> — stop asking about <em>that</em> tool for the rest of this conversation. It is per tool and per conversation; it is never remembered after you start a new chat or restart KKSS.</li><li><strong>Deny</strong> — don&#39;t run it. Nothing on disk is touched, and the assistant is told you refused, so it explains what it wanted rather than trying again.</li></ul><p>Read-only tools — inspect, measure, mesh info, render — run without asking, so asking questions feels exactly as it did before. A tool KKSS has no policy for always asks; that is currently every tool from the Kratos server, because that server is fetched at runtime and its tools can&#39;t be classified in advance.</p><p><strong>Settings ▸ LLM Assistant ▸ Tool Approval</strong> changes this: <em>Ask before tools that change files</em> (the default), <em>Ask before every tool</em>, or <em>Never ask</em>. The last one turns the gate off entirely and asks you to confirm once before it does.</p><p>Note this covers the built-in sidebar only. If you expose the toolset to an external MCP client (below), that path is protected by its bearer token alone — there is nobody at the keyboard to ask.</p><p>For a few CAD tools — applying edit operations, and running a parametric or saved script — a fourth button appears: <strong>Validate (dry run)</strong>. It runs the same call in a mode that writes nothing and executes nothing, and shows you the result right there in the prompt. The prompt stays open, so you read the report and <em>then</em> choose Allow or Deny.</p><p>What it checks is narrower than a preview: it tells you which operations parse and are legal, not what the geometry would look like afterwards. The report is for you only — the assistant is not told about it, and is not told a call happened when it did not.</p><h3 id="tokens-context-and-cost" tabindex="-1">Tokens, context and cost <a class="header-anchor" href="#tokens-context-and-cost" aria-label="Permalink to “Tokens, context and cost”">​</a></h3><p>The sidebar header shows how much of the model&#39;s context window your last message filled, and roughly what the conversation has cost so far — for example <code>12.4k/1M · \$0.03</code>. Hover it for the full breakdown: the model, the split between fresh and cached input, and the output total.</p><p>It turns amber as the window fills up. That is worth acting on: when a conversation no longer fits, the provider rejects it outright, and the fix is to start a new one with the header&#39;s <strong>+</strong> (New conversation) button (the old conversation is saved, not lost).</p><p>Cost is an estimate from published per-token prices for the Claude models KKSS knows about. Point the OpenAI-compatible provider at your own gateway — Ollama, OpenRouter, a local model — and KKSS has no way to know what it charges, so you get token counts and nothing else rather than a made-up figure. None of this leaves your machine; it is calculated from what the provider returned with your response.</p><h3 id="when-a-conversation-gets-long" tabindex="-1">When a conversation gets long <a class="header-anchor" href="#when-a-conversation-gets-long" aria-label="Permalink to “When a conversation gets long”">​</a></h3><p>Every message you send includes the whole conversation so far, so a long one eventually stops fitting in the model&#39;s context window. Rather than failing, KKSS starts leaving the <strong>oldest tool results</strong> out of what it sends — usually the bulk of a long conversation — and a line under the header tells you how many.</p><p>Your transcript is not changed: every result is still there in full, and it is still saved. Only what the assistant is re-sent shrinks, and it can always re-run a tool if it needs an older result again.</p><p>If even that is not enough, you will get the &quot;start a new conversation&quot; message. The old one is saved and one click away in the header&#39;s <strong>history</strong> button.</p><h3 id="seeing-what-the-assistant-sees" tabindex="-1">Seeing what the assistant sees <a class="header-anchor" href="#seeing-what-the-assistant-sees" aria-label="Permalink to “Seeing what the assistant sees”">​</a></h3><p>Some tools return pictures — a rendered snapshot of a model, or a side-by-side comparison of two. Those appear in the tool&#39;s entry in the transcript, so when the assistant says a part looks wrong you can look at the same image it did. A result that has just arrived opens by itself; older ones in a long conversation stay folded away until you click them.</p><p>Images live for as long as the app is running. They are not written into the saved conversation, so reopening it later shows the tool call and its text, without the pictures.</p><h3 id="conversations" tabindex="-1">Conversations <a class="header-anchor" href="#conversations" aria-label="Permalink to “Conversations”">​</a></h3><p>Conversations are saved as you go and survive quitting the app — including the tool calls that record what the assistant actually did to your files, which is often the only account of it. The header&#39;s <strong>+</strong> button starts a fresh one and keeps the current one; nothing is discarded, so there is nothing to confirm.</p><p>The <strong>history</strong> button in the sidebar header lists your saved conversations, newest first, each named after its first message and showing when you last used it:</p><ul><li>click a row to switch to it — the full transcript comes back;</li><li>the <strong>pencil</strong> renames it (<code>Enter</code> to confirm, <code>Esc</code> to cancel);</li><li>the <strong>trash can</strong> deletes it, and asks once (the button turns into <em>Delete?</em>) because that is the one thing here you cannot undo.</li></ul><p>The 50 most recently used conversations are kept; older ones are dropped, except the one you currently have open. Switching away from a conversation while the assistant is still working stops that response first and records how far it got, so the transcript you come back to reads honestly.</p><h3 id="use-your-own-mcp-client" tabindex="-1">Use your own MCP client <a class="header-anchor" href="#use-your-own-mcp-client" aria-label="Permalink to “Use your own MCP client”">​</a></h3><p>If you&#39;d rather drive KKSS from an <strong>external</strong> LLM client (Claude Code, GitHub Copilot, Claude Desktop, another agent) than the built-in sidebar, enable <strong>Settings ▸ MCP Server</strong>. KKSS then serves the same unified cad + mesh + Kratos toolset — plus the Kratos worked-example resources and guided prompts — over a localhost <strong>Streamable HTTP</strong> MCP endpoint. Use <strong>Copy Address &amp; Token…</strong> to grab the <code>http://127.0.0.1:&lt;port&gt;/mcp</code> URL and its bearer token (default port <code>7391</code>). It is off by default and bound to localhost only; the token gates access because these tools read and write files on disk — <strong>Regenerate Token…</strong> rotates it (update your clients afterwards). Change the port under the same menu (toggle the server off and on to rebind). Tools arrive namespaced <code>cad__*</code> / <code>mesh__*</code> / <code>kratos__*</code>; the Kratos worked examples show up as MCP resources and prompts.</p><p>Leave KKSS running with the server enabled, then point a client at it:</p><p><strong>Claude Code</strong> — register it as an HTTP server (repeat <code>--header</code> for the token):</p><div class="language-bash"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({
		"--shiki-light": "#24292e",
		"--shiki-dark": "#e1e4e8",
		"--shiki-light-bg": "#fff",
		"--shiki-dark-bg": "#24292e"
	})}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">claude</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> mcp</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> add</span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}"> --transport</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> http</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> kkss</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> http://127.0.0.1:7391/mcp</span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}">  --header</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> &quot;Authorization: Bearer &lt;token&gt;&quot;</span></span></code></pre></div><p>Run <code>/mcp</code> inside Claude Code to confirm <code>kkss</code> is connected and list its tools. Remove it later with <code>claude mcp remove kkss</code>.</p><p><strong>GitHub Copilot (VS Code)</strong> — add an HTTP server entry to <code>.vscode/mcp.json</code> in your workspace (or run <em>MCP: Add Server…</em> from the Command Palette). Using an <code>input</code> keeps the token out of the file — VS Code prompts for it once and stores it securely:</p><div class="language-jsonc"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({
		"--shiki-light": "#24292e",
		"--shiki-dark": "#e1e4e8",
		"--shiki-light-bg": "#fff",
		"--shiki-dark-bg": "#24292e"
	})}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}">  &quot;inputs&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">: [</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">    { </span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}">&quot;id&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">: </span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}">&quot;kkss-token&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">, </span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}">&quot;type&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">: </span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}">&quot;promptString&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">, </span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}">&quot;description&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">: </span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}">&quot;KKSS MCP token&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">, </span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}">&quot;password&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">: </span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}">true</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}"> }</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">  ],</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}">  &quot;servers&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}">    &quot;kkss&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}">      &quot;type&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">: </span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}">&quot;http&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}">      &quot;url&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">: </span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}">&quot;http://127.0.0.1:7391/mcp&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}">      &quot;headers&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">: { </span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}">&quot;Authorization&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">: </span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}">&quot;Bearer \${input:kkss-token}&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}"> }</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">    }</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">  }</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">}</span></span></code></pre></div><p>Click <strong>Start</strong> on the server in <code>mcp.json</code>, then open Copilot Chat in <strong>Agent</strong> mode and enable the <code>kkss</code> tools from the tools (🛠) picker.</p><p>Any MCP client that speaks Streamable HTTP with a bearer header works the same way — give it the URL and the <code>Authorization: Bearer &lt;token&gt;</code> header.</p><h2 id="kratos-background-jobs" tabindex="-1">Kratos background jobs <a class="header-anchor" href="#kratos-background-jobs" aria-label="Permalink to “Kratos background jobs”">​</a></h2><p>Open <strong>Jobs</strong> in the toolbar or <strong>View ▸ Toggle Jobs</strong> to follow simulations started through the Kratos MCP server, including jobs launched by an external MCP client. The toolbar shows the active count. Jobs and Chat share the right-hand panel; switching between them keeps the conversation and simulations running. The Jobs panel works without an LLM API key.</p><p>Select a job to see its case directory, state, elapsed time, current step/time when available, and the last 100 log lines. Active jobs refresh every five seconds; <strong>Refresh</strong> also reloads the selected log. <strong>Cancel simulation</strong> requests cancellation from the server. A failed cancellation is shown beside the button.</p><p><img${ssrRenderAttr("src", kratos_jobs_default)} alt="Kratos background jobs and simulation log" width="540" height="1137"></p><p>If the server disconnects, the panel keeps the last known state and offers <strong>Retry</strong>. Missing runtime setup offers <strong>Install uv for KKSS</strong>, using the same setup flow as Chat. Server-owned job records are rediscovered after reconnecting or reopening Jobs after an app restart. Closing the panel, stopping chat, and quitting KKSS do not cancel these background jobs.</p><p>This panel covers <code>kratos__run_simulation</code> jobs. Runs started from the mesh Problemtype UI or <code>mesh__case_run</code> keep their existing run controls, including <strong>File ▸ Stop Kratos Run</strong>. Progress and recovered completion states come from the Kratos server; a percentage is not estimated when the server supplies only step/time.</p><h2 id="text-editor" tabindex="-1">Text editor <a class="header-anchor" href="#text-editor" aria-label="Permalink to “Text editor”">​</a></h2><p>The <strong>Edit</strong> toolbar button opens the file currently loaded in the active mode (<code>.mdpa</code>, <code>.stp</code>, …) as plain text — handy for touching up an input deck without leaving the app. <strong>Text Editor</strong> on the home screen (or <strong>File ▸ Open in Text Editor…</strong>) opens any file via a dialog instead. It&#39;s a lightweight editor for input files, scripts and configuration — <code>.json</code> and <code>.py</code> get syntax highlighting; binary or very large files are refused with a notice. <code>Ctrl+S</code> saves, <code>Ctrl+Shift+S</code> saves as, and the toolbar has Open/Save/Save As buttons. Unsaved changes show a dot next to the file name; switching screens never loses the buffer, and closing the window with unsaved changes prompts to save. Pair it with the terminal panel (<code>Ctrl+\` </code>) to edit and launch a Kratos case side by side.</p><h2 id="settings" tabindex="-1">Settings <a class="header-anchor" href="#settings" aria-label="Permalink to “Settings”">​</a></h2><p><strong>Settings ▸ Open Settings…</strong> (<code>Ctrl+,</code>) opens the Settings page. The home screen&#39;s Settings button and the chat sidebar&#39;s gear open it too. It is KKSS&#39;s equivalent of VS Code&#39;s settings editor: everything the two embedded extensions would read from VS Code&#39;s settings, and everything KKSS adds on top, on one searchable page. Each row shows its id (for example <code>kratos.pythonPath</code>), when a change takes effect, and a reset icon once it differs from the default. A value locked by a <code>KKSS_*</code> environment variable (see <a href="/KKSS/guide/web-deployment">Web deployment</a>) shows as <em>Set by the environment</em> and cannot be edited.</p><p><img${ssrRenderAttr("src", settings_default)} alt="Settings page" width="2820" height="2040"></p><table tabindex="0"><thead><tr><th>Category</th><th>What it holds</th></tr></thead><tbody><tr><td><strong>Appearance</strong></td><td><strong>UI Theme</strong>: Follow system / Dark / Light / High contrast (dark) / High contrast (light). It applies immediately to the whole app, both viewers&#39; panels and both 3D scenes. The mesh scene follows it while its <strong>3D Scene Theme</strong> is <em>Auto</em>; Dark / Light / Scientific pin the scene instead. The same row also covers UI font family and size, and the interface scale.</td></tr><tr><td><strong>General</strong></td><td>Project folder, <strong>Restore Last Session</strong>, whether What&#39;s New is shown after an update, and the update channel (stable, or including prereleases).</td></tr><tr><td><strong>CAD Viewer</strong></td><td>The <code>cadPreview.*</code> defaults for newly opened models: background colour, up axis, grid and axes on open, default mesh size, B-rep tessellation quality, and the OpenSCAD binary. A per-document sidecar value or a runtime toggle wins once set.</td></tr><tr><td><strong>Mesh Viewer</strong></td><td>Large-mesh summary threshold, and where the Flowgraph editor opens (below or beside the 3D view).</td></tr><tr><td><strong>Kratos</strong></td><td>The Python interpreter, the compiled Kratos install and the extra environment variables used when a problemtype case is <strong>Run</strong>. A folder that does not look like a Kratos install is flagged. The same row holds the problemtype folders scanned under the project folder and whether solvers are stopped when KKSS quits. The install path and environment also reach the assistant&#39;s Kratos tools; <strong>Restart with current environment</strong> applies a change without restarting KKSS.</td></tr><tr><td><strong>Text Editor</strong></td><td>Font size and family, tab size, word wrap, line numbers. All apply immediately.</td></tr><tr><td><strong>Terminal</strong></td><td>Shell (applies to the next session), font size and family, scrollback, cursor style and blink.</td></tr><tr><td><strong>LLM Assistant</strong></td><td>Provider, <strong>Tool Approval</strong>, API keys, models, the OpenAI-compatible base URL, and the Codex / Claude Code model and executable. Keys are write-only: the page shows whether one is stored, never the key itself.</td></tr><tr><td><strong>MCP Server</strong></td><td>Enable the localhost endpoint, its port, and copying or regenerating the token (see <em>AI assistant ▸ Use your own MCP client</em> above).</td></tr><tr><td><strong>Cloud Accounts</strong></td><td>Per provider: client ID, client secret, and connect / disconnect. Also the cache size limit, and clearing the cache.</td></tr></tbody></table><p>The <strong>Settings</strong> menu keeps quick radios for <strong>UI Theme</strong>, <strong>3D Scene Theme</strong> and <strong>Terminal Shell</strong>, plus the checkboxes and actions it always had. Every menu entry and its row on the page stay in sync both ways. Three VS Code-only extension settings have no row, because KKSS never reaches the code that reads them: <code>kratos.showWhatsNew</code>, <code>kratos.preview.autoSave</code> and <code>kratos.run.launchMode</code>.</p><p>Viewer actions (mesh quality, field visualization, find entity…) are <em>not</em> in the menu bar — they live in each viewer&#39;s own toolbar.</p><h2 id="opening-files" tabindex="-1">Opening files <a class="header-anchor" href="#opening-files" aria-label="Permalink to “Opening files”">​</a></h2><ul><li><strong>Open… button</strong> or <code>Ctrl+O</code> — opens a file in the current mode, replacing whatever the focused tab currently shows.</li><li><strong>File ▸ Open</strong> in the CAD viewer&#39;s own File menu — same thing. (The mesh viewer&#39;s in-view File menu is hidden; use the app&#39;s <strong>File</strong> menu, which covers the same actions plus Save/Load Problem.)</li><li>Formats supported by both modes (<code>.stl</code>, <code>.obj</code>, <code>.ply</code>) open in whichever mode is currently active.</li><li>Mesh formats Pre-Processing can also import (<code>.mdpa</code>, <code>.vtk</code>, <code>.vtu</code>, <code>.med</code>, <code>.cgns</code>, <code>.exo</code>, <code>.xdmf</code>) always open in <strong>Post-Processing</strong>, which reads them natively. Use Pre-Processing&#39;s own Open dialog when you want the geometry-only CAD import instead.</li></ul><h3 id="tabs-—-several-open-documents-at-once" tabindex="-1">Tabs — several open documents at once <a class="header-anchor" href="#tabs-—-several-open-documents-at-once" aria-label="Permalink to “Tabs — several open documents at once”">​</a></h3><p>Each mode (Pre-Processing and Post-Processing) shows a row of tabs below the toolbar, one per open document. Click <strong>+</strong> at the end of the strip, or <strong>File ▸ New CAD Tab</strong> / <strong>New Mesh Tab</strong>, to open a second document alongside the first — each tab keeps its own camera position, edit history, and (in Post-Processing) its own Flowgraph session, completely independent of the others. Click a tab to switch to it; click its <strong>×</strong> to close it (closing needs no confirmation — nothing about tabs is unsaved in the way a text buffer can be, since both viewers autosave their sidecars). A mesh exported from a Pre-Processing tab (<strong>File ▸ Export…</strong> to <code>.mdpa</code>/<code>.vtk</code>/…) opens in a <em>new</em> Post-Processing tab. Exporting again to the same path refreshes its existing tab if it has no unsaved mesh edits; unrelated and dirty tabs are preserved.</p><h2 id="keyboard-shortcuts" tabindex="-1">Keyboard shortcuts <a class="header-anchor" href="#keyboard-shortcuts" aria-label="Permalink to “Keyboard shortcuts”">​</a></h2><table tabindex="0"><thead><tr><th>Shortcut</th><th>Action</th></tr></thead><tbody><tr><td><code>Ctrl+O</code></td><td>Open a file into the focused tab of the active mode</td></tr><tr><td>—</td><td><strong>File ▸ Open Folder…</strong> sets the project folder; <strong>Clear Project Root</strong> reverts to inferring it</td></tr><tr><td>—</td><td><strong>File ▸ Open Recent</strong> reopens one of the last ten files, in the mode it was opened in</td></tr><tr><td>—</td><td><strong>File ▸ New Blank Model…</strong> creates an empty <code>.brep</code> to build from scratch (Pre-Processing)</td></tr><tr><td><code>Ctrl+W</code></td><td>Close the focused tab</td></tr><tr><td><code>Ctrl+S</code></td><td>Save (CAD: flush sidecars · Mesh: overwrite the source file)</td></tr><tr><td><code>Ctrl+Shift+S</code></td><td>Save As</td></tr><tr><td><code>Ctrl+E</code></td><td>Export</td></tr><tr><td><code>Ctrl+Alt+S</code> / <code>Ctrl+Alt+O</code></td><td>Save / Load a problem archive (Post-Processing)</td></tr><tr><td><code>Ctrl+Alt+R</code></td><td>Reload the focused document from disk, replaying applied edits (Post-Processing)</td></tr><tr><td><code>Ctrl+Alt+Z</code> / <code>Ctrl+Alt+Shift+Z</code></td><td>Undo / Redo a mesh operation (Post-Processing) — not <code>Ctrl+Z</code>, which stays the text editor&#39;s</td></tr><tr><td><code>Ctrl+Alt+P</code></td><td>Screenshot the current view to PNG</td></tr><tr><td><code>Ctrl+0</code></td><td>Back to the home screen (main menu)</td></tr><tr><td><code>Ctrl+1</code> / <code>Ctrl+2</code></td><td>Switch to Pre-Processing / Post-Processing</td></tr><tr><td><code>Ctrl+\` </code></td><td>Toggle the embedded terminal</td></tr><tr><td><code>Ctrl+Shift+L</code></td><td>Toggle the AI chat sidebar</td></tr></tbody></table><p>On macOS use <code>Cmd</code> instead of <code>Ctrl</code>.</p><p>Inside the mesh view, <code>1</code>–<code>6</code> snap the camera to ±X/±Y/±Z and <code>i</code> to an isometric view.</p><p><strong>File ▸ Export Data Table…</strong>, <strong>File ▸ Stop Kratos Run</strong>, and <strong>File ▸ Pack Time Series Into One File…</strong> are also in the native menu: the mesh viewer contributes the first two to its own File strip (which KKSS hides in favour of the native menu bar), and the third is reachable upstream only from the Command Palette and the Kratos Runs tree — neither of which KKSS runs.</p><p>A mesh tab with unsaved operations shows a dot in the tab strip; closing it, or quitting with one open, prompts Save / Don&#39;t Save / Cancel.</p><h3 id="language-and-keyboard-navigation" tabindex="-1">Language and keyboard navigation <a class="header-anchor" href="#language-and-keyboard-navigation" aria-label="Permalink to “Language and keyboard navigation”">​</a></h3><p>Choose <strong>Settings → Language</strong> to select English or Spanish. Restart KKSS for the change to reach menus, panels and dialogs. CAD-Preview and VSCode-MDPA-Preview keep their own interface language. Press <code>F6</code> to move forward through the visible regions and <code>Shift+F6</code> to move backward; hidden panels are skipped and closing a panel returns focus to the region that opened it. In the document tab strip, use the arrow keys or Home/End to move between tabs, Enter to activate a tab, and Delete to close it.</p></div>`);
}
var _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
	const ssrContext = useSSRContext();
	(ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/getting-started.md");
	return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
var getting_started_default = /*#__PURE__*/ _plugin_vue_export_helper_default(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
//#endregion
export { __pageData, getting_started_default as default };
