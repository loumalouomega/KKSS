import { t as _plugin_vue_export_helper_default } from "./plugin-vue_export-helper.BOaGB7Aw.js";
import { ssrRenderAttr, ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
//#region ../../../../../screenshots/home-screen-light.png
var home_screen_light_default = "/KKSS/screenshots/home-screen-light.png";
//#endregion
//#region guide/configuration.md
var __pageData = JSON.parse("{\"title\":\"Configuration\",\"description\":\"\",\"frontmatter\":{},\"headers\":[],\"relativePath\":\"guide/configuration.md\",\"filePath\":\"guide/configuration.md\"}");
var _sfc_main = { name: "guide/configuration.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
	_push(`<div${ssrRenderAttrs(_attrs)}><h1 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to “Configuration”">​</a></h1><p>KKSS deliberately keeps configuration minimal (&quot;Keep Kratos Simple Stupid&quot;). Every preference lives on the <strong>Settings page</strong> (<strong>Settings ▸ Open Settings…</strong>, <code>Ctrl+,</code>). It is searchable and grouped by category. The settings the two embedded VS Code extensions read (<code>cadPreview.*</code>, <code>kratos.*</code>) appear there under their VS Code ids. See <a href="/KKSS/guide/getting-started#settings">Getting Started ▸ Settings</a> for the full list.</p><h2 id="ui-theme" tabindex="-1">UI theme <a class="header-anchor" href="#ui-theme" aria-label="Permalink to “UI theme”">​</a></h2><p><strong>Appearance ▸ UI Theme</strong> chooses the colour theme of the whole application. The options are Follow system (the default), Dark, Light, High contrast (dark) and High contrast (light). The change is immediate, and it covers:</p><ul><li>the shell, home screen, editor, terminal, chat and dialogs;</li><li>both viewers&#39; panels;</li><li>the CAD scene;</li><li>the mesh scene, while its <strong>3D Scene Theme</strong> is <em>Auto</em>.</li></ul><p>The four kinds are VS Code&#39;s own, so both viewers restyle exactly as they would inside VS Code.</p><p><img${ssrRenderAttr("src", home_screen_light_default)} alt="The home screen in the Light theme" width="4080" height="2580"></p><p><strong>Appearance ▸ 3D Scene Theme</strong> (Auto / Dark / Light / Scientific) pins the mesh viewer&#39;s scene background and palette independently of the UI. It applies to meshes opened afterwards.</p><h2 id="kratos-environment" tabindex="-1">Kratos environment <a class="header-anchor" href="#kratos-environment" aria-label="Permalink to “Kratos environment”">​</a></h2><p><strong>Kratos</strong> holds what a problemtype <strong>Run</strong> uses:</p><table tabindex="0"><thead><tr><th>Setting</th><th>Meaning</th><th>Default</th></tr></thead><tbody><tr><td><code>kratos.pythonPath</code></td><td>Python interpreter for <code>python MainKratos.py</code></td><td><code>python3</code> (<code>python</code> on Windows)</td></tr><tr><td><code>kratos.installPath</code></td><td>Compiled Kratos install, or a source checkout built in-tree. Prepended to <code>PYTHONPATH</code> and the shared-library path</td><td>pip-installed Kratos</td></tr><tr><td><code>kratos.extraEnv</code></td><td>Extra variables; they override computed ones</td><td>—</td></tr><tr><td><code>kratos.problemtypes.extraPaths</code></td><td>Folders under the project folder scanned for user problemtypes</td><td><code>.kratos/problemtypes</code></td></tr><tr><td><code>kratos.run.stopOnWindowClose</code></td><td>Kill running solvers when KKSS quits</td><td>on</td></tr></tbody></table><p>The install path and extra environment also reach the assistant&#39;s Kratos MCP server the next time it starts. <strong>Restart with current environment</strong> applies them immediately. The interpreter does not: that server runs in <code>uvx</code>&#39;s own isolated environment.</p><h2 id="interface-scale" tabindex="-1">Interface scale <a class="header-anchor" href="#interface-scale" aria-label="Permalink to “Interface scale”">​</a></h2><p>The <strong>scale picker</strong> on the right of the shell toolbar (75 %–150 %) sets how large the whole application is drawn — the toolbar, both viewers, the terminal, and the chat sidebar all scale together, so the layout stays proportional on high-DPI or low-resolution displays. The choice persists across launches. It&#39;s also on the keyboard: <code>Ctrl +</code> / <code>Ctrl -</code> step through the presets and <code>Ctrl+Shift+0</code> resets to 100 % (mirrored under <strong>View ▸ Zoom In / Zoom Out / Reset Zoom</strong>).</p><h2 id="llm-assistant" tabindex="-1">LLM assistant <a class="header-anchor" href="#llm-assistant" aria-label="Permalink to “LLM assistant”">​</a></h2><p><strong>Settings ▸ LLM Assistant</strong> configures the AI chat sidebar (<a href="/KKSS/guide/getting-started#ai-assistant">Getting Started ▸ AI assistant</a>):</p><table tabindex="0"><thead><tr><th>Setting</th><th>Meaning</th><th>Default</th></tr></thead><tbody><tr><td>Provider</td><td><code>Anthropic (Claude)</code>, <code>OpenAI-compatible</code>, <code>ChatGPT subscription (Codex)</code>, or <code>Claude subscription (Claude Code)</code></td><td>Anthropic</td></tr><tr><td>Anthropic API Key</td><td>stored encrypted (OS keychain via <code>safeStorage</code>)</td><td>—</td></tr><tr><td>Anthropic Model</td><td>any Claude model id</td><td><code>claude-opus-4-8</code></td></tr><tr><td>OpenAI-compatible API Key</td><td>optional (keyless backends like Ollama work)</td><td>—</td></tr><tr><td>OpenAI-compatible Base URL</td><td>any <code>chat/completions</code> endpoint</td><td><code>https://api.openai.com/v1</code></td></tr><tr><td>OpenAI-compatible Model</td><td>model name your backend expects</td><td><code>gpt-4o</code></td></tr><tr><td>Tool Approval</td><td><code>Ask before tools that change files</code> / <code>Ask before every tool</code> / <code>Never ask</code></td><td>Ask before tools that change files</td></tr></tbody></table><p>The provider menu also includes <strong>ChatGPT subscription (Codex)</strong> and <strong>Claude subscription (Claude Code)</strong>. These options require the matching official local tool and an eligible signed-in subscription account. Their submenu offers a runtime check, official setup link, model override, and optional executable path. KKSS does not import or store subscription credentials.</p><p><strong>Tool Approval</strong> decides when the assistant has to ask you before running one of its tools. Read versus write is a KKSS-side classification keyed by the tool&#39;s full name, not something the tool servers declare about themselves — a server&#39;s own claim about whether it is safe is exactly the thing that should not be trusted. A tool KKSS has no entry for always asks, which today is every tool from the Kratos server (it is fetched at runtime, so its tools cannot be classified in advance). The setting applies to the very next tool call. <em>Never ask</em> turns the gate off and confirms once before doing so; it covers the built-in sidebar only, since the HTTP MCP endpoint has no user to prompt.</p><p>Changes apply to the next chat message — no restart. API keys are encrypted with the OS keychain when one is available; on systems without a keyring they fall back to plaintext in <code>state.json</code> (below). Entering an empty value clears a stored key.</p><h2 id="cloud-accounts" tabindex="-1">Cloud accounts <a class="header-anchor" href="#cloud-accounts" aria-label="Permalink to “Cloud accounts”">​</a></h2><p><strong>Settings ▸ Cloud Accounts</strong> connects KKSS to Google Drive, Dropbox or OneDrive, so <strong>File ▸ Open from Cloud…</strong> can open a model directly — no desktop sync client required. See <a href="/KKSS/guide/getting-started#working-from-cloud-storage">Getting Started ▸ Working from cloud storage</a> for how a cloud document behaves once it is open.</p><p><strong>KKSS ships no OAuth credentials of its own.</strong> You register a desktop/installed-app client in your own provider console and paste its client ID into the menu. That means nothing here routes through an application account you do not control — and it means there is a one-time setup step per provider:</p><table tabindex="0"><thead><tr><th>Provider</th><th>Redirect URI to register</th><th>Client secret</th><th>Scopes requested</th></tr></thead><tbody><tr><td>Google Drive</td><td><code>http://127.0.0.1/callback</code> — Google accepts a loopback redirect on <strong>any</strong> port</td><td>Issued for a &quot;Desktop app&quot; client; paste it too</td><td><code>https://www.googleapis.com/auth/drive</code></td></tr><tr><td>Dropbox</td><td><code>http://127.0.0.1:53682/callback</code> — <strong>exactly this</strong>, Dropbox matches the whole URI including the port</td><td>None (PKCE public client)</td><td><code>files.metadata.read</code>, <code>files.content.read</code>, <code>files.content.write</code>, <code>account_info.read</code></td></tr><tr><td>OneDrive</td><td><code>http://localhost/callback</code> as a <strong>Mobile &amp; desktop</strong> platform redirect</td><td>None (PKCE public client)</td><td><code>Files.ReadWrite</code>, <code>offline_access</code>, <code>User.Read</code></td></tr></tbody></table><p>Two things worth knowing before you start:</p><ul><li><strong>Google&#39;s testing-mode refresh tokens expire after seven days.</strong> While your OAuth consent screen is in &quot;Testing&quot;, you will have to reconnect weekly. Publishing the consent screen (even for a single-user app) removes the limit.</li><li><strong>Google Drive asks for full <code>drive</code> scope, not <code>drive.file</code>.</strong> The narrower scope only sees files the app itself created or that were picked through <em>Google&#39;s own</em> file picker, which KKSS does not use — with it, the Open from Cloud browser would show an empty Drive.</li></ul><p>Client IDs are stored in <code>state.json</code>; client secrets and refresh tokens go through the same encrypted store as the LLM API key. <strong>Disconnect…</strong> forgets the sign-in and offers to delete the cached copies with it.</p><h2 id="where-state-lives" tabindex="-1">Where state lives <a class="header-anchor" href="#where-state-lives" aria-label="Permalink to “Where state lives”">​</a></h2><table tabindex="0"><thead><tr><th>State</th><th>Location</th></tr></thead><tbody><tr><td>App state (theme, interface scale, project folder, recent files, last session, one-time warnings)</td><td><code>state.json</code> in the platform&#39;s user-data dir (<code>~/.config/KKSS</code> on Linux, <code>%APPDATA%/KKSS</code> on Windows, <code>~/Library/Application Support/KKSS</code> on macOS)</td></tr><tr><td>CAD parts / edits / mesh options</td><td>JSON sidecars next to the opened model — see <a href="/KKSS/guide/cad-mode#sidecar-files">Pre-Processing mode</a></td></tr><tr><td>Mesh operation recipes</td><td>Saved explicitly via the Edit sidebar&#39;s Save/Load buttons</td></tr><tr><td>Cloud client IDs</td><td><code>state.json</code> (public by OAuth&#39;s design)</td></tr><tr><td>Cloud client secrets and refresh tokens</td><td><code>state.json</code>, encrypted with the OS keychain like the LLM API key</td></tr><tr><td>Cloud staging cache and its manifest</td><td><code>cloud-cache/</code> beside <code>state.json</code> in the same user-data dir</td></tr></tbody></table><p>Note that the user-data dir is <strong>not</strong> inside your project folder, so syncing a project folder never syncs your settings or your credentials.</p><h2 id="command-line" tabindex="-1">Command-line <a class="header-anchor" href="#command-line" aria-label="Permalink to “Command-line”">​</a></h2><p><code>kkss &lt;file&gt;</code> opens the given model on startup in the mode the file&#39;s extension implies.</p></div>`);
}
var _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
	const ssrContext = useSSRContext();
	(ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/configuration.md");
	return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
var configuration_default = /*#__PURE__*/ _plugin_vue_export_helper_default(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
//#endregion
export { __pageData, configuration_default as default };
