import { t as _plugin_vue_export_helper_default } from "./plugin-vue_export-helper.BOaGB7Aw.js";
import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
//#region roadmap.md
var __pageData = JSON.parse("{\"title\":\"Roadmap\",\"description\":\"\",\"frontmatter\":{},\"headers\":[],\"relativePath\":\"roadmap.md\",\"filePath\":\"roadmap.md\"}");
var _sfc_main = { name: "roadmap.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
	_push(`<div${ssrRenderAttrs(_attrs)}><h1 id="roadmap" tabindex="-1">Roadmap <a class="header-anchor" href="#roadmap" aria-label="Permalink to “Roadmap”">​</a></h1><p>Candidate work for future KKSS releases, prioritized by value versus effort given what the app already ships: two upstream VS Code extensions running side by side — one <em>ported</em> (<code>cadHost.ts</code>), one <em>shimmed</em> behind a fake <code>vscode</code> module — with scoped workflow-contract changes committed on their integration branches, OCCT and Gmsh in a compute worker, MMG and meshio++ in the mesh submodule&#39;s own workers, a forked Flowgraph server, custom <code>kkss://</code> and <code>kkss-file://</code> schemes standing in for VS Code&#39;s webview plumbing, five screens over stacked <code>WebContentsView</code>s (home, the two mode screens — each holding any number of open documents as tabs — the text editor, plus a terminal panel and shared Chat/Jobs sidebar), an LLM agent loop over three MCP servers whose write-shaped tool calls are approved in the transcript before they run — optionally after being validated there without running — and whose image results are shown in the sidebar, a per-conversation token and cost readout, request-side compaction that keeps a long conversation inside the model&#39;s context window, an optional HTTP meta MCP server that re-exposes the same toolset, a cloud staging layer that opens Drive/Dropbox/OneDrive documents as ordinary local files, and a streamed-desktop Docker image. Many items below are cheap precisely because that infrastructure exists; a few are expensive precisely because of where its boundaries fall.</p><p>This page is aspirational, not a commitment — items may be re-ordered, re-scoped, or dropped. Effort is a rough order of magnitude: <strong>S</strong> (a day or two), <strong>M</strong> (roughly a week), <strong>L</strong> (multi-week).</p><p>Everything previously shipped is tracked in <code>CHANGELOG.md</code>, and <code>CLAUDE.md</code> has the verified implementation details and non-negotiable invariants for anything currently in the codebase — this page is for what&#39;s <strong>not</strong> built yet, only.</p><h2 id="how-this-file-works" tabindex="-1">How this file works <a class="header-anchor" href="#how-this-file-works" aria-label="Permalink to “How this file works”">​</a></h2><ul><li><strong>Tiers are ordered, and the order is the recommendation.</strong> Each tier states an <em>admission criterion</em>; an item that doesn&#39;t meet it belongs in a different tier or in Non-goals, not at the top because it sounds exciting. <strong>An empty tier is removed from the file entirely, not kept as a placeholder</strong> — if a new item shows up later that needs a tier no longer listed here (e.g. a defect-shaped correctness/robustness finding, which is always top priority regardless of effort size), recreate that tier at the top rather than burying it under whatever tier happens to be listed first at the time.</li><li><strong>A closed item is removed from this list entirely</strong>, not struck through — its write-up moves to <code>CLAUDE.md</code> (a per-feature section with the verified implementation details) and its history stays in git. Numbering is renumbered to stay consecutive whenever an item closes, so a reference like &quot;item 5&quot; always means the file&#39;s current 5th item, not a fossil of one that shipped.</li><li><strong>Every item states its chat-parity obligation.</strong> <code>CLAUDE.md</code>&#39;s &quot;Keep chat features in sync&quot; rule requires a new app capability to reach the assistant in the <em>same</em> change: the capability description in <code>app/main/services/chat/chatService.ts</code>&#39;s system prompt, the server wiring in <code>app/main/services/chat/mcpManager.ts</code> if a new tool source is involved, the <strong>Settings ▸ LLM Assistant</strong> menu if it&#39;s configurable, and <code>ChatDeps.currentFiles()</code> / the context suffix if it&#39;s new context rather than a new ability (the system prompt stays byte-stable for prompt caching). Items whose surface the assistant genuinely cannot reach — window chrome, packaging, CI — say so explicitly, so the estimate includes the wiring rather than discovering it late.</li><li><strong>Every item also carries a doc-parity cost.</strong> <code>doc/</code>, <code>README.md</code> and <code>CLAUDE.md</code> move with the change, not after it; anything that alters the shell toolbar, the generated webview pages, or visible viewer behavior also means re-running <code>npm run docs:screenshots</code> (the PNGs are generated from the live app, never hand-captured).</li><li><strong>Items marked <em>needs upstream submodule work</em> cannot land in this repo alone.</strong> A change inside <code>cad/</code> or <code>mesh/</code> goes to that submodule&#39;s own branch (<code>kkss.dev</code> for MCP contracts, <code>application-downstream</code> for extension code), is committed there, and is integrated here through its gitlink — never as an uncommitted patch or on the submodule&#39;s default branch. No such item may be <em>estimated</em> until the upstream half is scoped; each one names what it needs.</li><li>Most items close a <strong>known, documented limitation</strong> of something that already ships, or are a natural next step identified while building it. A rare one is <strong>defect-shaped</strong> — describing behavior that loses data, corrupts state, or strands a session rather than merely lacking a feature. Those belong in the top tier regardless of effort size, queued here rather than filed separately because the fix and the feature are usually the same work.</li></ul><h2 id="magnusim-review-—-2026-09-21" tabindex="-1">Magnusim review — 2026-09-21 <a class="header-anchor" href="#magnusim-review-—-2026-09-21" aria-label="Permalink to “Magnusim review — 2026-09-21”">​</a></h2><p>Reviewed the pinned source tree at <code>af8d05945d3b65cd9daf220554cb08ab0392da2d</code>, following queue transitions, study cloning, requirement checks, mesher diagnostics, transient settings and result exporters into their implementation and available tests. Compared those paths with <code>app/main/services/jobs.ts</code>, CAD&#39;s <code>meshOptions.ts</code>/<code>gmshSizingFields.ts</code>/<code>gmshService.ts</code>, and mesh&#39;s problemtype, field-reader, threshold and integral code. This was a source review; Magnusim&#39;s WSL/OpenFOAM pipeline was not executed here.</p><table tabindex="0"><thead><tr><th>Evidence</th><th>Product opportunity and owner</th></tr></thead><tbody><tr><td><a href="https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/src/workbench/jobQueueOrder.ts" target="_blank" rel="noreferrer"><code>jobQueueOrder.ts</code></a>, especially <code>queueHasMeshDepViolation</code>, <code>queueKickAfterStart</code> and persisted-queue merging; <a href="https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/src/workbench/jobQueueOrder.test.ts" target="_blank" rel="noreferrer">queue tests</a></td><td>KKSS: a persistent execution queue that understands mesh → solve dependencies, beyond displaying existing Kratos jobs.</td></tr><tr><td><a href="https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/scripts/w17-simulation.js" target="_blank" rel="noreferrer"><code>w17-simulation.js</code></a> remaps mesh/run references when cloning a study; <a href="https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/project/hierarchy.py" target="_blank" rel="noreferrer"><code>hierarchy.py</code></a> defines ownership</td><td>KKSS: clone a study safely, inspect parameter differences and compare resulting quantities. A bounded parameter sweep is our proposed extension, not a claim about Magnusim.</td></tr><tr><td><a href="https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/registry/requirements.py" target="_blank" rel="noreferrer"><code>requirements.py</code></a> checks declared prerequisites</td><td>KKSS: capability-aware setup and action availability. Its <code>version_spec</code> is stored but not enforced by the checker, so version compatibility needs real probes here.</td></tr><tr><td><a href="https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/cad/gaps.py" target="_blank" rel="noreferrer"><code>cad/gaps.py</code></a>, <a href="https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/cad/volume.py" target="_blank" rel="noreferrer"><code>cad/volume.py</code></a>, <a href="https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/mesh/cad_adherence.py" target="_blank" rel="noreferrer"><code>mesh/cad_adherence.py</code></a></td><td>CAD: narrow-gap preflight, meshing cost estimates and measured geometry fidelity, scoped in <code>cad/doc/roadmap.md</code>.</td></tr><tr><td><a href="https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/project/transient.py" target="_blank" rel="noreferrer"><code>project/transient.py</code></a>, <a href="https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/case/function_objects.py" target="_blank" rel="noreferrer"><code>case/function_objects.py</code></a>, <a href="https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/units/pressure.py" target="_blank" rel="noreferrer"><code>units/pressure.py</code></a></td><td>Mesh: time-step/output planning, conservation reports and physical field units, scoped in <code>mesh/doc/roadmap.md</code>.</td></tr></tbody></table><p><strong>Limits found during review:</strong> <a href="https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/project/mesh_refinements.py" target="_blank" rel="noreferrer"><code>mesh_refinements.py</code></a> explicitly contains persist-only controls, so a refinement menu entry is not evidence of mesher support. The numerical helpers use heuristics (cell-count bands, passage equivalent diameters, sampled deviation) that must keep their assumptions visible. Existing KKSS jobs, mesh presets, thresholding, recording, field integrals and split views are foundations to extend, not features to re-list as missing. No OpenFOAM/WSL backend migration is implied.</p><p>Detailed geometry/export work belongs to the CAD roadmap (<code>cad/doc/roadmap.md</code>), numerical setup/results work to the mesh roadmap (<code>mesh/doc/roadmap.md</code>), and cross-mode execution/navigation to KKSS.</p><h2 id="queued" tabindex="-1">Queued <a class="header-anchor" href="#queued" aria-label="Permalink to “Queued”">​</a></h2><h3 id="tier-0-—-submodules-stay-current" tabindex="-1">Tier 0 — Submodules stay current <a class="header-anchor" href="#tier-0-—-submodules-stay-current" aria-label="Permalink to “Tier 0 — Submodules stay current”">​</a></h3><p><em>Admission: keeping <code>cad/</code> and <code>mesh/</code> at their latest release, with the features they gain actually reachable from the app.</em></p><ol><li><strong>Gitlink currency, with the mirror checklist a bump actually requires</strong> (<strong>S</strong> per bump, recurring). Both submodules must track their latest release and the new features must be <em>integrated</em>, not merely present in the tree — a bump that builds is not a bump that works. The full checklist lives in <code>doc/guide/development.md</code> under <strong>Submodule release maintenance</strong>: review the upstream notes and diff on the right branches (<code>kkss.dev</code> for MCP changes, <code>application-downstream</code> for extension code); run <code>npm run package --prefix mesh</code> before the parent build and confirm <code>out/meshio/dist/meshioplusplus_wasm*</code> lists all four files; mirror <code>tools/webviewMarkup.ts</code>&#39;s mesh body element for element against <code>buildPreviewHtml</code>; extend <code>vscodeShim.ts</code> rather than patching, and give menubar-only actions native menu routes; classify new MCP tools in <code>toolPolicy.ts</code>; build the CAD MCP kernel via <code>cadMcpWorkerConfig</code>, never copy upstream&#39;s artifact; then typecheck, test, smoke, docs and screenshots. <em>Chat parity:</em> a new viewer or file capability is only useful to the assistant if the submodule&#39;s own MCP server exposes it — if it doesn&#39;t, that is upstream work on the submodule&#39;s <code>kkss.dev</code> branch, and the gitlink bump waits for it. A new <strong>app-level</strong> ability arriving with the bump needs the system-prompt description updated in the same change.</li></ol><h3 id="tier-1-—-tutorials-and-worked-examples" tabindex="-1">Tier 1 — Tutorials and worked examples <a class="header-anchor" href="#tier-1-—-tutorials-and-worked-examples" aria-label="Permalink to “Tier 1 — Tutorials and worked examples”">​</a></h3><p><em>Admission: closes a gap between what the app can already do and what a new user is shown how to do — the guide pages document each mode in isolation, but nothing connects them into the single geometry-to-results pipeline the app exists for.</em></p><ol start="2"><li><p><strong>An end-to-end &quot;geometry to results&quot; tutorial</strong> (<strong>M</strong>). <code>doc/guide/cad-mode.md</code> and <code>doc/guide/mesh-mode.md</code> each document their own mode&#39;s toolbar in isolation, and <code>getting-started.md</code> covers app chrome, not workflow — nothing connects them into the single pipeline the app exists for. A new user has no page that starts at &quot;sketch or import a solid&quot; and ends at &quot;read off the stress field,&quot; even though every step already works end to end: build or import geometry and mesh it with Gmsh in CAD mode, export to <code>.mdpa</code>, open it in mesh mode, use the Problemtype sidebar (a built-in — Structural, Fluid, Convection-Diffusion, Potential Flow, Shallow Water — or the Flowgraph node editor) to assign materials and boundary conditions, <strong>Generate</strong> the ProjectParameters.json + materials file(s) + MainKratos.py, <strong>Run</strong> it in the embedded terminal, and <strong>Open results</strong> back into the viewer&#39;s VTK timeline. A separate <a href="/KKSS/guide/tutorial-kratos-examples">worked-case page</a> now includes three real solver runs, but starts from existing MDPA cases, so it does not close this geometry-to-results gap. Ship it as <code>doc/guide/tutorial-*.md</code> pages (added to <code>.vitepress/config.ts</code>&#39;s nav and sidebar) built around one small, reproducible worked example per physics domain — start with a structural cantilever, matching whatever fixture already ships under <code>cad/</code>&#39;s or <code>mesh/</code>&#39;s own example trees rather than inventing new geometry — with the walkthrough&#39;s screenshots produced by <code>npm run docs:screenshots</code> (<code>tools/screenshots.mjs</code>) like every other doc image, never hand-captured. Writing it needs a local Kratos install to actually exercise the <strong>Run</strong> step (the app doesn&#39;t bundle one); the app-local uv bootstrap now provides the tool-server launcher, but the manual Run step still needs a usable Kratos Python environment. <em>Chat parity: none directly</em> — but the same worked example is the natural seed for item 3 below, and for eventually showing the assistant drive the pipeline via the <code>cad__*</code>/<code>mesh__*</code>/<code>kratos__*</code> MCP tools as an alternative to the manual sidebar flow.</p></li><li><p><strong>A &quot;using the AI assistant to run a simulation&quot; tutorial</strong> (<strong>S</strong>, depends on item 2). Once the manual pipeline is documented, a companion page shows the same pipeline driven conversationally instead — &quot;import this STEP file, mesh it, set up a structural case, run it, tell me the max displacement&quot; — which is the only place today that would make the chat sidebar&#39;s actual value legible: <code>getting-started.md</code>&#39;s &quot;AI assistant&quot; section documents the sidebar&#39;s <em>chrome</em> (provider settings, MCP client mode) but has no worked prompt. Tool-call approval has shipped, so the walkthrough can show the real approve/deny prompt — including <strong>Validate (dry run)</strong> on the cad tools that support it — rather than a screenshot that goes stale the moment it lands. Image results render in the sidebar too, so a <code>render_snapshot</code> step can show what the assistant actually looked at. Transcript persistence has shipped, so the example can be a saved conversation a reader is pointed at rather than a one-off screenshot. <em>Chat parity: this <strong>is</strong> chat-surface documentation — keep its prompts and tool names in sync with <code>chatService.ts</code>&#39;s system-prompt capability description whenever that changes.</em></p></li></ol><h3 id="tier-2-—-distribution-and-deployment" tabindex="-1">Tier 2 — Distribution and deployment <a class="header-anchor" href="#tier-2-—-distribution-and-deployment" aria-label="Permalink to “Tier 2 — Distribution and deployment”">​</a></h3><p><em>Admission: the app builds and ships correctly, but is not yet easy to install, trust, or reach. Nothing in this tier is a prerequisite for anything above it.</em></p><ol start="4"><li><p><strong>macOS signing, notarization and hardened runtime</strong> (<strong>M</strong>, remaining). Hardened-runtime entitlements, optional signing inputs and CI validation are wired, while the default release remains explicitly unsigned until Developer ID and notarization credentials are configured. Close this item after a credentialed macOS build signs and staples both DMG and ZIP artifacts, passes signature/Gatekeeper checks, and enables the signed-build updater path. <em>Chat parity: none</em> (packaging).</p></li><li><p><strong>Streamed-desktop sharing and resize validation</strong> (<strong>S</strong>, remaining). The x11vnc and TigerVNC policies now reject additional viewers while preserving the first connection. Validate both display backends on native amd64 and arm64 runners, including second-client rejection, reconnect, CAD/mesh rendering and TigerVNC resize; Xvfb remains the default. Keep the entrypoint&#39;s Electron flags synchronized with <code>tools/smoke.e2e.mjs</code>/<code>tools/e2eShared.mjs</code>. <em>Chat parity: none</em> (deployment).</p></li></ol><h3 id="tier-3-—-deployment-validation" tabindex="-1">Tier 3 — Deployment validation <a class="header-anchor" href="#tier-3-—-deployment-validation" aria-label="Permalink to “Tier 3 — Deployment validation”">​</a></h3><p><em>Admission: the authenticated, persistent browser-session implementation is shipped; only acceptance that requires Docker or graphics hardware remains here.</em></p><ol start="6"><li><strong>Container and GPU acceptance validation</strong> (<strong>S</strong>, needs Docker CI and an Intel/AMD hardware runner). The compose, gateway, file browser, broker, Kratos image variant and CI checks are implemented and covered by local type, unit, Go, YAML and documentation checks. Run the Docker boot/login/stop/reconnect checks, offline Kratos import and MCP simulation, and the hardware-renderer/CAD/mesh/TigerVNC resize checks on the appropriate runners before closing this item. Keep Xvfb plus SwiftShader as the tested default, and fail clearly when <code>/dev/dri</code> or graphics initialization is unavailable. <em>Chat parity: none</em> (deployment/test infrastructure).</li></ol><h2 id="non-goals-known-constraints" tabindex="-1">Non-goals / known constraints <a class="header-anchor" href="#non-goals-known-constraints" aria-label="Permalink to “Non-goals / known constraints”">​</a></h2><ul><li><strong>Keeping app-specific submodule patches uncommitted or on a release branch</strong> — shared workflow contracts can require upstream changes; commit those on the designated integration branches and move the parent gitlinks only to their committed revisions.</li><li><strong>Duplicating viewer actions in the native menu</strong> — the menu bar holds app-level items only. Quality metrics, field visualization, find-entity and the rest live in the submodules&#39; own toolbars; mirroring them would create two surfaces to keep in sync and one to forget.</li><li><strong>Merging the two modes into one viewer or one document</strong> — the asymmetric reuse (a 1:1 <em>port</em> for CAD because its provider imports OCCT directly, a verbatim <em>shim</em> for mesh) and two independent WebGL renderers mean this is a rewrite of the thing that makes the app cheap to maintain, not a feature.</li><li><strong>A dirty-state prompt when closing a tab</strong> — cad and mesh have no app-level &quot;unsaved changes&quot; concept; their sidecars autosave on a debounce, so closing a tab is a plain dispose, exactly like replacing a tab&#39;s document has always been. Adding a prompt would mean inventing a document-dirty model the submodules don&#39;t have.</li><li><strong>Bundling Kratos or a Python runtime in the installer</strong> — the package is already large (OCCT alone is ~63 MB of WASM, and <code>asar: false</code> means it ships unpacked); <code>uvx</code> sources the Kratos server on demand. The app-local uv bootstrap improves that path rather than replacing it. The <code>-kratos</code> image is a separate tag of a separate distribution, and the installer stays lean.</li><li><strong>Operating a hosted KKSS service</strong> — the repo ships the unit of deployment (the image), the authenticated web front end and a reference per-user orchestrator so that <em>someone else</em> can run one. User accounts beyond what an OIDC provider supplies, billing, quotas, backups of user volumes and an SLA are the operator&#39;s concern, not this project&#39;s; the single-container image stays the default, and item 5 keeps its one-session scope explicit rather than implied.</li><li><strong>Hand-editing generated artifacts</strong> — <code>out/renderer/*/index.html</code>, <code>app/renderer/shell/shellIcons.ts</code>, the doc screenshots and the icon SVG/PNGs are all pipeline output. Edit the source and re-run the pipeline.</li><li><strong>Telemetry or usage analytics</strong> — not planned, recorded here so it isn&#39;t re-proposed. The chat sidebar&#39;s token and cost readout is not an exception to this: it is computed in the main process from what the provider returned, shown only to the user whose key paid for it, and transmitted nowhere. Crash <em>diagnostics</em> the user can read and send by hand are a different thing and are not excluded; the renderer-crash toasts are the seed of one.</li><li><strong>A second native module</strong> — node-pty is deliberately the only one, and the only <code>node_modules</code> entry that ships. It is N-API, so it needs no electron-rebuild; anything requiring a rebuild step would change the release matrix for every platform at once.</li></ul><h2 id="dependencies" tabindex="-1">Dependencies <a class="header-anchor" href="#dependencies" aria-label="Permalink to “Dependencies”">​</a></h2><p>A few items must land in order, and getting it wrong means rework:</p><ul><li><strong>2 → 3.</strong> The chat-driven tutorial reuses the manual one&#39;s worked example and pipeline description; writing it first means describing the same pipeline twice.</li><li><strong>5 → 6.</strong> The remaining sharing and resize validation must be settled before the container and hardware acceptance checks can close.</li></ul></div>`);
}
var _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
	const ssrContext = useSSRContext();
	(ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("roadmap.md");
	return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
var roadmap_default = /*#__PURE__*/ _plugin_vue_export_helper_default(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
//#endregion
export { __pageData, roadmap_default as default };
