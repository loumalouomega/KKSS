import { t as _plugin_vue_export_helper_default } from "./plugin-vue_export-helper.BOaGB7Aw.js";
import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
//#region guide/file-formats.md
var __pageData = JSON.parse("{\"title\":\"File Formats\",\"description\":\"\",\"frontmatter\":{},\"headers\":[],\"relativePath\":\"guide/file-formats.md\",\"filePath\":\"guide/file-formats.md\"}");
var _sfc_main = { name: "guide/file-formats.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
	_push(`<div${ssrRenderAttrs(_attrs)}><h1 id="file-formats" tabindex="-1">File Formats <a class="header-anchor" href="#file-formats" aria-label="Permalink to “File Formats”">​</a></h1><h2 id="which-mode-opens-what" tabindex="-1">Which mode opens what <a class="header-anchor" href="#which-mode-opens-what" aria-label="Permalink to “Which mode opens what”">​</a></h2><table tabindex="0"><thead><tr><th>Extension</th><th>Mode</th><th>Notes</th></tr></thead><tbody><tr><td><code>.step</code> <code>.stp</code> <code>.iges</code> <code>.igs</code> <code>.brep</code></td><td>🔷 Pre-Processing</td><td>B-rep, tessellated by OpenCascade</td></tr><tr><td><code>.csg</code> <code>.scad</code></td><td>🔷 Pre-Processing</td><td>OpenSCAD. <code>.csg</code> (the fully evaluated form) is parsed and built kernel-side into a solid; <code>.scad</code> is converted to <code>.csg</code> first by a <strong>user-installed <code>openscad</code> binary</strong> (<strong>Settings ▸ Open Settings… ▸ CAD Viewer ▸ OpenSCAD Binary</strong>, or <code>openscad</code> on <code>PATH</code>) — without one, opening a <code>.scad</code> reports that rather than failing silently. Import-only: export goes to STEP/IGES/BREP or a mesh target, never back. Anything the importer approximates or skips (a <code>hull()</code>, a faceted cylinder) is reported on the status line</td></tr><tr><td><code>.gltf</code> <code>.glb</code></td><td>🔷 Pre-Processing</td><td>Loaded natively by Three.js</td></tr><tr><td><code>.mdpa</code></td><td>🔶 Post-Processing</td><td>Kratos model part</td></tr><tr><td><code>.vtk</code> <code>.vtu</code> <code>.vtp</code> <code>.vti</code> <code>.vts</code> <code>.vtr</code> <code>.vtm</code></td><td>🔶 Post-Processing</td><td>Legacy + XML VTK, multiblock, time-series</td></tr><tr><td><code>.msh</code> <code>.inp</code> <code>.bdf</code> <code>.nas</code> <code>.fem</code> <code>.unv</code> <code>.mesh</code> <code>.vol</code> <code>.su2</code> <code>.xdmf</code> <code>.xmf</code> <code>.off</code> <code>.avs</code> <code>.dat</code> <code>.tec</code> <code>.mphtxt</code> <code>.node</code> <code>.ele</code> <code>.f3grid</code> <code>.pf3</code> <code>.post</code> <code>.dato</code> <code>.ugrid</code> <code>.mfm</code> <code>.wkt</code> <code>.xml</code> <code>.dex</code> <code>.ip</code> <code>.mff</code> <code>.case</code> <code>.geo</code> <code>.poly</code> <code>.e</code> <code>.exo</code> <code>.ex2</code> <code>.cgns</code> <code>.h5m</code> <code>.hmf</code> <code>.med</code> <code>.foam</code></td><td>🔶 Post-Processing</td><td>Extended formats read via <a href="https://github.com/nschloe/meshio" target="_blank" rel="noreferrer">meshio++</a> (Gmsh, Abaqus/ANSYS, Nastran, I-deas UNV, Medit, Netgen, SU2, XDMF, COMSOL, tetgen, EnSight Gold, Triangle, Exodus II, CGNS, MOAB, Salome MED, OpenFOAM, …). Named groups (Gmsh physical groups, Abaqus <code>*NSET</code>/<code>*ELSET</code>/<code>*SURFACE</code>, Exodus element blocks / node sets / side sets, OpenFOAM boundary patches) arrive as SubModelParts. <code>.e</code>/<code>.exo</code>/<code>.ex2</code> (Exodus) and <code>.xdmf</code>/<code>.xmf</code> describe their time steps within the dataset and drive the timeline from there (XDMF may reference a required sibling <code>.h5</code>; copy it too) — no <code>&lt;prefix&gt;_&lt;rank&gt;_&lt;step&gt;</code> sibling naming needed; Salome <code>.med</code> also accepts a time step, but reports no step count up front, so its timeline isn&#39;t discoverable before a read. <code>.dex</code>/<code>.ip</code>/<code>.mff</code> are field-only formats — they carry point fields with no geometry, so reading one yields a point cloud (or an empty mesh); <code>.case</code>/<code>.geo</code> (EnSight Gold) and <code>.poly</code> (Triangle) need their sibling file(s) alongside. <code>.foam</code> is an OpenFOAM case: the file you open is a 0-byte marker, and the real mesh — volume cells as Elements, boundary faces as Conditions, one named SubModelPart per boundary patch — lives in a sibling <code>constant/polyMesh/</code> tree, which the preview also watches, so re-running <code>blockMesh</code>/<code>snappyHexMesh</code> refreshes the view in place. Not read: time-directory fields, zones, moving-mesh cases, multi-region and decomposed (<code>processor*/</code>) cases. <strong>Saving a <code>.foam</code> case in place is refused</strong> — writing &quot;the file&quot; would rewrite the real mesh tree and collapse every patch name into one generic <code>defaultFaces</code>; use Export or Save As to write a new case directory instead</td></tr><tr><td><code>.stl</code> <code>.obj</code> <code>.ply</code></td><td>Both</td><td>Opens in the <strong>currently active</strong> mode</td></tr></tbody></table><p>Pre-Processing can <em>also</em> read <code>.vtk</code> <code>.vtu</code> <code>.med</code> <code>.cgns</code> <code>.exo</code> <code>.e</code> <code>.xdmf</code> <code>.mdpa</code> <code>.msh</code> <code>.inp</code> <code>.unv</code> <code>.su2</code> <code>.mesh</code> <code>.post.msh</code> <code>.foam</code> through meshio++, as a <strong>geometry-only boundary surface</strong> (named regions become Parts; fields are not converted, though a scalar point/cell field can be used to colour the model). Those are post-processing formats here and mesh mode reads them natively — fields, blocks and SubModelParts — so opening one always lands in Post-Processing regardless of the active mode. Reach the CAD-side importer from Pre-Processing&#39;s own <strong>Open…</strong> dialog (or drag the file onto the CAD view) when you specifically want to mesh or edit that geometry.</p><p><code>.msh2</code> (Gmsh 2.2) goes the other way and opens in <strong>Pre-Processing</strong> whatever the active mode — the mesh engine never registered it at all, so cad mode is the only one that can open it.</p><h2 id="export-targets" tabindex="-1">Export targets <a class="header-anchor" href="#export-targets" aria-label="Permalink to “Export targets”">​</a></h2><p><strong>Pre-Processing</strong> (depends on the source pipeline):</p><ul><li>B-rep sources → STEP, IGES, BREP, STL, OBJ, PLY, glTF</li><li>Mesh sources → STL, OBJ, PLY, glTF (no mesh→B-rep path exists)</li><li>FE meshing → Kratos MDPA (Elements+Conditions or Geometries), Gmsh <code>.msh</code> / <code>.msh2</code> / <code>.geo_unrolled</code> (+ XAO companion), VTK, UNV, Abaqus <code>.inp</code>, Nastran <code>.bdf</code>, SU2, and more — plus MED, CGNS and XDMF (+ its <code>.h5</code> companion) written through meshio++, which Gmsh&#39;s own writers can&#39;t produce</li><li>Screenshot → PNG (<strong>File ▸ Screenshot…</strong>, <code>Ctrl+Alt+P</code>)</li></ul><p>Every export target can optionally be <strong>unit-converted</strong> (mm/cm/m/in/ft) on the way out: a second quick-pick after the format, defaulting to native mm. This is a real geometric scale on the exported file — STEP and IGES also get their declared header unit written to match — not the display-unit selector in the view controls, which only changes how measurements read. A CGNS export of a pure surface mesh is a known gap: it writes, but meshio++&#39;s own reader can&#39;t read it back (volume meshes are fine).</p><p>When an FE-meshing export produces a file Post-Processing can display (<code>.mdpa</code>, <code>.vtk</code>, …), the app switches to Post-Processing mode and opens it automatically — a one-way pre → post handoff. Exports the post viewer can&#39;t open (<code>.msh</code>, <code>.inp</code>, …) and shared formats (<code>.stl</code>/<code>.obj</code>/<code>.ply</code>) stay in Pre-Processing.</p><p><strong>Post-Processing</strong>: MDPA, VTK (legacy), VTU, VTP, STL, OBJ, PLY — for the whole model or any single SubModelPart, or just the <strong>boundary skin</strong> of the volume cells (<strong>Advanced ▸ Export skin…</strong>) — plus ~36 extended formats written via <a href="https://github.com/nschloe/meshio" target="_blank" rel="noreferrer">meshio++</a> (Gmsh <code>.msh</code>, Abaqus <code>.inp</code>, Nastran <code>.bdf</code>/<code>.nas</code>/<code>.fem</code>, I-deas UNV, Medit <code>.mesh</code>, Netgen <code>.vol</code>, SU2, XDMF, Triangle <code>.poly</code>, Exodus <code>.e</code>/<code>.exo</code>/<code>.ex2</code>, CGNS, MOAB <code>.h5m</code>, <code>.hmf</code>, Salome <code>.med</code>, and more), including the field-only <code>.dex</code>/<code>.ip</code>/<code>.mff</code> targets (point fields kept, geometry dropped) and the write-only SVG/TikZ figure formats (a 2D/3D-projected drawing of the mesh, not a re-readable mesh). SubModelParts now survive an export to <code>.med</code> (as MED families) and <code>.inp</code> (as real <code>*NSET</code>/<code>*ELSET</code> sets), and every mesh block keeps its name in <code>.exo</code>; a <code>.msh</code> export still carries no groups (meshio++ writes no <code>\$PhysicalNames</code> for a mesh that didn&#39;t come from Gmsh). Exodus export remains lossy — element blocks and fields survive, genuine SubModelParts do not, and a time series flattens to one step; export to <code>.mdpa</code>, <code>.vtu</code> or <code>.med</code> when the grouping matters. Structured VTK types (<code>.vti</code>/<code>.vts</code>/<code>.vtr</code>) and <code>.vtm</code> are view-only.</p></div>`);
}
var _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
	const ssrContext = useSSRContext();
	(ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/file-formats.md");
	return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
var file_formats_default = /*#__PURE__*/ _plugin_vue_export_helper_default(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
//#endregion
export { __pageData, file_formats_default as default };
