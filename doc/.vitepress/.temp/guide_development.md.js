import { t as _plugin_vue_export_helper_default } from "./plugin-vue_export-helper.BOaGB7Aw.js";
import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
//#region guide/development.md
var __pageData = JSON.parse("{\"title\":\"Architecture & Building\",\"description\":\"\",\"frontmatter\":{},\"headers\":[],\"relativePath\":\"guide/development.md\",\"filePath\":\"guide/development.md\"}");
var _sfc_main = { name: "guide/development.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
	_push(`<div${ssrRenderAttrs(_attrs)}><h1 id="architecture-building" tabindex="-1">Architecture &amp; Building <a class="header-anchor" href="#architecture-building" aria-label="Permalink to “Architecture &amp; Building”">​</a></h1><h2 id="the-shim-architecture" tabindex="-1">The shim architecture <a class="header-anchor" href="#the-shim-architecture" aria-label="Permalink to “The shim architecture”">​</a></h2><p>KKSS reuses the two VS Code extensions <strong>without modifying them</strong>. Both are already split into a browser-side webview bundle (whose only VS Code touchpoint is <code>acquireVsCodeApi()</code>), vscode-free compute/parser modules, and a thin vscode-coupled glue layer. KKSS replaces only the glue:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({
		"--shiki-light": "#24292e",
		"--shiki-dark": "#e1e4e8",
		"--shiki-light-bg": "#fff",
		"--shiki-dark-bg": "#24292e"
	})}" tabindex="0" dir="ltr"><code><span class="line"><span>┌────────────────────────── BaseWindow ──────────────────────────┐</span></span>
<span class="line"><span>│ shell toolbar (Home · mode toggle · Open · title · toasts)     │</span></span>
<span class="line"><span>│ tab strip (per mode — several open documents, one per tab)     │</span></span>
<span class="line"><span>├────────────────────────────────────────────────────────────────┤</span></span>
<span class="line"><span>│ cad tab(s)                      │ mesh tab(s)                  │</span></span>
<span class="line"><span>│ cad/media/viewer.js (unmodified)│ mesh/media/webview.js (unmod)│</span></span>
<span class="line"><span>│ + acquireVsCodeApi shim         │ + acquireVsCodeApi shim      │</span></span>
<span class="line"><span>│ (one WebContentsView per tab)   │ (one WebContentsView per tab)│</span></span>
<span class="line"><span>└───────────────▲─────────────────┴──────────────▲───────────────┘</span></span>
<span class="line"><span>                │ IPC = the extensions&#39; own message protocols     │</span></span>
<span class="line"><span>┌───────────────▼──────────────────────────────────▼─────────────┐</span></span>
<span class="line"><span>│ Electron main                                                  │</span></span>
<span class="line"><span>│  one CadHost per open cad tab — port of cad/src/provider.ts    │</span></span>
<span class="line"><span>│    OCCT + Gmsh WASM → shared worker thread (cadCompute.worker) │</span></span>
<span class="line"><span>│  one MeshHost per open mesh tab — runs the REAL                │</span></span>
<span class="line"><span>│    Mdpa/VtkEditorProvider classes behind a \`vscode\` shim       │</span></span>
<span class="line"><span>│    module + a fake WebviewPanel; MMG → the submodule&#39;s own     │</span></span>
<span class="line"><span>│    worker pair; Flowgraph&#39;s child process is shared, ref-      │</span></span>
<span class="line"><span>│    counted across every mesh tab, unchanged                    │</span></span>
<span class="line"><span>└────────────────────────────────────────────────────────────────┘</span></span></code></pre></div><p>A fourth, full-window <code>WebContentsView</code> — the <strong>home screen</strong> (<code>app/renderer/home/</code>) — is stacked on top and shown on launch (and via the toolbar&#39;s Home button, <code>Ctrl+0</code>, or <strong>View ▸ Home</strong>). It covers the shell and every mode tab; entering a mode hides it. Screens are tracked as <code>Screen = &quot;home&quot; | &quot;editor&quot; | Mode</code> in <code>app/main/ipc.ts</code> and switched with <code>MainWindow.setScreen()</code> (<code>app/main/windows.ts</code>). Each mode screen can hold several open documents (&quot;tabs&quot;) — <code>MainWindow.openTab()</code>/<code>closeTab()</code>/<code>setActiveTab()</code> manage a <code>Tab {id, view}</code> registry per mode, one full <code>WebContentsView</code> per tab (the same lazy-create + <code>setVisible()</code> precedent as the terminal/chat panels, just N-of-a-kind); only the focused tab of the active mode is ever visible/bounded, so switching tabs or trips through the home screen never reload or lose a tab&#39;s camera/edit-history state. <code>app/main/index.ts</code>&#39;s <code>createTab(mode)</code> builds a tab&#39;s view and its <code>CadHost</code>/<code>MeshHost</code> together; <code>activeCadHost()</code>/<code>activeMeshHost()</code> resolve whichever tab is currently focused for callers (the native menu, the terminal&#39;s cwd, the chat context) that only ever need &quot;the current one.&quot; The tab strip is plain DOM inside the <code>shell</code> page (<code>app/renderer/shell/shell.ts</code>), synced wholesale via a <code>{type:&quot;tabs&quot;, mode, tabs, activeTabId}</code> message on every open/close/focus/title change — see <code>CLAUDE.md</code>&#39;s tabs invariant for the full model, including the two genuinely-shared pieces of state (<code>cadBRepCache.ts</code>&#39;s session-keyed worker cache and mesh&#39;s ref-counted <code>FlowgraphController</code>). The home menu&#39;s buttons are config-driven: add an entry to <code>app/renderer/home/homeConfig.ts</code>, a <code>HomeAction</code> case in <code>app/main/ipc.ts</code>, and its handler in <code>app/main/index.ts</code> (<code>home:toHost</code>/<code>home:toWebview</code> channels via <code>app/preload/homePreload.ts</code>, same contextBridge pattern as the shell). The recents list below those buttons is <em>not</em> config-driven: main pushes it over <code>home:toWebview</code> (<code>sendHome()</code>/<code>pushRecents()</code>), pre-formatted, because that renderer is a browser bundle and cannot import <code>node:path</code>. The renderer re-requests nothing — it replays on its own <code>homeReady</code>, the same handshake the shell uses, so a reload or a crash recovery repaints it.</p><p><strong>Interface scale.</strong> The shell toolbar&#39;s scale picker (and <strong>View ▸ Zoom In / Zoom Out / Reset Zoom</strong>, <code>Ctrl +</code>/<code>Ctrl -</code>/<code>Ctrl+Shift+0</code>) sets a single zoom factor via <code>MainWindow.setZoom()</code> (<code>app/main/windows.ts</code>). <code>setZoomFactor</code> scales each <code>WebContentsView</code>&#39;s <em>content</em> but not its bounds, so <code>layout()</code> multiplies the fixed chrome constants (<code>SHELL_HEIGHT</code>, <code>TAB_STRIP_HEIGHT</code>, <code>TERMINAL_HEIGHT</code>, <code>CHAT_WIDTH</code>) by the factor in lockstep — otherwise the scaled toolbar would clip. <code>TAB_STRIP_HEIGHT</code> is only ever added while a mode screen (which actually has tabs) is active. Electron resets a view&#39;s zoom to 1 on every navigation, so each tab&#39;s view re-asserts the factor on <code>did-finish-load</code> (a tab reloads when a file opens into it). The picker round-trips over the shell channel (<code>ShellToHost.setZoom</code> / <code>ShellToWebview.zoom</code>), and <code>app/main/index.ts</code> persists it under the <code>uiZoom</code> stateStore key and re-applies it on the next launch (passed into <code>createMainWindow</code>). Presets live in <code>ZOOM_PRESETS</code> — the shell renderer mirrors the same list to build the dropdown.</p><p><strong>One instance, one open queue.</strong> KKSS takes <code>app.requestSingleInstanceLock()</code> at module load (<code>app/main/index.ts</code>). A second launch does not become a second app: it prints a diagnostic, quits, and its argv reaches the running instance through the <code>second-instance</code> event, which focuses the window and opens the file. This matters beyond convenience — two instances share one <code>userData/state.json</code>, and because the store rewrites that file whole, the loser&#39;s first settings write would silently discard everything the winner had changed, including the safeStorage-encrypted API key and the MCP bearer token. macOS&#39;s <code>open-file</code> (Finder &quot;Open With&quot;, a file dropped on the dock icon) fires <em>before</em> <code>app.whenReady()</code>, so an early path is queued and flushed once the window and its starter tabs exist; the command-line argument drains through the same queue, so there is exactly one &quot;open this path at launch&quot; mechanism. A relative path forwarded by a second instance resolves against <em>that</em> process&#39;s working directory, which <code>second-instance</code> supplies. Set <code>KKSS_ALLOW_MULTIPLE_INSTANCES=1</code> to opt out — <code>tools/e2eShared.mjs</code> does, because the e2e harness relaunches the app repeatedly and SIGKILLs the process tree between runs, and it is the escape hatch for a developer who keeps KKSS open while running <code>npm run smoke</code> or <code>npm run docs:screenshots</code> against the same <code>~/.config/kkss</code>.</p><p><strong>Renderer-crash recovery.</strong> <code>wireView()</code> in <code>app/main/windows.ts</code> is the single place every <code>WebContentsView</code> in the window is wired up — it re-asserts the zoom factor on <code>did-finish-load</code> <em>and</em> watches for a dead renderer (<code>render-process-gone</code>, plus a main-frame <code>did-fail-load</code> on one of our own <code>kkss://app/</code> pages; aborted loads and subresource failures are ignored). It only reports: the policy lives in <code>app/main/index.ts</code>, which owns the host maps and so knows what to replay. Recovery leans entirely on handshakes that already exist — the shell replays screen, tab strips and zoom on <code>shellReady</code>, the editor replays its last document on <code>editorReady</code>, the terminal&#39;s <code>termReady</code> reuses the still-running pty, the chat replays from its main-side transcript (now also on disk), and a mode tab re-runs the provider handshake through <code>openPath()</code> (whose <code>disposeSession()</code> is also what rejects the pending promises a crash would otherwise leak). So recovery is a reload, plus a file replay for a mode tab. It is bounded (a few attempts inside a five-minute window) so a reproducible crash cannot become a reload loop, and every recovery raises a toast. <code>unresponsive</code> is deliberately <strong>not</strong> wired to recovery — it fires on any long synchronous parse, which is exactly what both viewers do on a large mesh, so reloading on it would destroy a working session mid-load.</p><h3 id="flowgraph-embedding" tabindex="-1">Flowgraph embedding <a class="header-anchor" href="#flowgraph-embedding" aria-label="Permalink to “Flowgraph embedding”">​</a></h3><p>The mesh submodule&#39;s <strong>Flowgraph</strong> problemtype (<code>view: &quot;flowgraph&quot;</code>) splits the MDPA preview&#39;s viewport to embed the AGPL-3.0-or-later <a href="https://www.npmjs.com/package/@kratos-flowgraph/flowgraph" target="_blank" rel="noreferrer"><code>@kratos-flowgraph/flowgraph</code></a> node editor in an <code>&lt;iframe&gt;</code>, served by a small Express+EJS app the submodule forks on demand (<code>mesh/src/flowgraphServer.ts</code>, <code>mesh/src/flowgraphController.ts</code>). <code>app/main/index.ts</code> owns the shared, ref-counted <code>FlowgraphController</code> instance — mirroring <code>mesh/src/extension.ts</code>&#39;s <code>activate()</code> — and injects it into every <code>MeshHost</code>, which passes it into <code>new MdpaEditorProvider(context, flowgraph, runs, recents)</code> (the VTK provider also takes <code>(context, flowgraph, runs, recents)</code> and owns a Problemtype controller for non-MDPA formats) and disposes it on Electron&#39;s <code>will-quit</code> so the forked child process never outlives the app. <code>runs</code> is the shared <code>RunManager</code> and <code>recents</code> mesh 3.15.0&#39;s <code>RecentMeshStore</code> — the same construct-once-inject-everywhere rule, for the same reason: a solve, and the recent-file list, both outlive the tab they came from.</p><p><strong>Path contract</strong>: like the MMG worker pair, <code>flowgraphController.ts</code> resolves its server and assets via <code>__dirname</code>, so <code>esbuild.mjs</code>&#39;s <code>copyArtifacts()</code> places <code>out/flowgraphServer.js</code> and the <code>out/flowgraph/</code> asset tree (copied from <code>mesh/dist/flowgraph/</code> — Flowgraph&#39;s <code>public/</code>+ <code>views/</code>, its <code>LICENSE</code>, and our <code>vscode-bridge.js</code>) directly beside <code>out/main.js</code>.</p><h3 id="extended-mesh-formats-meshio" tabindex="-1">Extended mesh formats (meshio++) <a class="header-anchor" href="#extended-mesh-formats-meshio" aria-label="Permalink to “Extended mesh formats (meshio++)”">​</a></h3><p>The mesh submodule reads and writes extended mesh formats (Gmsh, Abaqus, Nastran, I-deas UNV, Medit, Netgen, SU2, XDMF, COMSOL, tetgen, EnSight Gold, Triangle, Exodus II, CGNS, MOAB, Salome MED, …) through <a href="https://www.npmjs.com/package/@meshioplusplus/wasm" target="_blank" rel="noreferrer"><code>@meshioplusplus/wasm</code></a> (12.0.0) — meshio++&#39;s C++ core compiled to WebAssembly. meshio++ adds the field-only <code>.dex</code>/<code>.ip</code>/<code>.mff</code> formats: they carry point fields with no cell geometry, so writing one keeps the points plus a field and drops connectivity, and reading one yields a point cloud (or an empty mesh); plus write-only SVG/TikZ figure formats (a 2D/3D-projected drawing of the mesh) surfaced in the export menu&#39;s &quot;Figures&quot; group. Since 8.5.0 the WASM statically links HDF5 and netCDF, which is what makes Exodus/CGNS/H5M/HMF/MED reachable — and since 8.6.0 a multi-step file exposes its steps through <code>ReadOptions.timeStep</code> / <code>MeshMetadata.timeValues</code>, which is the in-file timeline (9.9.0 added <code>timeStep</code> for MED too; since 11.3.0/12.0.0 MED, CGNS and Tecplot also report header-only metadata, so all of them can size their in-file timeline before a read). 9.9.0 also made MED a writable format and let SubModelParts survive an export to MED/Abaqus, by fixing the shapeless-data boundary that silently reshaped an <code>(n, 3)</code> vector field into <code>(3n, 1)</code> on the way into the WASM. It is ESM-only (its Emscripten glue reads <code>import.meta.url</code>), so the submodule keeps it <code>external</code> and ships it verbatim as the <code>mesh/dist/meshio/</code> tree, and <code>mesh/src/parser/meshio.ts</code> loads it through a runtime dynamic <code>import()</code> rather than a bundled require.</p><p><strong>Path contract</strong>: <code>meshio.ts</code>&#39;s <code>packageDir()</code> falls back to <code>path.join(__dirname, &quot;meshio&quot;)</code>, and <code>meshio.ts</code> is bundled into <strong>both</strong> <code>out/main.js</code> (mesh host → <code>meshFileParser</code>/<code>meshWriter</code>) and <code>out/mcpServer.js</code> — both with <code>__dirname === out/</code>. So <code>copyArtifacts()</code> mirrors <code>mesh/dist/meshio/</code> to a single <code>out/meshio/</code> tree beside <code>out/main.js</code>, serving the app host and the MCP server at once. The <code>.wasm</code> is loaded via meshio++&#39;s <code>locateFile</code> hook (the <code>wasmBinary</code> buffer hook MMG uses is unavailable in this build), which is why <code>out/</code> stays unpacked (<code>asar: false</code>). <code>@meshioplusplus/wasm</code> is also added to the parent <code>mainConfig.external</code> in <code>esbuild.mjs</code>, because the bundled <code>meshio.ts</code> contains a <code>require.resolve(&quot;@meshioplusplus/wasm/package.json&quot;)</code> literal esbuild would otherwise try to resolve at build time.</p><p><strong>Both WASM variants must ship.</strong> Since meshio++ 8.8.0 the package carries a threaded build (<code>meshioplusplus_wasm_mt.{mjs,wasm}</code>, ~+6.2 MB) alongside the sequential one, and its <code>resolveVariant()</code> picks the threaded one under Node — which is what KKSS&#39;s main process and <code>out/mcpServer.js</code> are. Shipping only the sequential pair makes <strong>every</strong> extended format fail with an opaque <code>LinkError</code>. The mesh submodule&#39;s own copy plugin emits all four files into <code>mesh/dist/meshio/</code>, and <code>copyArtifacts()</code> mirrors that tree wholesale (<code>fs.cpSync(..., { recursive: true })</code>), so no parent-side enumeration needs updating — but after a submodule bump, check <code>out/meshio/dist/meshioplusplus_wasm*</code> really lists four files.</p><p>Native menu enabled states are rebuilt after every screen switch, including file-open routing. Rebuilding only when recent files changed left mesh actions disabled on the first mesh open because that notification preceded the mode change. The smoke suite checks packing availability after file open and CAD/mesh switches.</p><h3 id="submodule-release-maintenance" tabindex="-1">Submodule release maintenance <a class="header-anchor" href="#submodule-release-maintenance" aria-label="Permalink to “Submodule release maintenance”">​</a></h3><p>Keep both gitlinks at the latest releases, preserving the <code>kkss.dev</code> integration commits. A successful build alone does not establish that the new capabilities work.</p><ol><li>Review upstream release notes and the source diff. Keep app-only behavior in KKSS. Required MCP contracts belong on the submodule&#39;s <code>kkss.dev</code> branch and extension changes on <code>application-downstream</code>; commit those upstream changes before the gitlink bump here.</li><li>Run <code>npm run build</code>: it builds CAD and runs <code>npm run package --prefix mesh</code> <strong>before</strong> the parent build copies artifacts. Check that <code>out/meshio/dist/meshioplusplus_wasm{,_mt}.{mjs,wasm}</code> contains all four files. Exercise an extended-format read/write through the copied runtime.</li><li>Compare <code>tools/webviewMarkup.ts</code>&#39;s mesh body element for element with <code>mesh/src/webviewChrome.ts</code>&#39;s <code>buildPreviewHtml</code>, using its exported fragments. Preserve the KKSS stylesheets, shim-before-bundle order and deliberate omission of <code>startEmpty</code>. The build&#39;s theme guard checks new <code>--vscode-*</code> and <code>--ds-*</code> uses.</li><li>Audit reachable VS Code APIs and commands against <code>app/main/vscodeShim.ts</code>; extend the shim when needed, keeping unsupported commands loud. Every action available only from the hidden mesh menubar needs a native-menu route. Also check new palette/tree-only capabilities, such as time-series packing.</li><li>Start the <strong>copied</strong> CAD and mesh MCP servers with the environment from <code>serverSpecs()</code> in <code>mcpManager.ts</code>, list their actual tools and compare both missing and obsolete names against <code>toolPolicy.ts</code>. Classify new tools deliberately; the pinned test checks the policy table, not the live upstream tool list. Verify new viewer capabilities through MCP too, and update the stable system-prompt capability description for new app abilities. Settings and current-file context must stay aligned where applicable.</li><li>Run <code>npm run typecheck</code>, <code>npm test</code>, <code>npm run smoke</code> (under <code>xvfb-run -a</code> on headless Linux), and <code>npm run docs:build</code>. Exercise the release&#39;s new operations with upstream fixtures and temporary outputs. Update user guides, README and project memory; regenerate screenshots with <code>npm run docs:screenshots</code> if viewer markup or visible behavior changes.</li></ol><p>The CAD MCP kernel is built from the unchanged <code>cad/src/kernelWorker.ts</code> by <code>cadMcpWorkerConfig</code> in <code>esbuild.mjs</code>. It shares the interactive worker&#39;s Gmsh CJS alias and meshio/fTetWild loaders, but lives at <code>out/cad-runtime/dist/kernel-worker.js</code>, beside OCCT/Gmsh WASM. The loaders also resolve <code>../../meshio</code> and <code>../../ftetwild</code> from there. <strong>Do not overwrite it with <code>cad/dist/kernel-worker.js</code></strong>: that upstream artifact expects external npm packages shipped by the extension, which KKSS does not include. <code>test/cadMcpRuntime.test.ts</code> checks real OCCT and meshio calls through the bundled server and forked worker.</p><p>Format counts depend on whether extensions, reader keys or writer keys are counted. Use <code>mesh/src/parser/meshioFormats.ts</code>&#39;s read/write routing tables and <code>mesh/src/parser/writers/exportFormats.ts</code> for the supported surfaces instead of a shared approximate count. Packed time series use an <code>.xdmf</code> index plus a required sibling <code>.h5</code>; keep both when copying results.</p><h3 id="about-dialog-updates" tabindex="-1">About dialog &amp; updates <a class="header-anchor" href="#about-dialog-updates" aria-label="Permalink to “About dialog &amp; updates”">​</a></h3><p><strong>Help ▸ About KKSS…</strong> (and the home screen&#39;s Help button) opens a frameless singleton window (<code>app/main/services/about.ts</code>, same pattern as the modal picker) backed by <code>app/renderer/about/</code> over <code>about:init</code> / <code>about:toHost</code> / <code>about:toWebview</code> (<code>app/preload/aboutPreload.ts</code>). It shows the version (<code>app.getVersion()</code>), the author (injected from <code>package.json</code> by an esbuild <code>define</code>), and an update check.</p><p>Update flow (<code>app/main/services/updates.ts</code>):</p><ul><li><strong>Availability</strong> — the GitHub REST API (<code>releases/latest</code>) + a <code>semver</code> compare; works in dev runs too. Offline / rate-limited / bad tags degrade to a &quot;Couldn&#39;t check for updates&quot; line with Retry — never a crash.</li><li><strong>Delivery</strong> — <code>electron-updater</code> (GitHub provider), only where the app can self-replace: the Windows NSIS install and the Linux AppImage. <code>.deb</code> installs and the (unsigned) macOS builds get an &quot;Open releases page&quot; button instead, as does any runtime updater failure.</li><li>Both <code>semver</code> and <code>electron-updater</code> are devDependencies bundled into <code>out/main.js</code> by esbuild — the package still ships no <code>node_modules</code>.</li></ul><p>The feed plumbing electron-updater needs: the <code>publish:</code> block in <code>electron-builder.yml</code> makes electron-builder emit <code>latest*.yml</code> into <code>release/</code> and embed <code>resources/app-update.yml</code> in each package (even with <code>--publish never</code>), and <code>.github/workflows/release.yml</code> uploads <code>release/latest*.yml</code> + <code>release/*.blockmap</code> so they land on the GitHub Release next to the installers. Remove either half and in-app updates stop finding releases.</p><h3 id="what-s-new-changelog-dialog" tabindex="-1">What&#39;s New / changelog dialog <a class="header-anchor" href="#what-s-new-changelog-dialog" aria-label="Permalink to “What&#39;s New / changelog dialog”">​</a></h3><p><code>app/main/services/whatsNew.ts</code> shows a frameless singleton window (same pattern as the About dialog) backed by <code>app/renderer/whatsnew/</code> over <code>whatsNew:init</code> / <code>whatsNew:toHost</code> (<code>app/preload/whatsNewPreload.ts</code>). <code>checkForNewVersion()</code> runs once at startup (<code>app/main/index.ts</code>): it compares the <code>lastSeenVersion</code> stateStore key against <code>app.getVersion()</code> and, if the version changed, shows the CHANGELOG.md entries newer than the last-seen one (<code>semver.gt</code> per entry). It stays silent on a fresh install (nothing to diff against yet) and under the e2e smoke test (<code>KKSS_E2E</code>). <strong>Help ▸ What&#39;s New…</strong> (<code>showChangelog()</code>) reopens the full history on demand, regardless of version.</p><p>Content comes straight from the repo&#39;s <code>CHANGELOG.md</code> — <code>esbuild.mjs</code>&#39;s <code>copyArtifacts()</code> copies it verbatim to <code>out/CHANGELOG.md</code> (read via <code>__dirname</code> next to <code>out/main.js</code>, same path-contract pattern as the other <code>out/</code>-relative assets), and <code>app/main/services/changelog.ts</code>&#39;s <code>parseChangelog()</code> — kept electron-import-free like <code>updateCheck.ts</code>, so <code>test/changelog.test.ts</code> can exercise it directly — splits it on the <code>## [X.Y.Z] - YYYY-MM-DD</code> headings the <code>CLAUDE.md</code> changelog-sync rule enforces. Keeping that format is what keeps this dialog&#39;s content accurate; a heading that doesn&#39;t match the pattern is silently skipped.</p><h2 id="guided-workflow-services" tabindex="-1">Guided workflow services <a class="header-anchor" href="#guided-workflow-services" aria-label="Permalink to “Guided workflow services”">​</a></h2><p><code>app/main/services/workflows/</code> contains the app-owned v1 project, environment and queue contracts. The Home renderer and <code>app__*</code> tools call the same <code>WorkflowService</code>; the in-process tool registry is included by the shared MCP manager, so chat and the optional HTTP meta server see the same tool definitions and approval policy. Dynamic chat context includes the selected project/study identity. <code>ProjectStore</code> serializes read/modify/write updates and uses the existing atomic file writer; unknown project schema versions are preserved and rejected. Project-relative artifact references resolve against the selected root after folder moves, while external paths remain marked as external.</p><p>The environment probe runs a bounded Python import/version check separately for manual runs and the uvx tool runtime. The uvx probe uses offline/no-sync flags and never installs packages. KKSS wires the shared report into mesh&#39;s <code>RunManager</code> preflight: <code>PtController</code> publishes capability to the webview, which disables only Run when the case requirements fail, and <code>RunManager.start</code> forces a fresh check before dispatch. With no guard configured, the standalone extension retains its prior behavior. CAD MDPA exports produce a versioned handoff manifest and atomic owner/request receipts with artifact revisions; mesh queue runs snapshot inputs into isolated workspaces with stable owner/request receipts and owner-scoped cancellation. The persistent queue stores dispatch intent before calling a runner, reconciles CAD mesh and solver request IDs, and leaves unresolved work uncertain rather than resubmitting it. Terminal runs can be imported into immutable project-local input snapshots; large result files remain revision-checked references. Structural case generation writes a versioned per-step convergence monitor. <code>mesh__case_evaluate_quantity</code> uses the existing field parser and reductions to evaluate one explicitly selected field/component, region, time and reduction. <code>app__run_quantity_evaluate</code> requires an explicit unit, verifies the run and result revision, and stores the quantity with a portable source reference. Reviews omit values whose source result is stale; variant comparisons require matching definitions and units, and classify mesh-sensitivity separately from solver-setting changes using recorded mesh revisions. The Home row list can resume one waiting run row at a time while holding other waiting work, or preview a failed row&#39;s retry as a new study/run identity in the existing comparison group. Reviews include revision-checked generated inputs and the mesh runner&#39;s existing quality report. The versioned structural monitor records solver-published residual norms, criterion parameters, and solve-step coordinates for nonlinear residual-criterion runs. Linear solves, other criteria, interrupted runs, and truncated monitor files remain explicitly unavailable; process exit alone does not establish convergence. The preparation report records effective settings, units, validation findings, generator/runtime identity, source and mesh revisions, and hashes of generated inputs.</p><p>The real structural cantilever acceptance harness is opt-in because it launches Gmsh and Kratos. After building the app and submodules and preparing the repository&#39;s isolated Python 3.12/Kratos runtime, run:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({
		"--shiki-light": "#24292e",
		"--shiki-dark": "#e1e4e8",
		"--shiki-light-bg": "#fff",
		"--shiki-dark-bg": "#24292e"
	})}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">KKSS_RUN_TIER4_ACCEPTANCE</span><span style="${ssrRenderStyle({
		"--shiki-light": "#c62739",
		"--shiki-dark": "#F97583"
	})}">=</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}">1</span><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">KKSS_TIER4_PYTHON=</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}">&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">\$PWD</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}">/node_modules/.cache/kkss-tier4/runtime/bin/python&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">npx </span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}">vitest</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> run</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> test/tier4Acceptance.test.ts</span></span></code></pre></div><p>The harness imports the CAD fixture through the actual CAD MCP server, generates a tetrahedral mesh, generates and solves the structural case through the shared workflow service, reviews solver residual evidence, evaluates tip displacement, duplicates the solved study, then resumes a two-row parameter sweep across a service restart. The Euler–Bernoulli reference displacement for its 18 × 4 × 5 mm cantilever is approximately 0.00059986 mm; the first-order tetrahedral result is accepted within a 25% discretization tolerance (the recorded run was within 2%). The separate workflow tests cover interrupted dispatch, dependency failures, owner-scoped cancellation, moved projects, unknown schemas, and immutable run snapshots.</p><h2 id="embedded-terminal-node-pty-xterm-js" tabindex="-1">Embedded terminal (node-pty + xterm.js) <a class="header-anchor" href="#embedded-terminal-node-pty-xterm-js" aria-label="Permalink to “Embedded terminal (node-pty + xterm.js)”">​</a></h2><p>The Terminal toolbar button / <code>Ctrl+\` </code> toggles a bottom panel <code>WebContentsView</code> (lazily created in <code>app/main/windows.ts</code>; <code>layout()</code> shrinks the mode views by <code>TERMINAL_HEIGHT</code> while it&#39;s shown). The renderer (<code>app/renderer/terminal/</code>, <code>@xterm/xterm</code> + fit addon) talks to <code>app/main/services/terminal.ts</code> over <code>term:toHost</code> / <code>term:toWebview</code> (<code>app/preload/terminalPreload.ts</code>): one node-pty session shared by both modes, spawned on first show in the current file&#39;s directory — PowerShell on Windows, <code>\$SHELL</code> elsewhere, overridable via <strong>Settings ▸ Terminal Shell</strong> (<code>stateStore</code> key <code>terminalShell</code>) — kept alive while hidden, killed on quit; the renderer offers an Enter-to-restart when the shell exits.</p><p><strong>node-pty is the app&#39;s only native module</strong>, and the only <code>node_modules</code> entry that ships in the package (see the <code>files</code> rules in <code>electron-builder.yml</code>; <code>asar: false</code> means the <code>.node</code> binaries load directly). It is N-API, so <strong>no Electron-ABI rebuild step exists or is needed</strong> — Windows/macOS use the prebuilt binaries shipped in the npm package, Linux compiles once during <code>npm ci</code> (GitHub runners and typical dev boxes have the toolchain). Two consequences to keep in mind:</p><ul><li><code>package.json</code>&#39;s <code>allowScripts</code> must keep the <code>node-pty@…</code> entry — without it the install scripts are skipped and the binaries never materialize.</li><li>The release workflow builds on <strong>one runner per OS/arch</strong> (<code>ubuntu-24.04-arm</code>, <code>windows-11-arm</code> for the arm64 targets): Linux needs a native compile and Windows assembles arch-specific ConPTY binaries at install time, so cross-arch packaging from a single runner is no longer possible.</li></ul><p><strong>CSP note:</strong> xterm.js injects <code>&lt;style&gt;</code> elements at runtime, so <code>app/renderer/terminal/index.html</code> allows <code>&#39;unsafe-inline&#39;</code> styles — this page only; every other page keeps the strict <code>style-src kkss:</code>.</p><h2 id="text-editor-codemirror-6" tabindex="-1">Text editor (CodeMirror 6) <a class="header-anchor" href="#text-editor-codemirror-6" aria-label="Permalink to “Text editor (CodeMirror 6)”">​</a></h2><p>The <code>editor</code> screen (<code>Screen = &quot;home&quot; | &quot;editor&quot; | Mode</code>) is a <code>WebContentsView</code> with body bounds — the shell toolbar stays visible and the terminal panel shares space with it. <code>app/renderer/editor/</code> bundles CodeMirror 6 (<code>codemirror</code> basic setup + <code>@codemirror/lang-json</code>/<code>lang-python</code></p><ul><li>one-dark theme); all fs work lives in <code>app/main/services/editor.ts</code> behind <code>editor:toHost</code> / <code>editor:toWebview</code> (<code>app/preload/editorPreload.ts</code>) — the renderer never touches the filesystem. File ▸ Save / Save As route to the editor when it&#39;s the active screen (<code>main.screen()</code>), and the in-page CodeMirror keymap binds <code>Mod-s</code> for the focused case. Dirty handling: the buffer survives screen switches (views are only hidden), so prompts fire only on the destructive paths — window close (Save / Don&#39;t Save / Cancel) and opening another file over unsaved changes. Like the terminal page, the editor page allows <code>&#39;unsafe-inline&#39;</code> styles (CodeMirror injects <code>&lt;style&gt;</code> at runtime).</li></ul><h2 id="kratos-background-jobs" tabindex="-1">Kratos background jobs <a class="header-anchor" href="#kratos-background-jobs" aria-label="Permalink to “Kratos background jobs”">​</a></h2><p><code>app/main/services/jobs.ts</code> observes the pinned Kratos server through the same <code>McpHub</code> used by chat and the HTTP meta server. It calls only <code>job_list</code>, <code>job_status</code>, <code>job_logs</code>, and user-requested <code>job_cancel</code>. Structured results (including FastMCP&#39;s <code>result</code> wrapper) and JSON text are validated; error objects are failures. Records stay owned by the server, with no new local store or solver process ownership.</p><p>The Jobs view is created lazily and shares the right-hand sidebar area with Chat. Opening either hides the other without stopping work. Typed <code>jobs:toHost</code> / <code>jobs:toWebview</code> messages carry ready, refresh, selection, cancellation, runtime recovery and hide requests, and full snapshots. The service verifies renderer identity and known job IDs. A <code>panels</code> shell message (<code>{terminal, chat}</code>, from <code>pushPanels()</code> in <code>index.ts</code>) drives the Terminal and Chat buttons&#39; <code>aria-pressed</code>, and every toggle — including Jobs, which closes Chat — pushes it. A <code>jobs</code> shell message supplies active count, visibility and staleness; both views replay state after reload.</p><p>Polling is serial, every five seconds while visible or active and thirty seconds otherwise after server startup. Active records receive status detail; selected running logs refresh with the poll, completed logs on explicit refresh. Disconnect generations discard late responses; per-job revisions prevent old polls undoing cancellation. Failures retain stale records, and observed active-to-success/failure transitions notify once. Disposal removes listeners/timers without cancelling jobs.</p><p>Both chat and raw MCP calls use one timeout selector: ten minutes by default, or a positive <code>run_simulation.wait_seconds</code> plus sixty seconds when longer. Waits beyond Node&#39;s timer range return an error before execution. The prompt recommends <code>wait_seconds=0</code> plus status/log polling; chat approval policy is unchanged.</p><p>Validation: <code>test/jobs.test.ts</code>, the timeout cases in <code>test/kratosStartup.test.ts</code>, and <code>node tools/jobs.e2e.mjs</code> (requires a display; use xvfb-run on headless Linux). The Electron scenario uses a deterministic stdio MCP fixture discovered through a temporary uvx wrapper, exercises reconnect/cancel/layout, and is reused by the screenshot generator.</p><h2 id="ai-chat-sidebar-llm-agent-mcp" tabindex="-1">AI chat sidebar (LLM agent + MCP) <a class="header-anchor" href="#ai-chat-sidebar-llm-agent-mcp" aria-label="Permalink to “AI chat sidebar (LLM agent + MCP)”">​</a></h2><p>The Chat toolbar button / <code>Ctrl+Shift+L</code> toggles a right-hand sidebar <code>WebContentsView</code> (lazily created in <code>app/main/windows.ts</code>; <code>layout()</code> shrinks the body views and the terminal panel by <code>CHAT_WIDTH</code> while it&#39;s shown). The renderer (<code>app/renderer/chat/</code>, dependency-free, strict CSP) talks to <code>app/main/services/chat/chatService.ts</code> over <code>chat:toHost</code> / <code>chat:toWebview</code> (<code>app/preload/chatPreload.ts</code>); all network and child-process work stays in the main process, and the transcript is replayed on <code>chatReady</code> so hiding/showing the sidebar never loses the conversation.</p><p>In streamed deployments, <code>app/main/services/managedConfig.ts</code> overlays operator values in memory above <code>stateStore</code>: <code>KKSS_LLM_PROVIDER</code>, model/base URL, secret-file, project root, restore, theme/zoom, meta-server and cloud client settings are read at startup, shown as managed in the native menu, and blocked from persistence. <code>app/main/services/headless.ts</code> exposes only loopback, bearer-protected activity/shutdown endpoints. The SIGTERM path saves dirty mesh operation recipes and editor buffers (with recovery files where possible), then drains cloud uploads before the normal synchronous store flush. Unknown or stale job state inhibits the container&#39;s idle timer.</p><p><code>ChatService</code> runs the agent loop: a provider adapter streams one model turn, tool calls are dispatched, and the loop repeats until the model stops calling tools (or the user hits Stop — dangling tool calls are pruned from the next request by <code>transcript.ts</code>). API providers live under <code>app/main/services/chat/providers/</code>: <strong>Anthropic</strong> uses <code>@anthropic-ai/sdk</code>, while <strong>OpenAI-compatible</strong> uses raw <code>fetch</code> + SSE. Subscription providers live under <code>app/main/services/chat/agents/</code>: <strong>Codex</strong> speaks the official Codex App Server protocol over stdio, and <strong>Claude Code</strong> uses the official Agent SDK with an in-process MCP server. Both subscription adapters call the same <code>ChatService.executeTool()</code> dispatcher, so approval, denial, dry-run, image, cancellation, and transcript behavior is shared. Subscription sessions persist an opaque provider session id and a hash of the KKSS tool catalog; a mismatch starts a fresh session seeded with transcript data. Historical tool calls are sent as context and are never executed again.</p><p><strong>Conversations are durable, and there are many of them.</strong> Transcripts live under <code>&lt;userData&gt;/chats/</code> — one <code>&lt;id&gt;.json</code> per conversation plus a small <code>index.json</code> of the rows the sidebar&#39;s history popover lists (title, last-used time, entry count, and which conversation was last active). Each file is its own <code>JsonStore</code> instance, so every write is the same atomic temp+fsync+rename behind a single-writer chain, and only the active conversation is held in memory (<code>TranscriptStore.close()</code> flushes and drops the rest). The split mirrors <code>sessionCore.ts</code>/<code>session.ts</code>: <code>transcriptStoreCore.ts</code> is pure (the versioned blob shapes, parsing that <em>repairs</em> a damaged entry list rather than losing the conversation, title derivation, the caps) and <code>transcriptStore.ts</code> is the filesystem glue. <code>CHAT_STORE_VERSION</code> is ignored wholesale on mismatch, and an index row whose file is gone — or whose blob this build cannot read — is pruned at read time rather than reconciled by a transaction across the two files. Entries are written on append boundaries with a 1 s debounce (never per stream delta: a tool result is capped at <code>RESULT_CHARS</code> = 50 000 characters), plus an undebounced save on each user message and at the end of every turn, and <code>ChatService.flushSync()</code> from <code>will-quit</code> (which lands a still-pending debounced save rather than cancelling it, so quitting inside the debounce window loses nothing). Caps: <code>MAX_CONVERSATIONS</code> = 50 (least-recently-updated evicted, never the active one) and <code>CONVERSATION_ENTRY_CAP</code> = 2000. Stored entries keep the <strong>full</strong> tool-result text the model was given — <code>toWire()</code>/<code>PREVIEW_CHARS</code> in <code>transcript.ts</code> remains the single truncation point, so the renderer never sees it.</p><p><strong>A turn is bound to the conversation it started in.</strong> <code>run()</code> captures the active conversation once and every append targets that object; every message it emits goes through a guard that drops it unless that conversation is still on screen. Switching, or deleting the <em>active</em> conversation, therefore aborts the in-flight turn and waits for it to unwind before the switch takes effect — the interrupted turn is recorded as an assistant entry carrying whatever text had streamed, flagged <code>stopped</code> (an append, not a flag set on the previous entry, which would mislabel an already-finished turn when the abort lands on iteration 2+ of the tool loop). Deleting a <em>background</em> conversation never disturbs the running one. <code>newChat</code> archives rather than destroys, so there is nothing to confirm; an untouched conversation is neither written nor listed, so <strong>New</strong> costs nothing.</p><p>Tools come from three stdio MCP servers managed by <code>app/main/services/chat/mcpManager.ts</code> (spawned lazily on first chat use, per-server failure tolerated, tool names namespaced <code>cad__*</code> / <code>mesh__*</code> / <code>kratos__*</code>):</p><table tabindex="0"><thead><tr><th>Server</th><th>Bundle / command</th><th>Placement contract</th></tr></thead><tbody><tr><td><code>cad-preview</code> (56 tools)</td><td><code>out/cad-runtime/dist/mcp-server.js</code></td><td>beside the OCCT/Gmsh WASM, so its <code>extensionPath</code> (= <code>dirname/..</code>) resolves to <code>out/cad-runtime</code></td></tr><tr><td><code>kratos-mdpa</code> (23 tools)</td><td><code>out/mcpServer.js</code></td><td>beside <code>out/mmg-core.wasm</code> (the bundle reads <code>__dirname/mmg-core.wasm</code>) and the <code>out/meshio/</code> tree (meshio++&#39;s <code>__dirname/meshio</code> fallback, for the extended-format tools)</td></tr><tr><td><code>kratos-mcp-server</code> (52 tools)</td><td><code>uvx --with &quot;mcp&lt;2&quot; kratos-mcp-server@&lt;version&gt;</code> (or <code>uv tool run</code>)</td><td>pinned to <code>KRATOS_MCP_VERSION</code>; app-local uv setup and independent retry available in chat</td></tr></tbody></table><p>The kratos server is <strong>pinned</strong> to <code>KRATOS_MCP_VERSION</code> (<code>kratosMcpVersion.ts</code>) — bump that constant to upgrade; the tool/resource/prompt surface is discovered at runtime (<code>listTools</code>), so no other code changes when it grows. Its 0.3.0 knowledge layer also ships MCP <strong>resources</strong> (worked examples) and <strong>prompts</strong> (guided setups); <code>McpManager</code> aggregates both (<code>listResources</code>/<code>readResource</code>/<code>listPrompts</code>/ <code>getPrompt</code>, resource URIs owner-mapped, prompt names namespaced). The provider loop only understands tools, so these are surfaced to the chat as four synthetic <code>mcp__*</code> tools (<code>chatTools()</code> = real tools + <code>mcp__list_resources</code> / <code>mcp__read_resource</code> / <code>mcp__list_prompts</code> / <code>mcp__get_prompt</code>).</p><p><code>kratosRuntime.ts</code> probes the app-local uv executable, PATH&#39;s <code>uvx</code>, then PATH&#39;s <code>uv</code>. Discovery never installs anything. The fixed-purpose <code>installKratosRuntime</code> and <code>retryKratos</code> chat IPC actions reach the shared hub&#39;s Kratos-only reconnect operation; they accept no paths, commands, or URLs. Status carries an optional startup phase (<code>probing</code>, <code>installing</code>, <code>preparing</code>) and a failure category, with bounded diagnostics. The assistant sees current availability through the context suffix, while its tool definitions remain fixed for each user turn.</p><p>The official uv <code>0.12.10</code> installer runs with <code>UV_UNMANAGED_INSTALL</code> under <code>&lt;userData&gt;/runtimes/</code>, verifies the executable, and promotes it to <code>&lt;userData&gt;/runtimes/uv</code>. Temporary files are removed on failure and success. Existing system installations and uv cache/Python locations are preserved. Overlapping operations share one promise; shutdown aborts setup and closes pending MCP transports. Kratos connection and tool discovery each allow five minutes; CAD/mesh retain their existing connection timeout and tool-call timeouts are unchanged.</p><p>The <code>mcp&lt;2</code> constraint is retained for <code>kratos-mcp-server@0.5.0</code>, which still imports <code>mcp.server.fastmcp</code>. The version pin is shared by the launcher, bundled-runtime validation, and offline environment probes; Docker requirements and their hash lock must be updated alongside it. The published 0.5.0 package was verified to initialize and advertise 52 tools, 13 resources, and five prompts. Linux installer and Electron recovery are verified; Windows and macOS installer commands have unit coverage but remain unverified on real machines. <code>node tools/kratosStartup.e2e.mjs</code> runs the isolated-profile recovery check, also used by <code>npm run docs:screenshots</code>.</p><p>The two Node bundles are spawned with <strong>Electron&#39;s own binary + <code>ELECTRON_RUN_AS_NODE=1</code></strong> (packaged machines have no system Node), and the full parent environment is always passed to <code>StdioClientTransport</code> — the SDK otherwise strips env to a minimal set, which silently breaks <code>uvx</code> (PATH). The bundles are copied from the submodules&#39; <code>dist/</code> by <code>esbuild.mjs</code>&#39;s <code>copyArtifacts()</code> — which also mirrors the <code>out/meshio/</code> tree beside <code>out/mcpServer.js</code> so the <code>mesh_convert</code>/<code>mesh_info</code> tools can read/write the extended meshio++ formats (see <em>Extended mesh formats</em> above). The submodule MCP servers are built by their normal <code>build</code>/<code>package</code> scripts on <code>kkss.dev</code>; any required extension-only change is committed on <code>application-downstream</code>.</p><p>API keys are entered via <strong>Settings ▸ LLM Assistant</strong> (<code>showInputBox</code> modals) and stored in the stateStore encrypted with Electron <code>safeStorage</code> (<code>app/main/services/chat/secrets.ts</code>; plaintext fallback when the OS has no keyring). Subscription credentials remain in the official tools&#39; stores. Settings are read per request — no restart needed. stateStore keys: <code>llmProvider</code>, <code>llmModelAnthropic</code>, <code>llmKeyAnthropic</code>, <code>llmModelOpenai</code>, <code>llmKeyOpenai</code>, <code>llmOpenaiBaseUrl</code>, <code>llmModelCodex</code>, <code>llmCodexExecutable</code>, <code>llmModelClaudeCode</code>, <code>llmClaudeCodeExecutable</code>, <code>llmToolApproval</code>.</p><h3 id="tool-call-approval" tabindex="-1">Tool-call approval <a class="header-anchor" href="#tool-call-approval" aria-label="Permalink to “Tool-call approval”">​</a></h3><p><code>app/main/services/chat/toolPolicy.ts</code> decides whether a tool the model asked for runs straight away or has to be approved. It is a <strong>pure</strong> module (no <code>electron</code>, no <code>node:*</code>, the approval mode passed in rather than read from the stateStore) so <code>test/</code> drives it directly — <code>test/chatToolPolicy.test.ts</code>.</p><p>Classification is a <strong>KKSS-side table keyed by the full namespaced name</strong> (<code>cad__apply_edit_ops</code>, <code>mesh__mesh_transform</code>), covering 114 bundled/in-process tools: 56 CAD, 24 mesh, four in-process <code>mcp__*</code> tools, and 30 app-owned <code>app__*</code> workflow tools. It is deliberately <em>not</em> derived from MCP&#39;s <code>Tool.annotations</code>: the SDK&#39;s own type declarations say a client must never make tool-use decisions from a server&#39;s annotations, and no cad/mesh tool declares any in the first place. Name-prefix and description heuristics are rejected for the same reason — <code>mesh__problemtype_list</code> looks like a listing and actually <em>executes</em> workspace problemtypes. Anything unlisted is <code>unknown</code>, which always asks; that is what makes the external <code>kratos-mcp-server</code> (tools resolved by uvx at runtime) safe without pretending to know what it does. <code>gateFor</code>&#39;s precedence is <code>never</code> → an always-allow grant → <code>askAlways</code> → read-is-auto → ask.</p><p>The gate sits in <code>chatService.ts</code>&#39;s tool loop, between appending the <code>toolCall</code> entry (so the user can read the arguments they are approving) and <code>mcp.callTool</code>. Three rules are load-bearing and easy to break:</p><ul><li><strong><code>awaitApproval</code> never rejects and always settles.</strong> <code>settleTurn()</code> <em>awaits</em> the running turn and five paths reach it (Stop, New chat, selecting another conversation, deleting the active one, <code>will-quit</code>), so a promise that could stay pending would deadlock all five — the abort listener is what keeps the app closable. <code>flushSync()</code> settles it too, before the store latches shut.</li><li><strong>A denial still appends a <code>toolResult</code></strong> (<code>ok:false</code>). <code>transcript.ts</code> drops a <code>toolCall</code> with no matching result, so a silent denial would vanish from the next request and the model would re-emit the same call, burning one of <code>MAX_ITERATIONS</code>.</li><li><strong>A <em>pending</em> approval is never persisted.</strong> It rides <code>ChatToWebview</code>&#39;s <code>state.pendingApproval</code> and is replayed on <code>chatReady</code>, so a renderer reload resumes a blocked turn rather than stranding it — while a hard crash cannot replay live-looking buttons wired to a promise that no longer exists. The <em>decision</em> is persisted, as an optional <code>approval</code> field on the <code>toolCall</code> entry, which needed <strong>no <code>CHAT_STORE_VERSION</code> bump</strong> (a bump discards every stored conversation; an optional field degrades correctly in both directions).</li></ul><p>&quot;Always allow in this conversation&quot; lives in a service-level <code>Map&lt;conversationId, Set&lt;string&gt;&gt;</code>, <strong>not</strong> on <code>LiveConversation</code>: <code>store.close()</code> drops that object on every conversation switch, so a grant there would expire on a round trip through the history popover. New wire messages: <code>approveTool</code> (renderer → main, correlated by <code>callId</code>), <code>approvalRequest</code> and <code>approvalResolved</code>.</p><p><code>unclassifiedTools()</code> is the submodule-bump seam: <code>ChatService</code> logs the unclassified names once per process, and <code>test/chatToolPolicy.test.ts</code> pins the exact 83-name policy key set. Compare it with live <code>tools/list</code> during a bump: the snapshot alone cannot discover new upstream tools.</p><h4 id="validate-dry-run" tabindex="-1">Validate (dry run) <a class="header-anchor" href="#validate-dry-run" aria-label="Permalink to “Validate (dry run)”">​</a></h4><p>Four cad tools take a <code>dryRun</code> parameter (<code>cad__apply_edit_ops</code>, <code>cad__run_parametric_script</code>, <code>cad__run_saved_script</code>, <code>cad__import_svg</code>), listed in <code>toolPolicy.ts</code>&#39;s <code>DRY_RUN_PARAM</code>. When the blocked call is one of them the prompt grows a fourth button, <strong>Validate (dry run)</strong>, which re-issues it with that flag set so the user can answer the prompt with a report in front of them.</p><p>It is deliberately <strong>not</strong> a fourth decision. <code>dryRunTool</code> (renderer → main, <code>callId</code>-correlated like <code>approveTool</code>) leaves the gate open, and the report is pushed back as <code>dryRunResult</code> and <strong>never appended to the transcript</strong> — so the model is never handed a result for a call that did not happen, which is the semantics problem that kept this unbuilt, and no <code>contextSuffix()</code> or system-prompt change was needed. <code>dryRunArgs()</code> in <code>toolPolicy.ts</code> is the single decision point: it rewrites the arguments without mutating them, and returns <code>null</code> (which is also what hides the button) for a tool with no row, unparseable or non-object arguments, or a call the model already marked <code>dryRun: true</code>.</p><p>Because the handler runs outside <code>run()</code>, the turn&#39;s conversation and abort signal are carried on the <code>pendingApproval</code> record and re-read when the call returns — a Stop, a conversation switch or a delete drops the report rather than painting it into whatever is on screen. <code>awaitApproval</code>&#39;s <code>finish()</code> nulls that record, which is what makes an in-flight dry run stale on Deny, Allow, abort and <code>flushSync()</code> alike. Re-entrancy is guarded on main rather than by disabling the button, since a <code>state</code> replay rebuilds the prompt with a fresh one. A completed report rides <code>pendingApproval.dryRunPreview</code> so a reload keeps it.</p><p>The wording is narrow on purpose: cad gates its OCCT replay on the same flag it gates its writes on, so the report says which operations parse and are legal — not what the geometry would become.</p><h4 id="images-in-tool-results" tabindex="-1">Images in tool results <a class="header-anchor" href="#images-in-tool-results" aria-label="Permalink to “Images in tool results”">​</a></h4><p><code>mcpManager.extractImages()</code> forwards an MCP tool result&#39;s <code>{type:&quot;image&quot;}</code> blocks to the sidebar, which is how <code>cad__render_snapshot</code> and <code>cad__compare_models</code> become visible. <code>flattenContent()</code> is untouched, so the <em>model</em> still reads its <code>[image content]</code> placeholder and the two views cannot drift apart (a test in <code>test/chatMcpRouting.test.ts</code> pins the pairing).</p><p>They are <strong>session-only</strong>. The transcript store holds the full tool-result text <em>the model was given</em>, and the model is not given the bytes — so <code>ChatEntry</code>, <code>parseEntry</code> and <code>CHAT_STORE_VERSION</code> are unchanged, and no conversation file carries megabytes of base64 through its whole-file atomic rewrite. They also never ride a <code>ChatWireEntry</code>: they travel as their own <code>toolImages</code> message, sent after the entry and replayed after <code>state</code>, because <code>sendState()</code> fires on far more than a reload and <code>webContents.send</code> structured-clones synchronously on the main thread. The cache is one <code>Map&lt;callId, ChatImage[]&gt;</code> for the active conversation, cleared in <code>switchTo()</code>, so <code>IMAGE_BUDGET_BYTES</code> is the global bound by construction (oldest-first eviction).</p><p>Caps are set for the renderer&#39;s benefit, not the wire&#39;s: a byte cap bounds the transfer but not the decoded bitmap, so the per-image cap is small, the message carries <code>live</code> (a replay never force-opens a chip), and the <code>&lt;img&gt;</code> is created lazily when the chip is actually expanded. SVG is excluded from the mime allow-list and the base64 is shape-validated — it is interpolated into a <code>data:</code> URL by a page with no <code>&#39;unsafe-inline&#39;</code>. <code>app/renderer/chat/index.html</code>&#39;s CSP already allowed <code>img-src kkss: data:</code>, so it needed no change.</p><h4 id="token-cost-and-context" tabindex="-1">Token, cost and context <a class="header-anchor" href="#token-cost-and-context" aria-label="Permalink to “Token, cost and context”">​</a></h4><p><code>TurnResult.usage</code> carries what a turn cost, and is <strong>optional</strong>: absent means the provider reported nothing, which must stay distinguishable from &quot;cost nothing&quot;. Getting one comparable number out of two APIs takes work on both sides — Anthropic bills <code>cache_read_input_tokens</code> and <code>cache_creation_input_tokens</code> <em>in addition to</em> <code>input_tokens</code> (so the context figure is the sum of all three), while OpenAI counts cached tokens <em>inside</em> <code>prompt_tokens</code> (so <code>parseUsageChunk</code> subtracts them back out). <code>TurnUsage.input</code> means uncached input on both.</p><p>The OpenAI-compatible path needs a request change too: usage is only sent when <code>stream_options: {include_usage: true}</code> is asked for, and it arrives in a final chunk with an <strong>empty <code>choices</code> array</strong> — which is why it is parsed before the <code>delta</code> guard that skips choice-less chunks. A stricter gateway may reject the unknown field outright, so a 400 mentioning <code>stream_options</code> retries once without it and accepts no usage, mirroring the Anthropic conservative retry.</p><p><code>app/main/services/chat/modelInfo.ts</code> holds context windows and per-1M prices, in the same deliberate-table mould as <code>toolPolicy.ts</code>: a row is a reviewed edit and <code>test/chatModelInfo.test.ts</code> pins the key set. <strong>An unrecognised model resolves to <code>null</code>, and the sidebar then shows raw token counts with no cost and no context percentage</strong> — the normal case for the OpenAI-compatible provider, which points at arbitrary gateways whose pricing KKSS cannot know. Cache rates are stored per row rather than derived from the input price, because the usual 0.1x/1.25x relationship has exceptions.</p><p>Cumulative usage is an optional <code>usage</code> field on <code>StoredConversation</code>, needing <strong>no <code>CHAT_STORE_VERSION</code> bump</strong> (the <code>approval</code> precedent), and is persisted rather than session-only because a lifetime cost that reset on restart would be worse than none. <code>lastInput</code> is replaced per turn rather than summed — &quot;how full is the window&quot; is a different question from &quot;what has this cost&quot;. Main resolves the price and window before sending, so the renderer never carries the pricing table.</p><p>Two new <code>ChatErrorKind</code>s, <code>context</code> and <code>rateLimit</code>, replace the bare <code>other</code> banner these used to get. <strong>Any new kind must also be added to <code>ERROR_KINDS</code> in <code>transcriptStoreCore.ts</code></strong>, or a stored error of that kind is silently rewritten to <code>other</code> on read. A context overflow also suppresses the Anthropic conservative retry: it is a 400, but lowering <code>max_tokens</code> cannot shorten an over-long prompt, so retrying only cost a round trip and reported the wrong error.</p><h4 id="prompt-caching" tabindex="-1">Prompt caching <a class="header-anchor" href="#prompt-caching" aria-label="Permalink to “Prompt caching”">​</a></h4><p>The Anthropic request carries one <code>cache_control</code> breakpoint, on the <code>system</code> block. The render order is <code>tools</code> → <code>system</code> → <code>messages</code>, so it covers the tool definitions too — which is the point, since the whole MCP toolset is re-sent on each of up to <code>MAX_ITERATIONS</code> iterations. This is what <code>SYSTEM_PROMPT</code>&#39;s byte-stability and <code>contextSuffix()</code>&#39;s &quot;volatile context rides the newest user message&quot; rule were always for; before the breakpoint existed the discipline was paid for and never collected on. Consequently <strong>changing the system prompt, or moving volatile context into it, now has a measurable cost</strong>, and <code>cache_read_input_tokens</code> reading zero across repeated turns means something is invalidating the prefix. The conservative retry drops <code>cache_control</code> along with <code>thinking</code>.</p><p><code>ChatService.run()</code> snapshots the available tool definitions once per user turn and uses that list for every model request, including context-overflow retries and the unclassified-tool check. It does not wait for all MCP servers to connect: newly connected servers contribute tools on the next user turn. This keeps asynchronous startup from changing the tools + system cache prefix between iterations of the first turn. The system prompt is unchanged.</p><h4 id="transcript-compaction" tabindex="-1">Transcript compaction <a class="header-anchor" href="#transcript-compaction" aria-label="Permalink to “Transcript compaction”">​</a></h4><p>A long conversation eventually stops fitting in the model&#39;s context window. <code>app/main/services/chat/compaction.ts</code> handles that by <strong>clearing the text of older tool results from requests</strong> — not by summarizing. It runs on the copy <code>requestEntries()</code> builds, so <code>convo.entries</code>, the sidebar and <code>&lt;id&gt;.json</code> all keep the full text; the sidebar still shows every result, and a line under the header discloses that the model is no longer being sent them.</p><p>Clearing works because it is structurally invisible: <code>groupTurns</code> matches a result to its call by <code>callId</code> alone and the &quot;answered&quot; filter is a <code>Map.has</code>, so replacing a <code>toolResult</code>&#39;s text leaves the message list role-for-role identical. That removes the cut-point rule, the orphan-<code>tool_result</code> hazard and the model call summarizing would have needed. It only holds for tool <em>results</em> — blanking an assistant entry can delete a whole message. Only <code>ok === true</code> results are cleared: a denied call&#39;s text is the &quot;do not retry&quot; instruction the approval gate depends on.</p><p>The boundary is a <strong>count</strong> (<code>StoredConversation.compactedResults</code>), not a reference to a particular call. An id anchor fails twice — it vanishes when <code>capEntries</code> trims the front, and tool-call ids are not unique on the OpenAI-compatible path, where a gateway that streams no ids gets synthetic <code>call_&lt;index&gt;</code> values that repeat every iteration. A count has no identity to lose, and when it no longer matches the entries it simply clears more. The invariant is that a stale boundary must never clear <em>less</em> than before. It is persisted (with a parse line in <code>parseConversation</code>, which rebuilds field by field) so it survives a restart and a conversation switch, and it only ever grows.</p><p>Two triggers. <strong>Proactive</strong>: after the per-iteration usage fold, when <code>lastInput</code> passes 70% of a known context window — which with 1M windows rarely fires. <strong>Reactive</strong>: a <code>context</code> provider error advances the boundary and retries, and this is the path that does the real work, and the only one that helps a model whose window KKSS does not know. The retry wraps only the <code>streamTurn</code> call, never the loop body, because <code>assistantStart</code> opens a fresh assistant bubble in the renderer; it retries once by construction, since the second call has no catch.</p><p>What it cannot fix: the tool schemas re-sent every iteration, and <code>toolCall.argsJson</code>, which nothing truncates and which cannot be blanked because the wire formats need real JSON. And &quot;tool results dominate&quot; is an empirical claim, not a bound — one maximal assistant message is larger than one maximal tool result.</p><p>Two latent defects were fixed alongside it: <code>capEntries</code> snapped its cut forward to the next user entry instead of slicing mid-turn, and <code>toAnthropicMessages</code>&#39;s first-message trim now <em>strips</em> leading <code>tool_result</code> blocks rather than dropping the message (same-role merging means that first user message routinely carries the orphan and the first real user text together).</p><h3 id="meta-mcp-server-expose-the-toolset-over-http" tabindex="-1">Meta MCP server (expose the toolset over HTTP) <a class="header-anchor" href="#meta-mcp-server-expose-the-toolset-over-http" aria-label="Permalink to “Meta MCP server (expose the toolset over HTTP)”">​</a></h3><p>The same aggregated toolset can be re-exposed as a single MCP <strong>server</strong> so an <em>external</em> LLM client (Claude Desktop, another agent) drives KKSS — the inverse of the sidebar (which makes KKSS an MCP client). One <code>McpManager</code> is shared between both front-ends via <strong><code>McpHub</code></strong> (<code>app/main/services/chat/mcpHub.ts</code>), constructed once in <code>index.ts</code> and disposed on <code>will-quit</code>; whichever of {chat opened, external client connected} happens first spawns the three children, the other reuses them — never a double spawn.</p><p><code>app/main/services/metaServer/</code> holds the server: <code>buildServer.ts</code> wires <code>McpManager</code> behind the low-level MCP <code>Server</code> (raw JSON-Schema tools forwarded verbatim via <code>callToolRaw</code>, resources &amp; prompts re-exposed natively), and <code>metaServer.ts</code> (<code>MetaMcpServer</code>) runs a bare <code>http.createServer</code> bound to <code>127.0.0.1</code> with the SDK&#39;s <code>StreamableHTTPServerTransport</code> (stateful sessions keyed by <code>Mcp-Session-Id</code>; late server readiness emits <code>list_changed</code>). It is <strong>off by default</strong>, requires an <code>Authorization: Bearer &lt;token&gt;</code> (generated on first enable, safeStorage-encrypted like the API keys), and validates the <code>Host</code> header — these tools touch the filesystem and run simulations. The SDK server subpaths bundle into <code>out/main.js</code>; nothing new ships in <code>node_modules</code>. Enable it and copy the <code>http://127.0.0.1:&lt;port&gt;/mcp</code> address + token from <strong>Settings ▸ MCP Server</strong>. stateStore keys: <code>metaServerEnabled</code>, <code>metaServerPort</code> (default <code>7391</code>); secret: <code>metaServerToken</code>. <strong>Note the asymmetry with the built-in sidebar: <code>callToolRaw</code> here is deliberately NOT gated by the tool-approval policy above</strong> — an external client has no user to prompt, so this path is protected by the bearer token alone. That means one <code>McpHub</code> serving two front-ends with different trust models; it is a known gap, not an oversight.</p><h3 id="cloud-staging-layer" tabindex="-1">Cloud staging layer <a class="header-anchor" href="#cloud-staging-layer" aria-label="Permalink to “Cloud staging layer”">​</a></h3><p><code>app/main/services/cloud/</code> opens a Google Drive / Dropbox / OneDrive file by <strong>downloading it into a local staging cache and handing that local path to <code>openFile()</code></strong>. Nothing below the staging layer knows a document is remote: routing (<code>router.ts</code>&#39;s longest-suffix rule), <code>allowRoot()</code>, both hosts&#39; <code>fs</code> calls and the submodules&#39; own MCP servers all keep working unmodified. That is the only shape that was available — every consumer of an opened path assumes a real file already on disk, and the zero-modification invariant rules out teaching the submodules&#39; <code>node:fs</code> calls to speak a provider API.</p><p>The pure/glue split is the usual one, so <code>test/</code> can drive the interesting parts without Electron: <code>cloudCore.ts</code> (types, <code>CLOUD_KEYS</code>), <code>cachePathCore.ts</code> (path derivation, sidecar rules), <code>manifestCore.ts</code>, <code>conflictCore.ts</code>, <code>oauthCore.ts</code>, <code>uploadChunkCore.ts</code> and the three <code>providerCore/*.ts</code> parsers are pure; <code>oauth.ts</code>, <code>providers/*.ts</code>, <code>stagingCache.ts</code>, <code>cloudSync.ts</code> and the <code>cloudService.ts</code> façade are the glue. Tests: <code>cloudCachePath</code>, <code>cloudManifest</code>, <code>cloudConflict</code>, <code>cloudOauth</code>, <code>cloudProviders</code>, <code>cloudUploadChunks</code> and <code>cloudSync</code> — the last drives the whole write-back engine against a real temp directory, real chokidar and a fake provider.</p><p>Decisions worth knowing before changing any of it:</p><ul><li><strong>Layout.</strong> <code>&lt;userData&gt;/cloud-cache/&lt;provider&gt;/&lt;sha256(account,item)[0:16]&gt;/&lt;original filename&gt;</code>. Deterministic so sidecars survive across sessions, opaque so a Dropbox path or Drive id cannot escape the cache root, and the filename is preserved byte-for-byte because routing is extension-driven (<code>case.post.msh</code> must not become <code>case.msh</code>). <strong>One directory holds exactly one document plus its sidecars</strong> — that is what makes cad&#39;s <code>\${modelPath}.parts.json</code> siblings land correctly with no <code>cadHost.ts</code> change, and makes &quot;every other non-artifact file here is a sidecar&quot; a safe rule rather than a guess.</li><li><strong><code>allowRoot()</code> is untouched.</strong> <code>CadHost.openPath</code> already allow-lists <code>path.dirname(fsPath)</code>, which for a staged file <em>is</em> its staging directory. Allow-listing <code>cloud-cache/</code> itself would make every cached document from every account fetchable by any webview — the mistake the project-folder invariant exists to avoid.</li><li><strong>Write-back is watcher-driven, not host-driven, and that is forced.</strong><code>mesh/src/meshExport.ts</code>&#39;s <code>saveMesh()</code> overwrites the document in place with a bare <code>fs.promises.writeFile</code> inside the submodule and never reports the path back, so a save can be observed but never intercepted. One <code>depth: 0</code> chokidar watch per staging directory (the bare <code>*</code> pattern added to <code>services/watcher.ts</code>) covers cad&#39;s eight sidecar writers, mesh&#39;s in-place save, an export aimed back into the directory and <code>EditorService</code> at once. Its <code>depth: 0</code> is also a real limit: XDMF&#39;s sibling <code>.h5</code> is uploaded, OpenFOAM&#39;s nested <code>constant/polyMesh/</code> tree is deliberately not.</li><li><strong>Revisions.</strong> Drive <code>headRevisionId</code>, Dropbox <code>rev</code>, Graph <strong><code>cTag</code> — never <code>eTag</code></strong>, which also moves on metadata-only edits and would manufacture a conflict copy every time OneDrive touched the file for its own reasons. The wire-level precondition is carried separately (<code>precondition</code> on <code>CloudFile</code>) because Graph&#39;s <code>if-match</code> accepts only <code>eTag</code>. Only Dropbox enforces the conditional server-side; Drive has no conditional media overwrite at all, so its guard is check-then-upload with a residual race, which its module header states plainly.</li><li><strong>Conflicts</strong> keep the local file untouched and upload it beside the remote one, named with <code>meshExtname</code>&#39;s longest-suffix split so <code>case.post.msh</code> stays routable. The remote&#39;s current revision is then adopted as the new baseline, or every subsequent tick would conflict forever.</li><li><strong><code>cad-preview-macros.json</code> and <code>cad-preview-mesh-presets.json</code> are never synced</strong> — both are per-<em>folder</em> libraries while the cache is per-<em>file</em>, so uploading either would let two models from one remote folder overwrite each other&#39;s.</li><li><strong>Quit.</strong> <code>will-quit</code> cannot await an HTTPS upload, so <code>before-quit</code> calls <code>preventDefault()</code><strong>once</strong> (guarded by a flag, so it can never loop), drains for up to 10 s, then re-quits. Anything still unsent stays <code>dirty: true</code> in the manifest and is reported on the next launch. <code>will-quit</code> only calls <code>cloud.flushSync()</code>, the manifest <code>JsonStore</code>&#39;s last synchronous write.</li><li><strong>OAuth is bring-your-own-client.</strong> No KKSS client id is baked in. <code>oauth.ts</code> reuses <code>metaServer.ts</code>&#39;s listen/error/Host-check shape for a one-shot <code>127.0.0.1</code> listener, with PKCE S256 and a <code>state</code> check; tokens go through <code>chat/secrets.ts</code>. Dropbox pins port 53682 because its console requires an exact redirect URI; Google and Microsoft take an ephemeral one.</li><li><strong>Uploads take a simple path below ~4 MB</strong> (Drive <code>uploadType=media</code>/<code>multipart</code>, Graph <code>PUT .../content</code>, Dropbox <code>files/upload</code>) and a chunked one above it. That is not only about cost — a sidecar is a couple of KB and a session would be three round trips for nothing — neither Drive nor Graph can finalise a <em>zero-byte</em> resumable session, so <code>uploadChunked</code> rejects <code>size === 0</code> outright rather than spinning on a chunk it can never produce.</li><li><strong>No new dependencies.</strong> All three providers are plain REST over <code>net.fetch</code>, <code>node:crypto</code>, <code>node:http</code> and <code>node:stream</code>, plus the <code>chokidar</code> KKSS already ships. The official SDKs are license-compatible but each drags a large tree into <code>out/main.js</code> for three endpoints.</li></ul><h4 id="verifying-a-provider-by-hand" tabindex="-1">Verifying a provider by hand <a class="header-anchor" href="#verifying-a-provider-by-hand" aria-label="Permalink to “Verifying a provider by hand”">​</a></h4><p>The consent round trip, the live endpoint shapes and resumable uploads cannot be unit-tested — they need a real registration. Per provider, once per release that touches this area:</p><ol><li>Register a desktop/installed-app OAuth client (see <a href="/KKSS/guide/configuration#cloud-accounts">Configuration ▸ Cloud accounts</a> for redirect URIs and scopes) and paste the client ID under <strong>Settings ▸ Cloud Accounts</strong>.</li><li><strong>Connect…</strong> — the browser opens, consent returns to the loopback listener, and the status row shows the account.</li><li><strong>File ▸ Open from Cloud…</strong> → open a <code>.stp</code>. Confirm the tab shows ☁ and the model renders.</li><li>Edit something, wait ~5 s, and confirm the sidecars appear beside the model remotely.</li><li>Change the file from the provider&#39;s web UI, edit locally again, and confirm a <code>(conflict …)</code> copy appears and the local file is untouched.</li><li>Upload something over 4 MB (Graph) / 150 MB (Dropbox) to exercise the chunked path.</li><li>Quit mid-upload and confirm the next launch reports the unsynced file.</li></ol><h2 id="settings" tabindex="-1">Settings <a class="header-anchor" href="#settings" aria-label="Permalink to “Settings”">​</a></h2><p>All settings are defined in <strong>one registry</strong>, <code>app/main/services/settings/registry.ts</code>. It is pure: no Electron and no stateStore. Each entry declares:</p><ul><li>an id (<code>kratos.pythonPath</code>, <code>appearance.uiTheme</code>, …), label, category, type, default and description;</li><li>the stateStore key that holds it (older keys keep their names, so nothing stored is lost);</li><li>when a change applies;</li><li>optionally, a <code>vscode: {section, key}</code> mapping.</li></ul><p>Three consumers read it:</p><ul><li><strong>The shim.</strong> <code>vscodeShim.ts</code>&#39;s <code>workspace.getConfiguration(section).get(key, default)</code> looks up <code>section.key</code>. Sections may be dotted, as in <code>kratos.flowgraph</code>. A mapped key is served from the stateStore; anything else returns the caller&#39;s default. That is how every <code>kratos.*</code> setting the unmodified mesh providers read works, with no submodule change. The shim also implements <code>update()</code> and <code>onDidChangeConfiguration</code>.</li><li><strong>The Settings page.</strong> <code>app/renderer/settings/</code> runs in its own singleton <code>BrowserWindow</code>, owned by <code>services/settings/settingsWindow.ts</code>, with <code>settings:toHost</code>/<code>settings:toWebview</code> guarded by sender. <ul><li>Main sends the schema once and then a <code>rows</code> snapshot on every stateStore change.</li><li>Every write is validated by the registry&#39;s <code>toStored</code> before it lands. A value equal to the default, or an empty string, deletes the key.</li><li>Entries with side effects route through the same functions the native menu uses: zoom, the update channel, the MCP server, cloud accounts, and the &quot;Never ask&quot; confirmation (<code>menu.ts</code>&#39;s <code>confirmApprovalOff</code>).</li><li>Secrets are write-only. The renderer submits a new value but only ever receives <code>isSet</code>.</li></ul></li><li><strong>The native menu.</strong> <code>menu.ts</code> keeps quick radios built from registry entries (<code>enumRadio</code>) and is rebuilt on any change to a registry-owned stateStore key. The page and the menu cannot drift.</li></ul><p><code>stateStore.onDidChange(key, value)</code> is the change event behind all three. <code>test/settingsRegistry.test.ts</code> parses both submodules&#39; <code>contributes.configuration</code> and requires every property to be either a registry entry with the same type, enum and default, or listed in <code>NOT_APPLICABLE</code> with a reason. A submodule bump that adds a setting fails that test until KKSS decides what to do with it.</p><p><strong>UI theme.</strong> <code>services/appearance.ts</code> (with the pure <code>appearanceCore.ts</code>) resolves <code>uiTheme</code> through Electron&#39;s <code>nativeTheme</code> to one of VS Code&#39;s four theme kinds (<code>vscode-dark</code>, <code>vscode-light</code>, <code>vscode-high-contrast</code>, <code>vscode-high-contrast-light</code>).</p><ul><li><strong>First paint.</strong> Every preload imports <code>app/preload/appearance.ts</code>, which fetches the snapshot synchronously (<code>kkss:appearance</code>). It puts the kind as a class plus <code>data-vscode-theme-kind</code> on <code>&lt;html&gt;</code> and <code>&lt;body&gt;</code>, and sets the UI font as CSSOM custom properties.</li><li><strong>Live changes.</strong> Main re-broadcasts to every <code>webContents</code> when a relevant key or the OS theme changes.</li><li><strong>Palettes.</strong> <code>vscode-vars.css</code> has a <code>:root</code> (Dark+) block plus one block per other kind. <code>check-theme-vars.mjs</code> fails the build if a block does not redefine exactly <code>:root</code>&#39;s colour variables.</li><li><strong>CAD viewer.</strong> It restyles itself: its <code>viewer.css</code> keys on the same body classes, and a MutationObserver re-reads the palette.</li><li><strong>Mesh viewer.</strong> Its <em>auto</em> scene samples the body background only when a theme is applied. The view shim therefore re-fires mesh&#39;s hidden <code>#theme-select</code> change on each appearance change while it is <code>auto</code>.</li><li><strong>Editor and terminal.</strong> They read the same snapshot for their own options: CodeMirror through a <code>Compartment</code>, xterm through <code>term.options</code>.</li></ul><p><strong>Kratos environment.</strong> <code>services/settings/kratosEnv.ts</code> builds the delta that <code>McpManager</code> spreads over the Kratos MCP server&#39;s environment on every (re)start. It uses mesh&#39;s vscode-free <code>problemtype/kratosEnv.ts</code>, the same code the Run button uses. <code>McpManager.restartKratos()</code> backs the page&#39;s restart button.</p><p>Key pieces (all under <code>app/</code>):</p><ul><li><strong><code>app/preload/viewPreload.ts</code> + <code>app/renderer/view/shim.ts</code></strong> — the entire VS Code compatibility layer: <code>acquireVsCodeApi().postMessage</code> → IPC, and inbound IPC → a normal window <code>message</code> event.</li><li><strong><code>app/main/vscodeShim.ts</code></strong> — a minimal <code>vscode</code> module. Its <code>workspace.workspaceFolders</code> is a <strong>getter</strong> backed by the <em>explicit</em> project folder (via the <code>__configureVscodeShim</code> hooks object): that is what makes <code>ptController.discoverExternal()</code> scan <code>&lt;root&gt;/.kratos/problemtypes</code>, so a project-local problemtype appears in the Problemtype list — impossible while this was permanently <code>undefined</code>. Explicit-only is a safety decision, not a style one: <code>discoverExternal</code> <em>executes</em> what it finds (sandboxed in a <code>node:vm</code> with no <code>require</code>/<code>process</code>/<code>fs</code>, no codegen and a 2 s timeout), so merely opening a mesh that happens to sit beside a <code>.kratos/problemtypes/</code> must never run it. Python problemtypes stay unavailable in KKSS (pyodide is not copied into <code>out/</code>) and surface as a per-file error row rather than a crash. The rest of the shim (dialogs, messages, file watcher, progress, <code>openWith</code>, <code>getConfiguration</code> — served from the settings registry (see <em>Settings</em> above), the Settings page being KKSS&#39;s settings.json — <code>openTextDocument</code>/<code>showTextDocument</code> routed to the app&#39;s own text-editor screen, and <code>env.asExternalUri</code> as an identity passthrough since there is no Remote-SSH/Codespaces tunnel) that esbuild aliases in place of the real API, letting <code>mesh/src/{mdpaEditorProvider,vtkEditorProvider,meshExport, opHistory,flowgraphController,ptController,runManager,recentMeshes,previewHtml,meshDocument}.ts</code> run verbatim.</li></ul><p>mesh 3.18.0 made both preview providers full <code>vscode.CustomEditorProvider</code>s (were <code>CustomReadonlyEditorProvider</code>): applying an operation now marks the document dirty, and only <code>vscode.workspace.save(uri)</code> clears it. <code>app/main/mesh/meshHost.ts</code>&#39;s <code>resolveProviderFor</code> mints the document from the provider&#39;s own <code>openCustomDocument(uri, {backupId: undefined, ...}, token)</code> rather than a bare stand-in (<code>resolveCustomEditor</code> unconditionally reads <code>document.takeRestoredOps()</code>), keeps it (and the provider that minted it) alongside its panel, and the shim&#39;s fifth hook, <code>saveMesh(fsPath)</code>, is what <code>workspace.save</code> calls into — it finds the <code>MeshHost</code> whose document owns the uri and calls its <code>saveDocument()</code>, which calls <code>saveCustomDocument</code> on that exact document. <code>MeshHost.isDirty()</code>/the <code>onDidChangeCustomDocument</code> subscription back the tab strip&#39;s dirty dot and <code>index.ts</code>&#39;s close/quit prompts (<code>confirmDiscardMeshTab</code>, mirroring <code>EditorService.confirmClose</code>&#39;s dialog shape). Three modules are deliberately left <em>unreachable</em> rather than shimmed — <code>runTreeView.ts</code>, <code>sidebarViews.ts</code> and <code>emptyPreview.ts</code>, each constructed only from the submodule&#39;s own <code>activate()</code>, which KKSS never calls — which is what keeps <code>createTreeView</code>/<code>TreeItem</code>/<code>registerCommand</code>/<code>createWebviewPanel</code> out of both the bundle and the shim.</p><ul><li><strong><code>app/main/cadHost.ts</code></strong> — a 1:1 port of <code>cad/src/provider.ts</code>&#39;s editor session (the cad provider imports OCCT directly, which must live in a worker here, so the cad side is ported rather than shimmed). Its <code>readOcctSource</code> is the single choke point every OCCT path reads through, so a <code>.scad</code> is converted to <code>.csg</code> by the user-installed <code>openscad</code> binary once and nothing downstream ever sees format <code>&quot;scad&quot;</code>. Deliberately <em>not</em> ported: SpaceMouse (it needs <code>node-hid</code>, a second native module — see the node-pty section) and the Models activity-bar view (a VS Code TreeView with no KKSS analogue); nor the provider&#39;s <code>getHtml</code>/<code>getNonce</code> (KKSS generates its page with <code>tools/gen-webview-html.mjs</code>, whose CSP allow-lists the <code>kkss:</code> scheme instead of a nonce) and its <code>registerCommands</code> (KKSS&#39;s command surface is <code>app/main/menu.ts</code>, and its What&#39;s New lives in <code>app/main/services/whatsNew.ts</code>). Its <code>CadHostHooks.onMeshExported</code> fires after a meshing-panel export writes a file; <code>app/main/index.ts</code> wires it to refresh a matching clean mesh tab or create a new one (gated by <code>modeForFile</code>) so a mesh exported in pre mode that post mode can display (<code>.mdpa</code>, <code>.vtk</code>, …) opens straight into the mesh view — a one-way pre → post sync. Three things to keep in step with the provider on a bump: <strong>plane references are resolved before the first tessellation</strong> (<code>ready</code> loads edits and planes together and runs <code>resolvePlaneRefs</code>, and <code>loadModel</code> resolves the replay tail again — a plane-authored profile carries no placement of its own since cad 2.5.0, and the kernel skips one that was never resolved); the <strong>meshing-preset library</strong> (<code>cad-preview-mesh-presets.json</code>, per <em>folder</em> like the macro library, atomic-written, never cloud-synced, with the bundled starters read from <code>out/cad-runtime/dist/mesh-presets/</code>); and the <strong>Standard-Parts thumbnail cache</strong>, which is module-level because the provider&#39;s is shared by every document while a <code>CadHost</code> is per tab.</li><li><strong><code>app/main/services/jsonStore.ts</code></strong> — the Electron-free half of <code>stateStore.ts</code> (the same split as <code>chat/secretCodec.ts</code> under <code>chat/secrets.ts</code>, and unit-tested by <code>test/stateStore.test.ts</code>). It is instantiated <strong>per file</strong> — <code>state.json</code> plus one instance per chat conversation and one for their index — so each file gets its own writer chain and <code>will-quit</code> flushes each of them. Every write goes through <strong><code>app/main/services/atomicWrite.ts</code></strong> — a sibling temp file, fsynced, then renamed over the target (atomic on POSIX and NTFS) — behind a single-writer chain, so concurrent <code>update()</code> calls cannot interleave or tear the file. This is not incidental: the LLM API key and the MCP meta-server&#39;s bearer token live in that same <code>state.json</code>, so one torn write used to lose every setting <em>and</em> both credentials at once, after which the store&#39;s silent corrupt-file fallback booted the app looking factory-fresh. A write that is still queued absorbs later mutations rather than making them wait, which is what the many fire-and-forget <code>void stateStore.update(...)</code> callers (every Settings menu click, the zoom picker, the mesh Memento) want. <code>will-quit</code> cannot await, so it calls <code>flushSync()</code> — an in-flight async write can only resume after that returns, and re-checks a stopped flag before its own rename, so it can never land a stale snapshot on top.</li><li><strong><code>app/main/services/atomicWrite.ts</code></strong> (tested by <code>test/atomicWrite.test.ts</code>) — the temp-file + fsync + rename primitive, extracted from <code>jsonStore.ts</code> so cadHost&#39;s eight sidecar writers get the same guarantee. It also <strong>serializes writes per resolved path</strong>, which is not optional once a temp name is involved: two overlapping writes to one path would share <code>&lt;file&gt;.&lt;pid&gt;.tmp</code> and the second <code>rename</code> would fail with ENOENT. Two such overlaps exist in <code>cadHost.ts</code> — <code>flushSidecars()</code> clears the six debounce timers but cannot cancel one that has <em>already fired</em>, and <code>cad-preview-macros.json</code> / <code>cad-preview-mesh-presets.json</code> are per-<em>folder</em> libraries that two tabs on models in one directory both write. The sync variant deliberately skips the fsync and the Windows rename retry (it runs on the quit path, where being fast matters more than being durable) and uses a distinct <code>.sync.tmp</code> name — it bypasses the chain, so sharing the async temp name would let a write that is mid-<code>open()</code> end up holding a handle to the file the sync write has just renamed into place. Sidecar suffixes live in <code>app/main/services/sidecarSuffixes.ts</code>, imported by both <code>cadHost.ts</code> and the cloud layer so the two lists cannot drift.</li><li><strong><code>app/main/services/recentFiles.ts</code></strong> (+ the pure <code>recentFilesCore.ts</code>, tested by <code>test/recentFiles.test.ts</code>) — the app-wide recents list, covering both modes. Recorded at exactly one choke point, <code>openFile()</code>, which is why every user-facing open routes through that function; the three <code>host.openPath()</code> callers that bypass it (crash replay, <code>openLatestResults</code>, <code>onMeshExported</code>) and session restore deliberately record nothing. De-duplication is by path with the mode refreshed, so a format both modes can read is one row that remembers where it was last opened. An entry carrying a <code>cloud</code> ref is <strong>exempt from the existence prune</strong>: its local copy is a staging copy the cache may have evicted, while the remote file is still there, so the row survives and clicking it re-downloads. The mesh submodule&#39;s own <code>RecentMeshStore</code> keeps running — both providers require it — but no longer drives any UI. <code>recentKey</code>/<code>recentLabel</code>/<code>recentDescription</code> are reused from <code>mesh/src/recentMeshesCore.ts</code>, which is vscode-free; its <code>recordRecent</code>/<code>parseRecentList</code> are not, because they would silently drop the <code>mode</code> field.</li><li><strong><code>app/main/services/projectRoot.ts</code></strong> (+ the pure <code>projectRootCore.ts</code>, tested by <code>test/projectRoot.test.ts</code>) — the project folder. Two levels: <strong>explicit</strong> (chosen via File ▸ Open Folder…, persisted under <code>projectRoot</code>) and <strong>effective</strong> (explicit, else the focused document&#39;s directory). Consumers use <code>effective()</code>, so with no explicit root every one of them behaves exactly as it did before the concept existed. Only <code>explicit()</code> is ever <em>displayed</em> or handed to the shim — an inferred root would change with the focused tab, and would put a developer&#39;s absolute path into the committed docs screenshots. A stored root that has been deleted or unmounted degrades to &quot;none&quot; on read but is deliberately not erased. <code>describeWithin()</code> is what makes Open Recent read as &quot;this project&quot;: entries inside the root show their folder relative to it, everything else keeps the <code>~</code>-abbreviated form — no re-sectioning, which would fight the list&#39;s newest-first order.</li><li><strong><code>app/main/services/session.ts</code></strong> (+ the pure <code>sessionCore.ts</code>, tested by <code>test/session.test.ts</code>) — session restore. Persists each mode&#39;s open documents in tab order plus the focused <em>path</em> (tab ids are a per-process counter, and an index shifts when pruning drops an earlier file), the screen, and panel visibility. Captured debounced from <code>syncTabs()</code>/<code>setScreen()</code>/the panel toggles — so a SIGKILL still leaves a usable session — and once more on <code>will-quit</code> <strong>before</strong> <code>stateStore.flushSync()</code>, since that call stops the store and would swallow a later write. Restoring prunes vanished paths <em>first</em>, opens each file with <code>openPath()</code> (append, no re-routing, no recording), closes the blank starter tab, then restores screen and panels. Gated by <code>KKSS_E2E</code>, <code>KKSS_NO_RESTORE=1</code>, and <strong>Settings ▸ Restore Last Session</strong>.</li><li><strong><code>kkss://</code> and <code>kkss-file://</code></strong> schemes — replacements for <code>asWebviewUri</code>/<code>localResourceRoots</code> (app assets and allow-listed user files respectively). External <code>kkss://open?file=…</code> links are validated by <code>app/main/launch.ts</code> and enter the same deferred queue as Finder and second-instance opens. Native associations and Linux MIME entries are checked against the CAD and mesh registries by <code>npm run check:packaging</code>.</li><li><strong><code>app/main/cadCompute.worker.ts</code> / <code>cadComputeClient.ts</code></strong> — the RPC boundary for everything WASM-backed on the cad side. The worker spreads fifteen modules into one flat method map (the submodule&#39;s <code>occtService</code>, <code>gmshService</code>, <code>massProperties</code>, <code>entityFacts</code>, <code>meshioService</code>, <code>meshioRegionParts</code> — CPU-only but pulls in Three.js — <code>hitTestService</code>, <code>meshHeal</code>, <code>primitiveReport</code>, <code>meshRegionFit</code>, <code>primitiveWrite</code>, <code>svgSilhouetteHost</code>, <code>modelDiffHost</code> and <code>stepPartsService</code>, plus KKSS&#39;s own <code>cadBRepCache</code>), and throws at startup if two export the same name; the client re-exposes each with the submodule&#39;s own signature via <code>Parameters&lt;typeof …&gt;</code>, so a changed signature is a type error rather than a runtime surprise. Pure text helpers (<code>stepUnits</code>/<code>igesUnits</code> unit detection) stay in the main process — a worker round trip would buy nothing. Since cad 3.0.0 the client also tracks <strong>kernel readiness</strong> for the status bar: <code>cadKernelStatus.ts</code> (Electron-free, so unit-tested) feeds cad&#39;s own reducer from <code>start</code>/<code>success</code>/<code>failure</code> events around every call and <code>reset</code> when the worker dies, translating KKSS&#39;s two differently-spelled RPC names (<code>loadBRepCachedInWorker</code>, <code>releaseBRepCache</code>) through <code>KKSS_FN_ALIAS</code>; <code>cadHost.ts</code> fans changes out as <code>kernelStatus</code> to every open CAD tab, and also posts <code>documentInfo</code> (the menubar chip — see <a href="/KKSS/guide/cad-mode#reading-the-window-at-a-glance">Pre-Processing mode</a>).</li><li><strong><code>app/main/cadBRepCache.ts</code></strong> — cad 1.2.6&#39;s <code>loadBRepCached</code> reuses a parsed base shape across interactive edits, but the cache entry it hands back owns live OCCT handles and can never be structured-cloned. So the entry lives in the worker beside the OCCT singleton that owns it, and only the plain <code>BRepResult</code> crosses the RPC. Entries are keyed by a stable per-tab session id, so every open cad tab gets its own slot; <code>releaseBRepCache</code> is the counterpart of the provider&#39;s <code>onDidDispose</code> teardown and is called from <code>CadHost.disposeSession</code>. On a thrown load the entry is dropped, never disposed — <code>loadBRepCached</code>&#39;s own contract, since a WASM abort may have left it half-torn-down.</li><li><strong><code>app/main/cadMeshioLoader.ts</code></strong> — esbuild aliases it in place of <code>@meshioplusplus/wasm</code> in the cad worker. <code>cad/src/meshioService.ts</code> loads meshio++ with a bare <code>await import(...)</code> and no directory fallback (unlike mesh&#39;s own loader, which ends at <code>__dirname/meshio</code>), which would be <code>ERR_MODULE_NOT_FOUND</code> in a packaged install where KKSS ships no <code>node_modules</code>. The shim resolves the copied <code>out/meshio/</code> tree and passes a name-aware <code>locateFile</code>. It must not use <code>require.resolve(&quot;@meshioplusplus/wasm/package.json&quot;)</code> — the alias catches that subpath too, and esbuild would try to resolve it against the shim file itself.</li><li><strong><code>tools/gen-webview-html.mjs</code></strong> (→ <code>tools/webviewMarkup.ts</code>) — builds each mode&#39;s HTML page from the submodules&#39; own markup modules (<code>viewerDom.ts</code>, <code>webviewChrome.ts</code>) at build time, so the DOM always matches what the extensions expect. The mesh page must mirror <code>webviewChrome.buildPreviewHtml</code>&#39;s body element for element (since mesh 3.15.0 that one function is the skeleton both editor providers and the standalone empty panel share, via <code>previewHtml.ts</code>; it can&#39;t simply be called, because KKSS links two extra stylesheets and must load <code>shim.js</code> before the bundle), and links three stylesheets in order: the submodule&#39;s <code>design-system.css</code> (the <code>--ds-*</code> token layer <code>style.css</code> builds on), <code>style.css</code>, then <code>app/renderer/theme/mesh-overrides.css</code>.</li><li><strong><code>app/renderer/theme/mesh-overrides.css</code></strong> — KKSS-only overrides for the mesh webview. It hides <code>#file-menu</code> and <code>#theme-select</code>: mesh 3.0.0 put the viewer&#39;s File menu and scene-theme picker in an in-flow <code>#menubar</code> strip, both of which the native menu already owns. The markup is still emitted (<code>webview/main.ts</code> looks those nodes up by id), so anything reachable <em>only</em> from that menu — Save/Load Problem — needs a native menu entry. The strip itself stays visible since the mesh UI redesign, because it holds the <strong>document chip</strong> (<code>#doc-chip</code>: file name, format badge, &quot;N unsaved edits&quot;, fed by the <code>documentInfo</code> message), and the page now ends in a <code>#statusbar</code> (engine activity from <code>engineStatus</code>, model counts, timeline frame, last Inspect pick). Both messages ride <code>MeshHost</code>&#39;s unfiltered <code>postMessage</code> relay — no KKSS-side handling — so a mesh bump only has to keep <code>meshBody()</code> mirroring <code>buildPreviewHtml</code> (the status bar is <code>STATUSBAR_HTML</code>, emitted after <code>#main</code>). The engine line reflects only the main process&#39;s engines: the chat sidebar&#39;s mesh MCP server is a separate child with its own meshio++/MMG, which the chip does not see. Note the converse: mesh 3.14.4&#39;s <strong>Edges</strong> toggle lives in the visible <code>View ▾</code> popup, not the strip, so it deliberately has no native entry (and no host message that could drive one).</li><li><strong><code>app/renderer/theme/vscode-vars.css</code></strong> — the <code>--vscode-*</code> theme variables VS Code normally injects; <code>tools/check-theme-vars.mjs</code> fails the build if a submodule update uses one that is missing, and likewise for a <code>--ds-*</code> token <code>style.css</code> uses but <code>design-system.css</code> doesn&#39;t define. The guard also scans every stylesheet under <code>app/renderer/</code>, so a <code>--vscode-*</code> variable used by KKSS&#39;s own pages must be defined here too.</li><li><strong><code>app/renderer/theme/kkss-ui.css</code></strong> + <strong><code>app/renderer/glyphs.ts</code></strong> — the design layer KKSS&#39;s <em>own</em> pages (shell, home, editor, terminal, chat, jobs, About, What&#39;s New, picker) share, matching CAD-Preview v3.0.0&#39;s look. <code>kkss-ui.css</code> holds the <code>--ui-*</code> metric tokens (radii, spacing, type sizes, control heights — never colour) and the shared recipes: <code>.btn</code> (ghost by default; <code>-secondary</code>/<code>-primary</code>/<code>-outline</code>/<code>-sm</code>/<code>-link</code>), <code>.icon-btn</code>, <code>.tb-div</code>, <code>.seg</code> (segmented control), <code>.section-header</code>/<code>.panel-icon</code>/<code>.eyebrow</code>, <code>.card</code>, <code>.field</code>, <code>.ui-badge</code>/<code>.ui-dot</code>, <code>.toast</code>, <code>.spinner</code>, the global <code>[hidden]</code> rule and the <code>:focus-visible</code> ring. Every page links it right after <code>vscode-vars.css</code> and before its own stylesheet, which therefore holds only layout and page-specific bits (a page that needs a pixel value uses the token scale: 5px controls, 8px cards). <code>glyphs.ts</code>&#39;s <code>glyph(id, size?)</code> re-exports cad&#39;s line-glyph set (<code>cad/src/uiGlyphs.ts</code>) plus KKSS extras (<code>x</code>, <code>menu</code>, <code>send</code>, <code>history</code>, …); add a missing icon to <code>KKSS_GLYPHS</code> there. The generated TikZ <code>shell/shellIcons.ts</code> still supplies the mode/home/open/edit/terminal/chat/settings/help icons. Strict-CSP pages (<code>style-src kkss:</code>) must not use inline <code>style=</code> attributes — toggle the <code>hidden</code> attribute instead.</li></ul><h2 id="building-from-source" tabindex="-1">Building from source <a class="header-anchor" href="#building-from-source" aria-label="Permalink to “Building from source”">​</a></h2><div class="language-bash"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({
		"--shiki-light": "#24292e",
		"--shiki-dark": "#e1e4e8",
		"--shiki-light-bg": "#fff",
		"--shiki-dark-bg": "#24292e"
	})}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">git</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> clone</span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}"> --recurse-submodules</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> https://github.com/loumalouomega/KKSS.git</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}">cd</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> KKSS</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">npm</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> ci</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">npm</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> run</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> submodules:install</span><span style="${ssrRenderStyle({
		"--shiki-light": "#62687b",
		"--shiki-dark": "#818e99"
	})}">   # npm ci in cad/ and mesh/</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">npm</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> run</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> build</span><span style="${ssrRenderStyle({
		"--shiki-light": "#62687b",
		"--shiki-dark": "#818e99"
	})}">                # submodule bundles → app bundles → HTML gen → theme guard</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">npm</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> start</span><span style="${ssrRenderStyle({
		"--shiki-light": "#62687b",
		"--shiki-dark": "#818e99"
	})}">                    # build + launch</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">npm</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> run</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> dist</span><span style="${ssrRenderStyle({
		"--shiki-light": "#62687b",
		"--shiki-dark": "#818e99"
	})}">                 # package installers into release/</span></span></code></pre></div><p>Day-to-day:</p><div class="language-bash"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({
		"--shiki-light": "#24292e",
		"--shiki-dark": "#e1e4e8",
		"--shiki-light-bg": "#fff",
		"--shiki-dark-bg": "#24292e"
	})}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">npm</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> run</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> build:app</span><span style="${ssrRenderStyle({
		"--shiki-light": "#62687b",
		"--shiki-dark": "#818e99"
	})}">   # skip the submodule rebuild when only app/ changed</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">npm</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> run</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> typecheck</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">npm</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> test</span><span style="${ssrRenderStyle({
		"--shiki-light": "#62687b",
		"--shiki-dark": "#818e99"
	})}">            # vitest glue tests (test/)</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">npm</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> run</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> smoke</span><span style="${ssrRenderStyle({
		"--shiki-light": "#62687b",
		"--shiki-dark": "#818e99"
	})}">       # headless end-to-end smoke test (needs xvfb on Linux)</span></span></code></pre></div><h3 id="dependency-security-pins" tabindex="-1">Dependency security pins <a class="header-anchor" href="#dependency-security-pins" aria-label="Permalink to “Dependency security pins”">​</a></h3><p>Only <code>node-pty</code> is a runtime dependency, but that understates the shipped surface: esbuild <strong>bundles</strong> several devDependencies into <code>out/main.js</code> (<code>@anthropic-ai/sdk</code>, <code>@modelcontextprotocol/sdk</code>, <code>electron-updater</code>, <code>semver</code>, and everything they pull in). A &quot;dev-only&quot; advisory on one of those transitives is therefore a real advisory against the packaged app, so check where a flagged package actually lands before dismissing it:</p><div class="language-bash"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({
		"--shiki-light": "#24292e",
		"--shiki-dark": "#e1e4e8",
		"--shiki-light-bg": "#fff",
		"--shiki-dark-bg": "#24292e"
	})}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">npm</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> ls</span><span style="${ssrRenderStyle({
		"--shiki-light": "#c62739",
		"--shiki-dark": "#F97583"
	})}"> &lt;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}">packag</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}">e</span><span style="${ssrRenderStyle({
		"--shiki-light": "#c62739",
		"--shiki-dark": "#F97583"
	})}">&gt;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}"> --all</span><span style="${ssrRenderStyle({
		"--shiki-light": "#62687b",
		"--shiki-dark": "#818e99"
	})}">      # who requires it</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">grep</span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}"> -c</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> &quot;&lt;package&gt;&quot;</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> out/main.js</span><span style="${ssrRenderStyle({
		"--shiki-light": "#62687b",
		"--shiki-dark": "#818e99"
	})}">   # does it reach the bundle?</span></span></code></pre></div><p>When an upstream range still admits a vulnerable version, pin it in the root <code>package.json</code>&#39;s <code>overrides</code> block (the <code>cad/</code> and <code>mesh/</code> submodules keep equivalent pins for their own trees) and drop the entry once upstream&#39;s own range excludes the bad versions. Current pins — <code>fast-uri</code> and <code>@hono/node-server</code>, both reaching the bundle through <code>@modelcontextprotocol/sdk</code> (via <code>ajv</code> and the SDK&#39;s <code>streamableHttp.js</code> transport respectively). Advisories that resolve only inside the <strong>electron-builder</strong> toolchain (<code>brace-expansion</code>, <code>minimatch</code>, <code>tar</code>) are build-time only — they never enter <code>out/</code>, and GitHub&#39;s Dependabot auto-dismisses them; do not force-resolve them, since the requested majors differ across that tree and a blanket override breaks packaging.</p><h2 id="regenerating-documentation-screenshots" tabindex="-1">Regenerating documentation screenshots <a class="header-anchor" href="#regenerating-documentation-screenshots" aria-label="Permalink to “Regenerating documentation screenshots”">​</a></h2><p>Screenshots are <strong>generated, not hand-captured</strong> — the same philosophy as the cad submodule&#39;s <code>scripts/screenshots/</code> pipeline, but even more end-to-end: <code>tools/screenshots.mjs</code> launches the real Electron app (Playwright-Electron) on real example files from the submodules and captures the live windows at 2x pixel density.</p><div class="language-bash"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({
		"--shiki-light": "#24292e",
		"--shiki-dark": "#e1e4e8",
		"--shiki-light-bg": "#fff",
		"--shiki-dark-bg": "#24292e"
	})}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">npm</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> run</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> build</span><span style="${ssrRenderStyle({
		"--shiki-light": "#62687b",
		"--shiki-dark": "#818e99"
	})}">                          # once, so out/ is complete</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">env</span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}"> -u</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> ELECTRON_RUN_AS_NODE</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> xvfb-run</span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}"> -a</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> npm</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> run</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> docs:screenshots</span><span style="${ssrRenderStyle({
		"--shiki-light": "#62687b",
		"--shiki-dark": "#818e99"
	})}">   # headless Linux</span></span></code></pre></div><p>PNGs land in <code>doc/public/screenshots/</code> (committed, kebab-case) and the two README heroes are refreshed in <code>images/</code>. Any change to the shell toolbar, the generated webview pages, or visible viewer behavior means re-running this — don&#39;t hand-edit the PNGs.</p><h2 id="icons" tabindex="-1">Icons <a class="header-anchor" href="#icons" aria-label="Permalink to “Icons”">​</a></h2><p><code>icons/</code> holds TikZ-drawn icon sources, mirroring the submodules&#39; pipeline (<code>pdflatex</code> + <code>pdftocairo</code> required — see <code>icons/README.md</code>):</p><ul><li><code>tikz-ui/*.tex</code> → <code>svg-ui/*.svg</code> → the generated (and committed) <code>app/renderer/shell/shellIcons.ts</code> — monochrome <code>currentColor</code> shell toolbar icons, tinted by the surrounding element&#39;s color.</li><li><code>tikz-app/kkss.tex</code> → <code>icons/app/icon{,-256,-1024}.png</code> — the teal tessellated-kiss application icon consumed by <code>electron-builder.yml</code> and the Linux window icon (<code>out/icon.png</code>).</li></ul><p>Regenerate everything with <code>npm run build:icons</code> and commit the sources together with the regenerated artifacts.</p><h2 id="updating-the-submodules" tabindex="-1">Updating the submodules <a class="header-anchor" href="#updating-the-submodules" aria-label="Permalink to “Updating the submodules”">​</a></h2><p>Upstream improvements are inherited by bumping the submodule pointer:</p><div class="language-bash"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({
		"--shiki-light": "#24292e",
		"--shiki-dark": "#e1e4e8",
		"--shiki-light-bg": "#fff",
		"--shiki-dark-bg": "#24292e"
	})}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">git</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> submodule</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> update</span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}"> --remote</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> cad</span><span style="${ssrRenderStyle({
		"--shiki-light": "#62687b",
		"--shiki-dark": "#818e99"
	})}">    # or mesh</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">npm</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> run</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> build</span><span style="${ssrRenderStyle({
		"--shiki-light": "#62687b",
		"--shiki-dark": "#818e99"
	})}">                        # rebuilds the bundle + re-runs check-theme-vars</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">npm</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> test</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}"> &amp;&amp; </span><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">npm</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> run</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> smoke</span><span style="${ssrRenderStyle({
		"--shiki-light": "#62687b",
		"--shiki-dark": "#818e99"
	})}">            # protocol drift shows up here</span></span>
<span class="line"><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">git</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> add</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> cad</span><span style="${ssrRenderStyle({
		"--shiki-light": "#24292E",
		"--shiki-dark": "#E1E4E8"
	})}"> &amp;&amp; </span><span style="${ssrRenderStyle({
		"--shiki-light": "#6F42C1",
		"--shiki-dark": "#B392F0"
	})}">git</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> commit</span><span style="${ssrRenderStyle({
		"--shiki-light": "#005CC5",
		"--shiki-dark": "#79B8FF"
	})}"> -m</span><span style="${ssrRenderStyle({
		"--shiki-light": "#032F62",
		"--shiki-dark": "#9ECBFF"
	})}"> &quot;Bump cad submodule&quot;</span></span></code></pre></div><p>The typecheck imports the extensions&#39; protocol types and the build re-greps their stylesheets, so a breaking protocol or theming change fails loudly rather than silently misbehaving.</p><p>If a change <strong>inside</strong> a submodule is ever unavoidable, commit it to a dedicated branch in that submodule (e.g. <code>application-downstream</code>) and point the KKSS gitlink there — never to the submodule&#39;s default branch.</p><h2 id="releasing" tabindex="-1">Releasing <a class="header-anchor" href="#releasing" aria-label="Permalink to “Releasing”">​</a></h2><p>Tag and push: <code>git tag v0.2.0 &amp;&amp; git push --tags</code>. The release workflow builds Windows/macOS/Linux installers and attaches them to a GitHub Release; tags containing a hyphen (<code>v0.2.0-rc1</code>) are marked as prereleases.</p><h2 id="full-electron-acceptance-suite" tabindex="-1">Full Electron acceptance suite <a class="header-anchor" href="#full-electron-acceptance-suite" aria-label="Permalink to “Full Electron acceptance suite”">​</a></h2><p>After <code>npm run build</code>, run <code>npm run e2e</code> (<code>xvfb-run -a npm run e2e</code> on headless Linux). CI runs this suite sequentially, without assertion retries, and uploads <code>test-results/</code> logs, page URLs, HTML and screenshots on failure. <code>npm run smoke</code> remains the shorter viewer/Home check. Individual scenarios live under <code>tools/e2e/</code> and can be run directly with Node.</p><p>The suite exercises generated CAD/mesh DOM, native menu/shim reload, CAD → mesh export and repeat-export refresh, editor saves, PTY output, tabs and actual view zoom, streamed chat with approval/denial/cancellation and transcript persistence, authenticated HTTP MCP enable/call/disable, cloud upload draining and recovery, session restoration, and real single-instance acquisition/forwarding/reacquisition. Assertions check rendered state, disk contents and service effects. Linux is the acceptance platform; these tests do not claim macOS/Windows validation.</p><p>Profiles and documents are temporary. Chat uses a loopback OpenAI-compatible SSE server and Kratos uses a local stdio MCP fixture through a temporary <code>uvx</code> wrapper. No provider credentials or solver installation are needed. CAD and mesh retain their real bundled runtimes. The cloud transport fixture activates only in unpackaged apps with <code>KKSS_E2E=1</code> and an absolute <code>KKSS_E2E_CLOUD_DIR</code>; it replaces only Dropbox&#39;s provider implementation, preserving the real cache, watcher, sync and quit paths. It has no renderer IPC controls. Upload barriers prove quit waits; failed and stalled transfers retain dirty state and can be retried after relaunch.</p><p><code>launchApp</code> accepts <code>env</code>, <code>restore</code> and <code>singleInstance</code> options. <code>restore: true</code> sets <code>KKSS_E2E_RESTORE=1</code>; explicit <code>KKSS_NO_RESTORE=1</code> and the user&#39;s Restore Last Session setting still take precedence. Defaults continue suppressing restoration and bypassing the instance lock for smoke and screenshots. Lifecycle tests use bounded graceful quit and observe the process exit; forced cleanup cannot count as acceptance.</p><p>Regression fixes found by this suite: assistant completion replaces the streaming bubble without also rendering the persisted entry; opening a staged path restarts upload tracking; repeated CAD exports refresh a matching clean mesh tab, while unrelated or dirty tabs remain intact.</p><h2 id="interface-language-and-accessibility" tabindex="-1">Interface language and accessibility <a class="header-anchor" href="#interface-language-and-accessibility" aria-label="Permalink to “Interface language and accessibility”">​</a></h2><p><code>app/shared/i18n/en.json</code> is the typed English message catalog and stable source key set; <code>es.json</code> supplies Spanish text. <code>t()</code> interpolates values without translating filenames, setting IDs, serialized values, logs or provider content, and falls back to English when an upstream or unknown message has no Spanish entry. The <code>general.language</code> setting is stored as <code>uiLanguage</code>; locale is resolved once at startup and sent to renderer pages through the locale preload bridge. Language changes require restart so all app windows and the native menu use one locale. Embedded viewer pages are owned by their upstream submodules.</p><p><code>windows.ts</code> moves focus between visible WebContentsView regions on F6 and Shift+F6 and restores focus to the invoking region after closing a panel. Document tabs use a roving tab stop and arrow/Home/End navigation. Renderer accessibility acceptance lives in <code>tools/e2e/accessibility.mjs</code>; it scans representative app-owned pages with axe-core in both locales and all four application themes, then checks focus movement and language persistence.</p><p><code>npm run perf</code> is an opt-in Electron benchmark, separate from CI gates. It measures five fresh launches to Home readiness and five file opens each for <code>cad/examples/STP/bull.stp</code> and <code>mesh/src/test/fixtures/regions/UUea.inp</code>, then reports samples and medians. These are fresh processes with fresh profiles; filesystem caches are uncontrolled. The checked-in baseline records fixture hashes, revisions, runtime and host metadata. Runs warn above three times the baseline; <code>PERF_STRICT=1</code> makes those warnings fail. Use <code>npm run perf -- --update-baseline</code> only after reviewing a deliberate change; the ordinary command writes only <code>test-results/perf.json</code>. Failure to reach one unambiguous completion marker always fails the run. The meshio++ fixture&#39;s model arrives in the <code>vtkFrame</code> protocol message, which is the timed model-load completion signal.</p><p>The 2026-09-24 baseline on an AMD Ryzen 7 255 / Linux x64 software-rendered host measured medians of <strong>753.3 ms</strong> to interactive Home, <strong>2,233.6 ms</strong> for the CAD geometry fixture and <strong>517.5 ms</strong> for the meshio++ model fixture. These are reference measurements from fresh processes; the harness neither warms WASM nor controls operating-system filesystem caches, so they are not cold cache figures or cross-machine performance targets.</p></div>`);
}
var _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
	const ssrContext = useSSRContext();
	(ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/development.md");
	return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
var development_default = /*#__PURE__*/ _plugin_vue_export_helper_default(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
//#endregion
export { __pageData, development_default as default };
