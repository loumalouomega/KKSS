import { t as _plugin_vue_export_helper_default } from "./plugin-vue_export-helper.BOaGB7Aw.js";
import { t as mesh_viewer_default } from "./mesh-viewer.DgnEvCrG.js";
import { ssrRenderAttr, ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
//#region ../../../../../screenshots/mesh-outline.png
var mesh_outline_default = "/KKSS/screenshots/mesh-outline.png";
//#endregion
//#region ../../../../../screenshots/mesh-view-menu.png
var mesh_view_menu_default = "/KKSS/screenshots/mesh-view-menu.png";
//#endregion
//#region ../../../../../screenshots/mesh-vtk-timeline.png
var mesh_vtk_timeline_default = "/KKSS/screenshots/mesh-vtk-timeline.png";
//#endregion
//#region guide/mesh-mode.md
var __pageData = JSON.parse("{\"title\":\"Post-Processing (Mesh) Mode\",\"description\":\"\",\"frontmatter\":{},\"headers\":[],\"relativePath\":\"guide/mesh-mode.md\",\"filePath\":\"guide/mesh-mode.md\"}");
var _sfc_main = { name: "guide/mesh-mode.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
	_push(`<div${ssrRenderAttrs(_attrs)}><h1 id="post-processing-mesh-mode" tabindex="-1">Post-Processing (Mesh) Mode <a class="header-anchor" href="#post-processing-mesh-mode" aria-label="Permalink to “Post-Processing (Mesh) Mode”">​</a></h1><p>Post-Processing mode embeds the full <a href="https://loumalouomega.github.io/VSCode-MDPA-Preview/" target="_blank" rel="noreferrer">VSCode-MDPA-Preview</a> viewer — KKSS drives the extension&#39;s own host code, so behavior matches the extension exactly. This page summarizes the highlights; see the <a href="https://loumalouomega.github.io/VSCode-MDPA-Preview/" target="_blank" rel="noreferrer">extension documentation</a> for the complete guide.</p><p><img${ssrRenderAttr("src", mesh_viewer_default)} alt="Mesh mode: an MDPA model with the outline, edit history and mesh-modification sidebar" width="5280" height="4578"></p><h2 id="reading-the-window-at-a-glance" tabindex="-1">Reading the window at a glance <a class="header-anchor" href="#reading-the-window-at-a-glance" aria-label="Permalink to “Reading the window at a glance”">​</a></h2><p>Post-Processing uses the same layout as <a href="/KKSS/guide/cad-mode#reading-the-window-at-a-glance">Pre-Processing</a>, so switching modes never changes where things are:</p><ul><li><strong>Menubar</strong> — a <strong>document chip</strong> on the right with the file name, a format badge (<code>MDPA</code>, <code>VTK</code>, …) and, once you have applied operations that are not saved, a dot and <code>N unsaved edits</code>. It clears when you save, and again if you undo back to the last save.</li><li><strong>Sidebar</strong> — sections have an icon and a chevron; <strong>Layers</strong>, <strong>Edit</strong>, <strong>Variables</strong>, <strong>Mesh Modification</strong> and (when a problemtype is active) <strong>Problemtype</strong> are at the top, with the read-only <strong>Information</strong> section folded into a collapsed <strong>Advanced</strong> group. Drag the edge, or focus it and press <code>←</code>/<code>→</code>, to resize.</li><li><strong>Toolbar and dock</strong> — a floating toolbar at the top right (<strong>View</strong> and <strong>Advanced</strong> hold the menus), and a one-row dock along the bottom with reset/fit/zoom, Shaded/Wire, the <strong>clip plane</strong> and Persp/Ortho; the <strong>⋯</strong> button holds rotate and pan steppers, the free clip normal, opacity and <em>Center on model</em>.</li><li><strong>Status bar</strong> — the engine line (<code>meshio++ ready · MMG loading…</code>), the model counts (<code>13,368 nodes · 62,980 elements</code>), the timeline frame (<code>frame 3 / 12 · step 6</code>) and your last <strong>Inspect</strong> pick. <strong>Engines idle</strong> is normal for a plain <code>.mdpa</code>: it only reports an engine after a call that needed it has succeeded, so a model that needs neither meshio++ nor MMG never shows one.</li></ul><h2 id="what-you-can-do" tabindex="-1">What you can do <a class="header-anchor" href="#what-you-can-do" aria-label="Permalink to “What you can do”">​</a></h2><ul><li><strong>Inspect MDPA models</strong>: a vtk.js 3D view with a navigable ModelPart/SubModelPart outline, toggleable layers, node IDs, a background grid, an orientation cube, and a clip plane.</li><li><strong>Visualize results</strong>: nodal/elemental/conditional field data with colormaps. The Field panel offers <strong>combinable display modes</strong> — solid contour, isosurfaces, vector quivers, and a <strong>deformed-shape</strong> warp driven by a displacement field — that can be layered together; VTK time-series play back on a timeline, and new step files written by a running simulation extend it automatically.</li><li><strong>Check mesh quality</strong>: aspect/edge ratio, min/max angles, and size gradation histograms with bad-element highlighting.</li><li><strong>Analyze mesh size</strong>: the <strong>Mesh Size</strong> panel (in the <strong>Advanced</strong> toolbar menu) reports nodal size (Kratos <code>NODAL_H</code> — min distance to a node sharing an element) and element size (mean edge length) as box-whisker statistics with the standard deviation, and highlights the IQR-outlier small/large elements.</li><li><strong>Modify meshes</strong> with full undo/redo (also <code>Ctrl+Alt+Z</code> / <code>Ctrl+Alt+Shift+Z</code>, or the sidebar&#39;s own buttons) and saveable JSON recipes, grouped in six collapsible sidebar subcategories: <em>Element order &amp; topology</em> (merge coincident nodes, remove orphans, scale/translate/rotate, delete/rename/extract SubModelParts, linear→quadratic and the inverse quadratic→linear, <strong>refine</strong> — the whole mesh, cells a per-cell field marks (defaulting to the error estimator&#39;s own output), or a SubModelPart; selected cells split fully and their neighbours get the smallest partial split that keeps the mesh conforming, so the result has no hanging nodes (triangles and tetrahedra only) — simplexify), <em>Remeshing (MMG)</em> (<strong>MMG remeshing</strong> — including a <code>size = ƒ(h)</code> mode that sets each node&#39;s target size from a formula over the nodal size, the whole-mesh statistics and the coordinates, with per-SubModelPart overrides, and an <strong>anisotropic</strong> mode that differentiates a scalar nodal field twice inline and adapts the mesh to the curvature of that solution: fine across a boundary layer, coarse along it. A <strong>Frozen entities &amp; local sizes</strong> block names whole EntityBlocks or SubModelPart subtrees MMG must leave bit-identical — a coupled or contact surface another code owns — and assigns per-block/per-part <code>hmin</code>/<code>hmax</code>/<code>hausd</code> bounds — and <strong>level-set splitting</strong>, whose <strong>Materials &amp; base references</strong> subform can put each split cell back into its original block and SubModelParts (<strong>keep materials</strong>, carrying the side on the generated <code>MMG_Domain_Inside</code>/<code>_Outside</code> parts instead of losing block identity to them), name blocks/parts the split must leave uncut, and delete parasitic components either below a volume fraction (<strong>rmc</strong>) or that don&#39;t touch a named boundary (<strong>base references</strong>) — both run in a worker thread with live progress and cancel), <em>Smoothing &amp; renumbering</em> (Taubin/Laplacian smoothing, RCM/Morton/Hilbert reordering, space-filling-curve partitioning), <em>Selection &amp; combination</em> (box/plane crop, merge another mesh file with optional coincident-node weld), <em>Fields</em> (a safe formula field calculator and nodal↔elemental averaging), and <em>Sphere elements</em>.</li><li><strong>Open a very large mesh as a summary first</strong>: above <strong>Settings ▸ Open Settings… ▸ Mesh Viewer ▸ Large-Mesh Summary Threshold</strong> (default 250 MB; set it to 0 to always load in full) a mesh opens as a header card — node/cell counts, entity blocks, field names, regions, time steps — with an <strong>Open full mesh anyway</strong> button, rather than loading it (and hanging the window) up front. That choice sticks for as long as the tab stays open.</li><li><strong>Work with SPHERE / particle meshes</strong>: one-node elements render as real sphere glyphs scaled in model space (<strong>Advanced ▸ Spheres…</strong>), automatically for a mesh carrying a <code>RADIUS</code> field and with a suggested radius — half the median nearest-neighbour spacing — for the usual radius-less DEM/peridynamics file. <strong>Set element radius</strong> is an ordinary undoable operation that creates or scales the field, optionally limited to one SubModelPart.</li><li><strong>Find inverted elements</strong>: <strong>Advanced ▸ Face normals</strong> draws an arrow on every surface and boundary face; a cell wound against its neighbours points the wrong way, is counted and highlighted in red — invisible in the quality statistics, and a hard error for the solver.</li><li><strong>Unsaved edits are tracked like any other document</strong>: applying an operation marks the tab dirty (a dot in the tab strip), and closing that tab or quitting with it open prompts Save / Don&#39;t Save / Cancel. Edits are never auto-saved, even with the system auto-save setting on — a save always has to be asked for, since re-serializing the mesh can drop comments and formatting the preview doesn&#39;t retain.</li><li><strong>See the document and session at a glance</strong>: a <strong>document chip</strong> at the right of the viewer&#39;s top strip shows the file name, its format badge and — once you apply an operation — a dot and &quot;N unsaved edits&quot;. The count is the operations that differ from what the file on disk holds, so undoing back to the last save clears it immediately (unlike the tab&#39;s dot, which stays until you save). A <strong>status bar</strong> along the bottom shows which WebAssembly engines this session has used (meshio++, MMG, Pyodide; opening a plain <code>.mdpa</code> reads &quot;Engines idle&quot;), the model&#39;s node / element / condition counts, the timeline frame for multi-step data, and the last entity you picked with <strong>Inspect</strong>.</li><li><strong>Save and export</strong>: overwrite the source (with a one-time warning), Save As, or export the whole model — a single SubModelPart, or just the boundary skin of the volume cells (<strong>Advanced ▸ Export skin…</strong>) — to MDPA, VTK, VTU, VTP, STL, OBJ, or PLY, plus ~37 extended formats via meshio++ (Gmsh <code>.msh</code>, Abaqus <code>.inp</code>, Nastran, UNV, Medit, Netgen, SU2, XDMF, Triangle <code>.poly</code>, Exodus, CGNS, MOAB, Salome <code>.med</code> — SubModelParts survive as MED families, and as real <code>*NSET</code>/<code>*ELSET</code> sets in Abaqus — and more, including the field-only <code>.dex</code>/<code>.ip</code>/<code>.mff</code> targets and the write-only SVG/TikZ figure formats, grouped in the Export menu under Solvers, Fields, and Figures).</li><li><strong>Inspect an entity by clicking it</strong>: the <strong>Inspect</strong> toolbar button turns clicks into a probe — id, block, SubModelPart membership and every field value at the picked node/element/condition, with no id needed up front (unlike Find). Its <strong>Measure</strong> sub-mode draws a line between two clicked nodes and reports the distance and Δx/Δy/Δz.</li><li><strong>Isolate a value range</strong>: the Field panel&#39;s <strong>Threshold</strong> mode shows only the cells whose value falls inside an editable window, combinable with contour colouring; contour/isosurface/threshold also take a component selector, a lockable colour range, a log scale, discrete bands, and an in-scene scalar bar that survives a screenshot.</li><li><strong>Define named variables</strong>: the sidebar&#39;s <strong>Variables</strong> section turns a <em>formula</em> over coordinates and existing fields, a <em>distance</em> (to a file, a SubModelPart, or the mesh&#39;s own exterior skin), or a <em>global reduction</em> (min/max/mean/std/median/sum/… of any field) into a field you can play, colour the mesh by and use <strong>by name in any other formula</strong> — including the remesh sizing formula. Every field on the mesh gets a row, so this panel and the Field panel always agree; a reduction stays live as you scrub a time series instead of going stale.</li><li><strong>Remesh with a preset</strong>: the MMG form&#39;s <code>size = ƒ(h)</code> mode offers a <strong>Uniform</strong> and a <strong>Boundary layer</strong> preset. Picking the latter prepares the Variables section for you — a distance row <code>d</code> (its surface stays your call) plus the <code>mean_h</code>/<code>min_h</code>/<code>max_h</code> globals — and the formula scales with the mesh, finest at the wall and growing to the mean size away from it. A remesh now carries data fields onto the new mesh (interpolated for nodal values, taken from the containing cell for elemental/conditional ones) and reports what landed and what was dropped.</li><li><strong>Step through more in-file timelines</strong>: MED, CGNS and Tecplot (<code>.dat</code>/<code>.tec</code>) files open with a real timeline scrubber, sized from the file header before any step is read, the way Exodus already did.</li><li><strong>Clip at any angle</strong>: the nav card&#39;s <strong>Clip</strong> group takes the X/Y/Z presets or a <strong>Free</strong> mode with raw normal-vector inputs, plus flip, an Off/On toggle and a live position readout.</li><li><strong>Split the view</strong>: <strong>View ▾ ▸ Layout</strong> puts the mesh in 1, 2 or 4 viewports, each with its own camera, field settings and clip plane (the panel names the focused pane and offers <strong>Copy to all panes</strong>); layers, colours and the analysis overlays stay shared.</li><li><strong>Run a case and watch it</strong>: the Problemtype section generates ProjectParameters/materials/MainKratos.py and starts the solver as a tracked child process with a real exit code, live progress read from <code>vtk_output/</code>, and a graceful <strong>File ▸ Stop Kratos Run</strong> (SIGINT → SIGTERM → SIGKILL). Runs outlive the tab that started them, and finished results open in a <strong>new</strong> Post-Processing tab jumped to the latest complete step.</li><li><strong>Pack a finished run into one file</strong>: a solve writes one mesh per step, so its results are a directory of hundreds of files. <strong>File ▸ Pack Time Series Into One File…</strong> combines the currently open series into an XDMF time series (<code>.xdmf</code> plus a required sibling <code>.h5</code>; copy both together) — step numbers from the filenames become the time axis — streamed one step at a time, cancellable, and the packed file reopens as the same scrubbable timeline. A lone file or a format that already carries its own steps is refused; so is a series whose mesh changes between steps.</li><li><strong>Record the viewport</strong>: <strong>View ▾ ▸ Record…</strong> captures a camera turntable or a time-series playthrough as WebM, or as numbered PNGs with the exact <code>ffmpeg</code> command to assemble them.</li><li><strong>Render beams as tubes</strong>: <strong>Advanced ▸ Beams…</strong> draws line elements at their real cross-section, sized from each cell&#39;s own <code>Properties</code> <code>CROSS_AREA</code> — enabled automatically only when actual Elements carry one, so a 2D boundary skin keeps drawing as plain lines.</li><li><strong>Tabulate and plot</strong>: <strong>Advanced ▸ Data table…</strong> lists every node/element/condition/geometry as a row (CSV or XLSX export), and Inspect&#39;s <strong>Plot over time</strong> samples one entity&#39;s field across every step of a series, with a chart you can click to jump the 3D view.</li><li><strong>Integrate and check closure</strong>: <strong>Advanced ▸ Field integrals…</strong> reports cell-measure-weighted totals and means per region; the Face-normals status line now also reports watertightness (boundary edges, non-manifold edges, zero-area faces).</li><li><strong>Queue several edits</strong>: a checkbox in the Edit section stages each Apply into a list, then <strong>Apply queued steps</strong> runs them in order under one progress bar — each still its own undoable history entry.</li><li><strong>Reload from disk</strong> (<code>Ctrl+Alt+R</code>): re-reads the file and <strong>replays</strong> the applied edits onto it rather than discarding them; an op that no longer applies is kept and marked rather than dropped.</li><li><strong>Tune the render</strong>: <strong>Advanced ▸ Lighting…</strong> (specular/ambient/diffuse + backface culling, useful for spotting an inverted shell from the inside) and <strong>Advanced ▸ Camera Bookmarks…</strong> (named views for the session, with a JSON textarea to copy one out or paste one in). Keys <code>1</code>–<code>6</code> snap to ±X/±Y/±Z and <code>i</code> to an isometric view; hovering an outline row reveals a per-layer opacity slider.</li><li><strong>Screenshots</strong>: save the current view as a PNG (<strong>View ▾ ▸ Screenshot…</strong>, or <code>Ctrl+Alt+P</code>).</li><li><strong>Turn the edge lines off</strong>: faces and cell edges share one alpha, so a transparent mesh otherwise reads as a wire cage. A global <strong>Edges</strong> switch (<strong>View ▾</strong>, and the nav card&#39;s Display group) hides the darkened edges on every layer; point-only layers stay edge-free regardless.</li><li><strong>See the strays</strong>: nodes no cell references are reported in <code>mesh_info</code> and highlighted, and a SubModelPart isolated with the main mesh hidden now draws the nodes that are isolated <em>within that part</em> — previously invisible, because the global highlight only sees whole-model connectivity.</li><li><strong>Reopen what you had</strong>: <strong>File ▸ Open Recent</strong> lists the last ten files you opened — meshes <em>and</em> CAD models, newest first — with <strong>Clear Recent</strong> at the bottom. Each entry reopens in the mode it belongs to, and files that have since moved or been deleted drop off by themselves. The home screen shows the same list.</li></ul><table tabindex="0"><thead><tr><th>Outline &amp; edit sidebar</th><th>View menu</th></tr></thead><tbody><tr><td><img${ssrRenderAttr("src", mesh_outline_default)} alt="Sidebar" width="816" height="4401"></td><td><img${ssrRenderAttr("src", mesh_view_menu_default)} alt="View menu" width="630" height="936"></td></tr></tbody></table><blockquote><p>The viewer&#39;s toolbar is <strong>Reset · Pan · Quality · Field · Find · Inspect · View ▾ · Advanced ▾</strong>: the display toggles (Node IDs, Grid, Edges) and Screenshot live in <strong>View ▾</strong>, and Clip moved into the nav card. The extension also draws its own File menu in a strip at the top of the view; KKSS hides it, since the native <strong>File</strong> menu already covers Open / Save / Save As / Export — plus <strong>Save Problem…</strong> (<code>Ctrl+Alt+S</code>), <strong>Load Problem…</strong> (<code>Ctrl+Alt+O</code>), <strong>Pack Time Series Into One File…</strong>, and <strong>Undo/Redo Mesh Operation</strong> (<code>Ctrl+Alt+Z</code> / <code>Ctrl+Alt+Shift+Z</code> — not the plain <code>Ctrl+Z</code>/<code>Ctrl+Shift+Z</code> upstream binds, which would otherwise steal the text editor&#39;s own undo), which only exist in that strip, or the Command Palette, upstream.</p></blockquote><h2 id="problemtypes-kratos-case-setup" tabindex="-1">Problemtypes: Kratos case setup <a class="header-anchor" href="#problemtypes-kratos-case-setup" aria-label="Permalink to “Problemtypes: Kratos case setup”">​</a></h2><p>The Problemtype sidebar section turns the loaded MDPA model into a runnable Kratos case: pick a built-in (Structural, Fluid, Convection-Diffusion, Potential Flow, Shallow Water) or a workspace <code>.js</code>/<code>.py</code> problemtype, assign conditions and materials to SubModelParts, then <strong>Generate</strong> writes ProjectParameters.json, the materials file(s), and MainKratos.py next to the mesh; <strong>Run</strong> launches <code>python MainKratos.py</code> in the embedded terminal; <strong>Open results</strong> opens the first <code>vtk_output/</code> file back in the viewer. In KKSS, Run is enabled only when the selected case&#39;s manual interpreter, required built-in application and case directory pass the environment check; it repeats that check before dispatch. The assistant tool runtime is checked independently. The standalone extension keeps its existing launch behavior. Kratos itself isn&#39;t bundled — point Settings at a local install or rely on a pip-installed <code>KratosMultiphysics</code> on PATH.</p><p><strong>Flowgraph</strong> is a built-in that replaces the sidebar forms with an embedded node-editor: selecting it splits the viewport (toggle the split direction, or hide/restore the pane) to show the <a href="https://www.npmjs.com/package/@kratos-flowgraph/flowgraph" target="_blank" rel="noreferrer"><code>@kratos-flowgraph/flowgraph</code></a> graph editor, seeded from the current case&#39;s ProjectParameters.json; its own Generate button writes the graph back the same way as the standard flow. It&#39;s UI-only — there&#39;s no headless/MCP equivalent — and it&#39;s why KKSS is licensed <a href="https://github.com/loumalouomega/KKSS/blob/master/LICENSE" target="_blank" rel="noreferrer"><strong>AGPL-3.0-or-later</strong></a>: Flowgraph itself is AGPL-3.0-or-later, and the mesh engine bundling it moved to AGPL-3.0-or-later too.</p><h2 id="timeline-discovery" tabindex="-1">Timeline discovery <a class="header-anchor" href="#timeline-discovery" aria-label="Permalink to “Timeline discovery”">​</a></h2><p>Opening one file of a Kratos-style series (<code>&lt;prefix&gt;_&lt;rank&gt;_&lt;step&gt;.vtk</code>) discovers its siblings, builds the timeline, and merges rank/subpart files into the outline. The directory is watched, so a still-running simulation keeps extending the timeline as new steps land on disk.</p><p>Exodus (<code>.e</code>/<code>.exo</code>/<code>.ex2</code>) drives the same timeline from the steps recorded <strong>inside the single file</strong>, so no sibling naming is needed; that one file is watched instead of the directory, and a solver still appending steps to it extends the timeline live.</p><p><img${ssrRenderAttr("src", mesh_vtk_timeline_default)} alt="A VTK time series with the playback timeline" width="5280" height="4578"></p></div>`);
}
var _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
	const ssrContext = useSSRContext();
	(ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/mesh-mode.md");
	return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
var mesh_mode_default = /*#__PURE__*/ _plugin_vue_export_helper_default(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
//#endregion
export { __pageData, mesh_mode_default as default };
