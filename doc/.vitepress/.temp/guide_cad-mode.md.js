import { t as _plugin_vue_export_helper_default } from "./plugin-vue_export-helper.BOaGB7Aw.js";
import { t as cad_viewer_default } from "./cad-viewer.CRIx3Wuk.js";
import { ssrRenderAttr, ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
//#region ../../../../../screenshots/cad-components-tree.png
var cad_components_tree_default = "/KKSS/screenshots/cad-components-tree.png";
//#endregion
//#region ../../../../../screenshots/cad-file-menu.png
var cad_file_menu_default = "/KKSS/screenshots/cad-file-menu.png";
//#endregion
//#region ../../../../../screenshots/cad-fe-mesh-panel.png
var cad_fe_mesh_panel_default = "/KKSS/screenshots/cad-fe-mesh-panel.png";
//#endregion
//#region ../../../../../screenshots/cad-edits-panel.png
var cad_edits_panel_default = "/KKSS/screenshots/cad-edits-panel.png";
//#endregion
//#region ../../../../../screenshots/cad-parts-panel.png
var cad_parts_panel_default = "/KKSS/screenshots/cad-parts-panel.png";
//#endregion
//#region ../../../../../screenshots/cad-view-controls.png
var cad_view_controls_default = "/KKSS/screenshots/cad-view-controls.png";
//#endregion
//#region guide/cad-mode.md
var __pageData = JSON.parse("{\"title\":\"Pre-Processing (CAD) Mode\",\"description\":\"\",\"frontmatter\":{},\"headers\":[],\"relativePath\":\"guide/cad-mode.md\",\"filePath\":\"guide/cad-mode.md\"}");
var _sfc_main = { name: "guide/cad-mode.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
	_push(`<div${ssrRenderAttrs(_attrs)}><h1 id="pre-processing-cad-mode" tabindex="-1">Pre-Processing (CAD) Mode <a class="header-anchor" href="#pre-processing-cad-mode" aria-label="Permalink to “Pre-Processing (CAD) Mode”">​</a></h1><p>Pre-Processing mode embeds the full <a href="https://loumalouomega.github.io/CAD-Preview/" target="_blank" rel="noreferrer">CAD-Preview</a> viewer. Everything documented for the extension applies inside KKSS too — this page summarizes the highlights; see the <a href="https://loumalouomega.github.io/CAD-Preview/" target="_blank" rel="noreferrer">CAD-Preview documentation</a> for the complete feature guide.</p><p><img${ssrRenderAttr("src", cad_viewer_default)} alt="CAD mode: bull.stp with the components tree, parts, edits and FE mesh panels" width="5280" height="4578"></p><h2 id="what-you-can-do" tabindex="-1">What you can do <a class="header-anchor" href="#what-you-can-do" aria-label="Permalink to “What you can do”">​</a></h2><ul><li><strong>View</strong> STEP, IGES, and BREP models (tessellated by OpenCascade in a background worker) and STL, OBJ, PLY, glTF/GLB meshes (loaded natively by Three.js), with orbit/pan/zoom, an orientation cube, five <strong>display modes</strong> (Shaded, Wireframe, X-Ray, Hidden Lines, Flat), a capped <strong>clipping plane</strong>, an exploded-view slider, background/opacity controls, an orthographic/perspective toggle, and a searchable component tree with per-part isolate/hide. A STEP or IGES file&#39;s declared length unit is detected and preselected.</li><li><strong>Import mesh-only formats</strong>: VTK/VTU, MED, CGNS, Exodus, XDMF and Kratos MDPA open as a boundary surface through meshio++, with named regions turned into Parts and any scalar point/cell field usable to <strong>colour the model</strong>. Geometry only — for fields, blocks and SubModelParts, open the file in Post-Processing instead (which is where a double-click lands it).</li><li><strong>Define parts</strong>: pick volumes, surfaces, lines, or points and group them into named parts (Kratos sub-model-parts). Assignments persist to a <code>&lt;model&gt;.parts.json</code> sidecar — the CAD file is never modified. After a topology-changing edit, part assignments are geometrically <strong>rebound</strong> to the renumbered entities instead of being silently lost.</li><li><strong>Edit geometry parametrically</strong>: transforms, booleans, fillets/chamfers, feature modeling (extrude/revolve/sweep/loft — loft optionally steered by a <strong>guide rail</strong>, a resampling fallback rather than the kernel&#39;s own rail wiring, on exactly two closed sections), <strong>wrap</strong> (develops a flat sketch face onto a cylinder/cone target — Standalone appends the wrapped shell, Emboss fuses it into the target volumes, Engrave cuts it out), primitives, 2D sketches, bottom-up wireframe modeling, and named variables with expressions. The ordered op-list persists to <code>&lt;model&gt;.edits.json</code> and replays on every open.</li><li><strong>Measure and inspect</strong>: distance, edge length, angle and radius as a live overlay, each with an <strong>⟟ Exact</strong> button that recomputes against the true OCCT geometry instead of the triangulation; a measurement can be <strong>pinned</strong> to the model so it persists across sessions. Plus a <strong>Mass Properties</strong> panel (volume, surface area, centre of mass, moments of inertia) for the whole model or one entity.</li><li><strong>Keep your place</strong>: the camera, display mode, projection and clip plane are saved per document, and the components tree reads a STEP file&#39;s real XCAF assembly structure. Edits replay from a cached parse, so an interactive change re-tessellates in a fraction of the original load time. Sidecars and the source are watched, so an external change reloads in place.</li><li><strong>Annotate</strong>: freehand, line, arrow, rectangle and circle markup drawn over the 3D view, with undo/redo and an eraser, baked into screenshots.</li><li><strong>Generate FE meshes with Gmsh</strong> (WASM): size controls, element shape (tets/hexes/hex-dominant) and order (linear/quadratic), per-part mesh sizes, physical groups from your parts, a live mesh overlay, and a <strong>quality summary</strong> with the worst elements highlighted through the model.</li><li><strong>Export</strong>: STEP/IGES/BREP (via OCCT), STL/OBJ/PLY/glTF (via Three.js), and FE meshes to Kratos <strong>MDPA</strong>, Gmsh <code>.msh</code>, VTK, UNV, Abaqus, Nastran, SU2, MED, CGNS, XDMF, and more — each optionally <strong>unit-converted</strong> (mm/cm/m/in/ft) on the way out. <strong>File ▸ Screenshot…</strong> (<code>Ctrl+Alt+P</code>) saves the current view as a PNG.</li><li><strong>Split the view and link cameras</strong>: 1, 2 or 4 panes each with their own camera (persisted to <code>&lt;model&gt;.view.json</code>), plus a <strong>Link cameras</strong> toggle that keeps every open CAD tab looking the same way.</li><li><strong>Preview an edit before applying it</strong>: the drafted operation is replayed and drawn tinted by intent (green additive, red subtractive) without ever entering the op stack.</li><li><strong>Explain what&#39;s under the cursor</strong>: a hover tooltip names the entity and which ops mention it; the inspector card reports the analytic surface type and its parameters, and right-click builds <strong>selection groups</strong> from a query vocabulary (by direction, planarity, area, length, largest/smallest N).</li><li><strong>Save and re-run macros</strong>: named, parameterized scripts in a folder-level library — a macro&#39;s ops land on the normal undo stack exactly like hand-applied edits.</li><li><strong>Name construction planes</strong>: save the current clip plane, enter one numerically, derive one from a picked face or three points, or build a midplane between two — persisted to <code>&lt;model&gt;.planes.json</code>.</li><li><strong>Repair and promote meshes</strong>: the Mesh Health panel checks whether an STL/OBJ/PLY/glTF skin is healable, <strong>Repair (robust)</strong> makes it watertight with fTetWild, and <strong>Promote to B-rep</strong> turns a clean one into a STEP/IGES/BREP solid. <strong>Region fit</strong> grows a region from a picked point and fits a plane, cylinder or sphere to it, each with its own residual.</li><li><strong>Export 2D drawings</strong>: silhouette <strong>SVG</strong> or <strong>DXF</strong> from any named or current view, or a full <strong>technical drawing</strong> with hidden-line removal — with pinned tolerance bands rendered as real dimensions.</li><li><strong>Import 2D</strong>: SVG paths and DXF entities trace into B-rep sketch polylines.</li><li><strong>Start from nothing</strong>: <strong>File ▸ New Blank Model…</strong> creates an empty <code>.brep</code> and opens it, so the Edits panel&#39;s whole creation vocabulary — primitives, 2D sketch profiles, bottom-up wireframe modeling, booleans, fillets, patterns — works from scratch rather than only on top of an existing model. The file itself stays an empty compound; everything you author lives in the <code>.edits.json</code> sidecar exactly as it does for an edited STEP, so <strong>File ▸ Export…</strong> or <strong>Save Preprocess…</strong> is what produces a standalone file. It refuses to overwrite an existing model rather than leaving its edit history replaying against nothing.</li><li><strong>Open OpenSCAD models</strong>: <code>.csg</code> (OpenSCAD&#39;s fully evaluated form) is parsed and built into a solid like any other imported source. A <code>.scad</code> source is converted to <code>.csg</code> first by a <strong>user-installed <code>openscad</code> binary</strong> — point <strong>Settings ▸ Open Settings… ▸ CAD Viewer ▸ OpenSCAD Binary</strong> at it, or leave it unset to resolve <code>openscad</code> on <code>PATH</code>. Both are import-only: export goes to STEP/IGES/BREP or any mesh target, never back to <code>.csg</code>. Anything the importer has to approximate or skip (a <code>hull()</code>, a faceted cylinder) is reported on the status line rather than silently dropped.</li><li><strong>Pin an operand as a query</strong>: instead of baking a positional face id into an edit, the extrude/revolve/shell/draft forms&#39; <strong>Pin query</strong> row records a re-executable <em>query</em> for the selected face — re-matched geometrically on every replay, so the op keeps naming the right surface after the op list is spliced. A query that can no longer be derived freezes on its cached ids with a status line rather than silently resolving to the wrong entity. Parts carry the same mechanism and are re-resolved on open.</li><li><strong>Collapse what you&#39;re not using</strong>: every sidebar section — Components, Parts, Edits, FE Mesh, Mass Properties, Mesh Health, Region fit, Macros, Standard Parts — has a chevron that collapses it to just its header, independently. The layout is remembered per document in <code>&lt;model&gt;.view.json</code>.</li><li><strong>Resize the sidebar</strong>: drag its edge (or focus the handle and press <code>←</code>/<code>→</code>, <code>Home</code>/<code>End</code>) between 176 and 420 px; the width is remembered per document. Dropdown menus navigate with the arrow keys and close on <code>Esc</code>.</li><li><strong>Bookmark views</strong>: <strong>View ▾ ▸ Save current view…</strong> names the camera orientation, projection, display mode and clip plane, and lists it in the same menu to restore, replace, rename or delete. Bookmarks live per document in <code>&lt;model&gt;.view.json</code>; a restore reframes from the model&#39;s <em>current</em> extents, so it stays meaningful after an edit.</li><li><strong>Reuse meshing presets</strong>: the FE Mesh panel&#39;s <strong>Saved presets</strong> section applies or saves a named bundle of mesh options (stored in mm, with a pinned engine). Four starters — <code>coarse-preview</code>, <code>balanced</code>, <code>fine-detail</code>, <code>robust-repair</code> — ship read-only; your own live in a folder-level <code>cad-preview-mesh-presets.json</code> shared by every model beside it, and the assistant reads and writes the same file. The assistant can also <strong>compare refinement</strong>: it meshes one model at several explicit sizes and reports cost against element quality (density trends are not solver convergence).</li><li><strong>Sketch on a named plane</strong>: a circle, rectangle or polygon profile can be placed from a construction plane plus in-plane offsets (and a rotation), so moving the plane moves everything authored on it.</li><li><strong>Select by volume or point, and zoom to it</strong>: <strong>Select ▾</strong> filters also cover volumes (size, centre, largest/smallest N) and points (near a plane, near or inside the selection), and <strong>Zoom to selection</strong> frames the current selection. <strong>Clash ▸ Check all</strong> takes a pair/boolean budget; pairs past it are reported as <em>unchecked</em>, never as clash-free.</li><li><strong>Find standard parts faster</strong>: search results show a thumbnail beside each part; the text row stays if an image can&#39;t be fetched.</li></ul><table tabindex="0"><thead><tr><th>Components tree</th><th>File menu</th><th>FE Mesh panel</th></tr></thead><tbody><tr><td><img${ssrRenderAttr("src", cad_components_tree_default)} alt="Components tree" width="816" height="246"></td><td><img${ssrRenderAttr("src", cad_file_menu_default)} alt="File menu" width="630" height="1299"></td><td><img${ssrRenderAttr("src", cad_fe_mesh_panel_default)} alt="FE Mesh panel" width="816" height="1017"></td></tr></tbody></table><p>The toolbar is <strong>Fit · Tree · FE Mesh</strong> plus four dropdowns — <strong>View ▾</strong> (Grid, Edges, Screenshot), <strong>Select ▾</strong> (selection mode + Point/Vol/Surf/Line), <strong>Measure ▾</strong> and <strong>Markup ▾</strong> — with the display modes, clip, appearance and unit controls in the view-controls panel.</p><h2 id="reading-the-window-at-a-glance" tabindex="-1">Reading the window at a glance <a class="header-anchor" href="#reading-the-window-at-a-glance" aria-label="Permalink to “Reading the window at a glance”">​</a></h2><p>A CAD tab is laid out the same way in every version of the viewer:</p><ul><li><strong>Menubar</strong> — the <strong>File</strong> menu, and on the right a <strong>document chip</strong> with the open file&#39;s name, a format badge (<code>STEP</code>, <code>STL</code>, …) and, when it has edits the source file does not yet contain, a dot and a count (<code>3 unsaved edits</code>). Hover the chip for the full path.</li><li><strong>Sidebar</strong> — the four sections that edit the document (<strong>Components</strong>, <strong>Parts</strong>, <strong>Edits</strong>, <strong>FE Mesh</strong>) sit at the top, each with an icon. The read-only analysis sections (Mass Properties, Clash, Mesh Health, Region fit, Primitives) and the two libraries (Macros, Standard Parts) are folded into one collapsed <strong>Advanced</strong> group, with a count of how many it holds.</li><li><strong>Toolbar and dock</strong> — the toolbar floats at the top right; the dock along the bottom holds navigation, display mode, the clip plane and perspective, with less-used controls behind its <strong>⋯</strong> button.</li><li><strong>Status bar</strong> — along the bottom: which <strong>kernels</strong> have loaded (<code>OCCT ready · Gmsh ready</code>), the entity counts (<code>36 faces · 98 edges · 64 points</code>), the generated FE mesh (<code>mesh 10,000 el · min SICN 0.412</code>) and the live cursor position on the model, in the units chosen in the dock.</li></ul><h3 id="why-unsaved-edits-does-not-go-away" tabindex="-1">Why &quot;unsaved edits&quot; does not go away <a class="header-anchor" href="#why-unsaved-edits-does-not-go-away" aria-label="Permalink to “Why &quot;unsaved edits&quot; does not go away”">​</a></h3><p>CAD-Preview keeps your edits in the <code>&lt;model&gt;.edits.json</code> sidecar and only writes them into the source file when it <em>bakes</em> them. KKSS never bakes — there is no save-into-the-source action — so an edited STEP, IGES, BREP, STL, OBJ or PLY keeps reading <strong>N unsaved edits</strong>. That is accurate rather than a fault: the <em>source file</em> does not contain what is on screen, while the sidecar (autosaved) does, and <strong>File ▸ Export…</strong> or <strong>Save Preprocess…</strong> is what writes a standalone file. Undoing back to the file&#39;s own state clears the chip.</p><p><strong>Kernels idle</strong> is also normal: the OCCT and Gmsh engines load lazily and the line only reports one after a call that needed it has succeeded, so a plain STL open, which uses neither, honestly reads idle.</p><h2 id="viewer-defaults" tabindex="-1">Viewer defaults <a class="header-anchor" href="#viewer-defaults" aria-label="Permalink to “Viewer defaults”">​</a></h2><p><strong>Settings ▸ Open Settings… ▸ CAD Viewer</strong> seeds a newly opened document: <strong>Up Axis</strong> (Y or Z), <strong>Default Mesh Size</strong> (Coarse/Medium/Fine), <strong>Tessellation Quality</strong> (Draft/Standard/Fine — how finely a B-rep is triangulated, traded against load time) and <strong>Show Grid &amp; Axes on Open</strong>. They are only the starting point — a per-document <code>&lt;model&gt;.mesh.json</code> or <code>&lt;model&gt;.view.json</code> value, or a runtime toggle in the view controls, always wins once set. Tessellation quality is re-read on every B-rep load, so a change applies at the next edit or reopen.</p><table tabindex="0"><thead><tr><th>Edits panel</th><th>Parts panel</th><th>View controls</th></tr></thead><tbody><tr><td><img${ssrRenderAttr("src", cad_edits_panel_default)} alt="Edits panel" width="816" height="2874"></td><td><img${ssrRenderAttr("src", cad_parts_panel_default)} alt="Parts panel" width="816" height="111"></td><td><img${ssrRenderAttr("src", cad_view_controls_default)} alt="View controls" width="2514" height="120"></td></tr></tbody></table><h2 id="sidecar-files" tabindex="-1">Sidecar files <a class="header-anchor" href="#sidecar-files" aria-label="Permalink to “Sidecar files”">​</a></h2><p>Pre-Processing mode never writes your CAD file. State lives beside it:</p><table tabindex="0"><thead><tr><th>File</th><th>Contents</th></tr></thead><tbody><tr><td><code>&lt;model&gt;.parts.json</code></td><td>Part definitions (entity ids, colors, mesh sizes)</td></tr><tr><td><code>&lt;model&gt;.edits.json</code></td><td>Replayable edit operations + parametric variables</td></tr><tr><td><code>&lt;model&gt;.annotations.json</code></td><td>Pinned measurements</td></tr><tr><td><code>&lt;model&gt;.view.json</code></td><td>Camera, display mode, projection, clip plane, split layout, sidebar width, view bookmarks</td></tr><tr><td><code>&lt;model&gt;.mesh.json</code></td><td>Gmsh meshing options</td></tr><tr><td><code>&lt;model&gt;.planes.json</code></td><td>Named construction planes (resolved point + normal, never a face reference)</td></tr><tr><td><code>&lt;model&gt;.geo</code></td><td>Generated Gmsh script (one-way; regenerated on change)</td></tr><tr><td><code>&lt;dir&gt;/cad-preview-macros.json</code></td><td>The macro library — <strong>per folder</strong>, shared by every model beside it</td></tr><tr><td><code>&lt;dir&gt;/cad-preview-mesh-presets.json</code></td><td>Your saved meshing presets — <strong>per folder</strong>, like the macro library (the four bundled starters ship inside the app and are not written here)</td></tr></tbody></table><p><strong>Save</strong> (<code>Ctrl+S</code>) flushes all sidecars immediately; otherwise they autosave half a second after each change. Every sidecar write is atomic — a temp file, then a rename — so a reader can never see a half-written one. That matters most when the model lives in a folder a desktop sync client is watching (Drive, Dropbox, OneDrive): it can no longer upload a truncated sidecar or raise a spurious &quot;conflicted copy&quot;.</p><p><strong>File ▸ Save Preprocess…</strong> bundles the CAD source and whichever sidecars exist into a single <code>.zip</code>, and <strong>Load Preprocess…</strong> restores one next to a destination you pick and opens it. The archive carries a manifest with a SHA-256 per entry, so a tampered or truncated file is rejected rather than half-restored; the reader also refuses entries that decompress far beyond their stored size. The <code>.geo</code> script is deliberately not packed — it is regenerated from the restored mesh options.</p></div>`);
}
var _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
	const ssrContext = useSSRContext();
	(ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/cad-mode.md");
	return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
var cad_mode_default = /*#__PURE__*/ _plugin_vue_export_helper_default(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
//#endregion
export { __pageData, cad_mode_default as default };
