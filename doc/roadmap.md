# Roadmap

Candidate work for future KKSS releases, prioritized by value versus effort given what the app already ships: two upstream VS Code extensions running side by side — one *ported* (`cadHost.ts`), one *shimmed* behind a fake `vscode` module — with scoped workflow-contract changes committed on their integration branches, OCCT and Gmsh in a compute worker, MMG and meshio++ in the mesh submodule's own workers, a forked Flowgraph server, custom `kkss://` and `kkss-file://` schemes standing in for VS Code's webview plumbing, five screens over stacked `WebContentsView`s (home, the two mode screens — each holding any number of open documents as tabs — the text editor, plus a terminal panel and shared Chat/Jobs sidebar), an LLM agent loop over three MCP servers whose write-shaped tool calls are approved in the transcript before they run — optionally after being validated there without running — and whose image results are shown in the sidebar, a per-conversation token and cost readout, request-side compaction that keeps a long conversation inside the model's context window, an optional HTTP meta MCP server that re-exposes the same toolset, a cloud staging layer that opens Drive/Dropbox/OneDrive documents as ordinary local files, and a streamed-desktop Docker image. Many items below are cheap precisely because that infrastructure exists; a few are expensive precisely because of where its boundaries fall.

This page is aspirational, not a commitment — items may be re-ordered, re-scoped, or dropped. Effort is a rough order of magnitude: **S** (a day or two), **M** (roughly a week), **L** (multi-week).

Everything previously shipped is tracked in `CHANGELOG.md`, and `CLAUDE.md` has the verified implementation details and non-negotiable invariants for anything currently in the codebase — this page is for what's **not** built yet, only.

## How this file works

- **Tiers are ordered, and the order is the recommendation.** Each tier states an *admission criterion*; an item that doesn't meet it belongs in a different tier or in Non-goals, not at the top because it sounds exciting. **An empty tier is removed from the file entirely, not kept as a placeholder** — if a new item shows up later that needs a tier no longer listed here (e.g. a defect-shaped correctness/robustness finding, which is always top priority regardless of effort size), recreate that tier at the top rather than burying it under whatever tier happens to be listed first at the time.
- **A closed item is removed from this list entirely**, not struck through — its write-up moves to `CLAUDE.md` (a per-feature section with the verified implementation details) and its history stays in git. Numbering is renumbered to stay consecutive whenever an item closes, so a reference like "item 5" always means the file's current 5th item, not a fossil of one that shipped.
- **Every item states its chat-parity obligation.** `CLAUDE.md`'s "Keep chat features in sync" rule requires a new app capability to reach the assistant in the *same* change: the capability description in `app/main/services/chat/chatService.ts`'s system prompt, the server wiring in `app/main/services/chat/mcpManager.ts` if a new tool source is involved, the **Settings ▸ LLM Assistant** menu if it's configurable, and `ChatDeps.currentFiles()` / the context suffix if it's new context rather than a new ability (the system prompt stays byte-stable for prompt caching). Items whose surface the assistant genuinely cannot reach — window chrome, packaging, CI — say so explicitly, so the estimate includes the wiring rather than discovering it late.
- **Every item also carries a doc-parity cost.** `doc/`, `README.md` and `CLAUDE.md` move with the change, not after it; anything that alters the shell toolbar, the generated webview pages, or visible viewer behavior also means re-running `npm run docs:screenshots` (the PNGs are generated from the live app, never hand-captured).
- **Items marked *needs upstream submodule work* cannot land in this repo alone.** A change inside `cad/` or `mesh/` goes to that submodule's own branch (`kkss.dev` for MCP contracts, `application-downstream` for extension code), is committed there, and is integrated here through its gitlink — never as an uncommitted patch or on the submodule's default branch. No such item may be *estimated* until the upstream half is scoped; each one names what it needs.
- Most items close a **known, documented limitation** of something that already ships, or are a natural next step identified while building it. A rare one is **defect-shaped** — describing behavior that loses data, corrupts state, or strands a session rather than merely lacking a feature. Those belong in the top tier regardless of effort size, queued here rather than filed separately because the fix and the feature are usually the same work.

## Magnusim review — 2026-09-21

Reviewed the pinned source tree at `af8d05945d3b65cd9daf220554cb08ab0392da2d`, following queue transitions, study cloning, requirement checks, mesher diagnostics, transient settings and result exporters into their implementation and available tests. Compared those paths with `app/main/services/jobs.ts`, CAD's `meshOptions.ts`/`gmshSizingFields.ts`/`gmshService.ts`, and mesh's problemtype, field-reader, threshold and integral code. This was a source review; Magnusim's WSL/OpenFOAM pipeline was not executed here.

| Evidence | Product opportunity and owner |
| --- | --- |
| [`jobQueueOrder.ts`](https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/src/workbench/jobQueueOrder.ts), especially `queueHasMeshDepViolation`, `queueKickAfterStart` and persisted-queue merging; [queue tests](https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/src/workbench/jobQueueOrder.test.ts) | KKSS: a persistent execution queue that understands mesh → solve dependencies, beyond displaying existing Kratos jobs. |
| [`w17-simulation.js`](https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/scripts/w17-simulation.js) remaps mesh/run references when cloning a study; [`hierarchy.py`](https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/project/hierarchy.py) defines ownership | KKSS: clone a study safely, inspect parameter differences and compare resulting quantities. A bounded parameter sweep is our proposed extension, not a claim about Magnusim. |
| [`requirements.py`](https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/registry/requirements.py) checks declared prerequisites | KKSS: capability-aware setup and action availability. Its `version_spec` is stored but not enforced by the checker, so version compatibility needs real probes here. |
| [`cad/gaps.py`](https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/cad/gaps.py), [`cad/volume.py`](https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/cad/volume.py), [`mesh/cad_adherence.py`](https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/mesh/cad_adherence.py) | CAD: narrow-gap preflight, meshing cost estimates and measured geometry fidelity, scoped in `cad/doc/roadmap.md`. |
| [`project/transient.py`](https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/project/transient.py), [`case/function_objects.py`](https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/case/function_objects.py), [`units/pressure.py`](https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/units/pressure.py) | Mesh: time-step/output planning, conservation reports and physical field units, scoped in `mesh/doc/roadmap.md`. |

**Limits found during review:** [`mesh_refinements.py`](https://github.com/Lilmill2000/Magnusim/blob/af8d05945d3b65cd9daf220554cb08ab0392da2d/magnusim-web/python/cfddesk/project/mesh_refinements.py) explicitly contains persist-only controls, so a refinement menu entry is not evidence of mesher support. The numerical helpers use heuristics (cell-count bands, passage equivalent diameters, sampled deviation) that must keep their assumptions visible. Existing KKSS jobs, mesh presets, thresholding, recording, field integrals and split views are foundations to extend, not features to re-list as missing. No OpenFOAM/WSL backend migration is implied.

Detailed geometry/export work belongs to the CAD roadmap (`cad/doc/roadmap.md`), numerical setup/results work to the mesh roadmap (`mesh/doc/roadmap.md`), and cross-mode execution/navigation to KKSS.

## Queued

### Tier 0 — Submodules stay current

*Admission: keeping `cad/` and `mesh/` at their latest release, with the features they gain actually reachable from the app.*

1. **Gitlink currency, with the mirror checklist a bump actually requires** (**S** per bump, recurring). Both submodules must track their latest release and the new features must be *integrated*, not merely present in the tree — a bump that builds is not a bump that works. The full checklist lives in `doc/guide/development.md` under **Submodule release maintenance**: review the upstream notes and diff on the right branches (`kkss.dev` for MCP changes, `application-downstream` for extension code); run `npm run package --prefix mesh` before the parent build and confirm `out/meshio/dist/meshioplusplus_wasm*` lists all four files; mirror `tools/webviewMarkup.ts`'s mesh body element for element against `buildPreviewHtml`; extend `vscodeShim.ts` rather than patching, and give menubar-only actions native menu routes; classify new MCP tools in `toolPolicy.ts`; build the CAD MCP kernel via `cadMcpWorkerConfig`, never copy upstream's artifact; then typecheck, test, smoke, docs and screenshots. *Chat parity:* a new viewer or file capability is only useful to the assistant if the submodule's own MCP server exposes it — if it doesn't, that is upstream work on the submodule's `kkss.dev` branch, and the gitlink bump waits for it. A new **app-level** ability arriving with the bump needs the system-prompt description updated in the same change.

### Tier 1 — Distribution and deployment

*Admission: the app builds and ships correctly, but is not yet easy to install, trust, or reach. Nothing in this tier is a prerequisite for anything above it.*

2. **macOS signing, notarization and hardened runtime** (**M**, remaining). Hardened-runtime entitlements, optional signing inputs and CI validation are wired, while the default release remains explicitly unsigned until Developer ID and notarization credentials are configured. Close this item after a credentialed macOS build signs and staples both DMG and ZIP artifacts, passes signature/Gatekeeper checks, and enables the signed-build updater path. *Chat parity: none* (packaging).

3. **Streamed-desktop sharing and resize validation** (**S**, remaining). The x11vnc and TigerVNC policies now reject additional viewers while preserving the first connection. Validate both display backends on native amd64 and arm64 runners, including second-client rejection, reconnect, CAD/mesh rendering and TigerVNC resize; Xvfb remains the default. Keep the entrypoint's Electron flags synchronized with `tools/smoke.e2e.mjs`/`tools/e2eShared.mjs`. *Chat parity: none* (deployment).

### Tier 2 — Deployment validation

*Admission: the authenticated, persistent browser-session implementation is shipped; only acceptance that requires Docker or graphics hardware remains here.*

4. **Container and GPU acceptance validation** (**S**, needs Docker CI and an Intel/AMD hardware runner). The compose, gateway, file browser, broker, Kratos image variant and CI checks are implemented and covered by local type, unit, Go, YAML and documentation checks. Run the Docker boot/login/stop/reconnect checks, offline Kratos import and MCP simulation, and the hardware-renderer/CAD/mesh/TigerVNC resize checks on the appropriate runners before closing this item. Keep Xvfb plus SwiftShader as the tested default, and fail clearly when `/dev/dri` or graphics initialization is unavailable. *Chat parity: none* (deployment/test infrastructure).

## Non-goals / known constraints

- **Keeping app-specific submodule patches uncommitted or on a release branch** — shared workflow contracts can require upstream changes; commit those on the designated integration branches and move the parent gitlinks only to their committed revisions.
- **Duplicating viewer actions in the native menu** — the menu bar holds app-level items only. Quality metrics, field visualization, find-entity and the rest live in the submodules' own toolbars; mirroring them would create two surfaces to keep in sync and one to forget.
- **Merging the two modes into one viewer or one document** — the asymmetric reuse (a 1:1 *port* for CAD because its provider imports OCCT directly, a verbatim *shim* for mesh) and two independent WebGL renderers mean this is a rewrite of the thing that makes the app cheap to maintain, not a feature.
- **A dirty-state prompt when closing a tab** — cad and mesh have no app-level "unsaved changes" concept; their sidecars autosave on a debounce, so closing a tab is a plain dispose, exactly like replacing a tab's document has always been. Adding a prompt would mean inventing a document-dirty model the submodules don't have.
- **Bundling Kratos or a Python runtime in the installer** — the package is already large (OCCT alone is ~63 MB of WASM, and `asar: false` means it ships unpacked); `uvx` sources the Kratos server on demand. The app-local uv bootstrap improves that path rather than replacing it. The `-kratos` image is a separate tag of a separate distribution, and the installer stays lean.
- **Operating a hosted KKSS service** — the repo ships the unit of deployment (the image), the authenticated web front end and a reference per-user orchestrator so that *someone else* can run one. User accounts beyond what an OIDC provider supplies, billing, quotas, backups of user volumes and an SLA are the operator's concern, not this project's; the single-container image stays the default, and item 3 keeps its one-session scope explicit rather than implied.
- **Hand-editing generated artifacts** — `out/renderer/*/index.html`, `app/renderer/shell/shellIcons.ts`, the doc screenshots and the icon SVG/PNGs are all pipeline output. Edit the source and re-run the pipeline.
- **Telemetry or usage analytics** — not planned, recorded here so it isn't re-proposed. The chat sidebar's token and cost readout is not an exception to this: it is computed in the main process from what the provider returned, shown only to the user whose key paid for it, and transmitted nowhere. Crash *diagnostics* the user can read and send by hand are a different thing and are not excluded; the renderer-crash toasts are the seed of one.
- **A second native module** — node-pty is deliberately the only one, and the only `node_modules` entry that ships. It is N-API, so it needs no electron-rebuild; anything requiring a rebuild step would change the release matrix for every platform at once.

## Dependencies

A few items must land in order, and getting it wrong means rework:

- **3 → 4.** The remaining sharing and resize validation must be settled before the container and hardware acceptance checks can close.
